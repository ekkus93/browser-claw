# BrowserClaw Project Memory

Persistent cross-session context. Newest entries at the bottom. See the "Memory file" section in `CLAUDE.md` for the workflow and entry format.

## 2026-06-12T00:45:44Z - Claude Opus 4.8 - Initialized project memory
- Confirmed the `memory.md` workflow from CLAUDE.md: read at session start, update before responding, timestamped `## <UTC time> - <model> - <desc>` headings, never guess timestamps (use `date -u`).
- Ran `/init` this session: created project `CLAUDE.md` and a `/qa` skill (`.claude/skills/qa/SKILL.md`) that runs typecheck/lint/tests + a decrypted-secret-leak grep.
- Project status: greenfield. Only specs/mockups in `docs/browserclaw_text_mockups/`; no app code scaffolded yet. Phase 0 (React+TS+Vite+Tailwind+Redux+Router+Dexie) not started.
- Conventions: package manager is **pnpm**; solo git workflow committing directly to `master`; GitHub remote is `git@github.com:ekkus93/browser-claw.git`.
- Note: this project `memory.md` is separate from Claude Code's built-in memory store; they do not sync.

## 2026-06-12T01:01:33Z - Claude Opus 4.8 - Baseline commit + Ralph Loop ground rules agreed
- Committed and pushed the project baseline (commit `fc3706a`): extracted `docs/` specs/mockups, `CLAUDE.md`, `/qa` skill, `memory.md`; removed redundant handoff `.zip`; gitignored `*~` and `*.zip`. master is in sync with origin.
- Ralph Loop ground rules confirmed with user (phil) before starting:
  1. `run_claude.sh` is the user's personal restart shortcut (gitignored) — NOT the Ralph driver and not my concern.
  2. No verification suite exists until Phase 0 lands the toolchain; that's expected. First iterations bootstrap it.
  3. Task unit = one TODO leaf checkbox → commit; I MAY break large leaves into smaller subtasks freely.
  4. **If stuck, STOP and ask the user** — do not thrash or fake completion.
  5. **Push to GitHub periodically** after each task/subtask. User's priority: full history must live on GitHub (rollback is fine, but GitHub is source of truth).
  6. `memory.md` should be a MORE detailed history than git commit messages; I may add anything that could matter later; user may also request entries.
- Source of truth for "what's next": `docs/browserclaw_text_mockups/BROWSERCLAW_UI_TODO.md` (Phase 0 → Phase 13). Iterations should tick `[ ]`→`[x]` as work completes.

## 2026-06-12T01:08:41Z - Claude Opus 4.8 - Ralph iteration 1: Phase 0 scaffold + tooling
- Scaffolded the Vite app directly in repo root (manual scaffold — `create vite` won't run in a non-empty dir).
- Installed toolchain (exact versions in package.json / pnpm-lock.yaml). Notable, all current/cutting-edge: React 19.2, Vite 8.0, TypeScript 6.0, Tailwind **4.3** (`@tailwindcss/vite` plugin + `@import "tailwindcss"` + `@theme` block — NO tailwind.config.js / postcss), Vitest 4.1, ESLint **10** (flat config), RTK 2.12, react-router 7.17, Dexie 4.4.
- Files added: `index.html`, `vite.config.ts` (Vitest config inlined via `test` key, jsdom + globals + `src/test/setup.ts`), `tsconfig.json`/`tsconfig.app.json`/`tsconfig.node.json` (strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`), `src/main.tsx`, `src/App.tsx`, `src/App.test.tsx`, `src/index.css` (minimal design tokens as Tailwind `@theme` vars — full token mapping deferred to Phase 1), `eslint.config.js`, `.prettierrc.json`, `.prettierignore`.
- GOTCHA (ESLint 10): spreading `reactHooks.configs['recommended-latest']` / `reactRefresh.configs.vite` as full preset objects fails ("plugins must be object, not string array"). FIX: register `react-hooks`/`react-refresh` in a `plugins: {}` object and pull in only their `.rules`. See `eslint.config.js`.
- Prettier scope: `.prettierignore` excludes `docs/` (canonical specs — do NOT reformat) and hand-maintained root meta (`CLAUDE.md`, `memory.md`, `README.md`). Formatter governs `src/**` + config files only.
- `.gitignore` updated for Node/Vite (`node_modules`, `dist`, `coverage`, logs) — was Rust-only before.
- Verification gate ALL GREEN: `pnpm typecheck`, `lint`, `format:check`, `test` (1 passing), `build` (dist OK, Tailwind CSS compiled), and `pnpm dev` boots + serves the app entry on :5199.
- Scripts available: dev / build / preview / typecheck / lint / format / format:check / test / test:watch.
- NEXT (Phase 0 remaining): wire minimal Redux Toolkit store (+ Provider), react-router (RouterProvider), and a Dexie db instance — deps installed, not yet integrated. Those 3 boxes left unchecked.

## 2026-06-12T01:14:03Z - Claude Opus 4.8 - Ralph iteration 2: wire Redux/router/Dexie + zero-tolerance lint policy
- **Completes Phase 0.** All 8 Phase 0 boxes now checked.
- Redux: `src/store/store.ts` (configureStore), `src/store/bootstrapSlice.ts` (minimal `bootstrap` slice — placeholder reducer so the store is valid; real domain slices + listener middleware are Phase 2), `src/store/hooks.ts` (typed `useAppDispatch`/`useAppSelector` via `.withTypes`). App wrapped in `<Provider>`.
- Router: `src/router.tsx` (`createBrowserRouter`, single index route → App; nine real screens are Phase 6). `<RouterProvider>` wired in `main.tsx`.
- Dexie: `src/db/db.ts` — `BrowserClawDB extends Dexie`, `version(1).stores({})` empty bootstrap schema (full 17-store schema + migrations are Phase 3). Singleton `db` exported.
- Tests added: `src/store/store.test.ts`, `src/db/db.test.ts` (db test checks `db.name` only — no IndexedDB needed in jsdom; Phase 3 can add fake-indexeddb for real open/query tests). 3 test files / 3 tests pass.
- **NEW POLICY (user request 2026-06-12): linting is zero-tolerance and coupled to tests.** `lint` script is now `eslint . --max-warnings 0` (warnings = failures). Added `pretest` script `pnpm run lint`, so `pnpm test` lints ALL files before Vitest runs. **Never suppress** lint findings (no `eslint-disable`, no rule-downgrading) — fix the code. Documented in CLAUDE.md (Conventions) and the `/qa` skill.
- Verbatim-module-syntax note: imports use explicit `.ts`/`.tsx` extensions and `import type` for type-only imports (required by `verbatimModuleSyntax` + `allowImportingTsExtensions`).
- Gate ALL GREEN: typecheck, lint (max-warnings 0), format:check, `pnpm test` (3/3), build (35 modules, dist OK).

## 2026-06-12T01:18:14Z - Claude Opus 4.8 - Ralph iteration 3: Phase 1 design tokens
- Implemented the full `design_tokens.json` set in `src/index.css` as Tailwind v4 `@theme static`.
- Colors (17): background, surface, surface-subtle, border, text, muted, muted-subtle, primary(+subtle), success(+subtle), warning(+subtle), danger(+subtle), purple(+subtle). Utilities: `bg-*`/`text-*`/`border-*` etc.
- Typography: `--font-sans` (Inter); sizes xs=12 sm=13 md=14 lg=16 xl=20 2xl=24 (px, matching the canonical tokens exactly — note: px not rem, so they don't scale with user font settings; revisit if a11y wants rem). Weights regular..extrabold → `font-regular`/`medium`/`semibold`/`bold`/`extrabold`.
- Radii: `rounded-button` (8px), `rounded-card` (12px).
- Layout dims kept as plain `:root` custom props (not utility-generating): `--bc-app-width` 1586, `--bc-app-height` 992, `--bc-left-nav-width` 260, `--bc-right-inspector-width` 380. Use via arbitrary values e.g. `w-[var(--bc-left-nav-width)]`.
- GOTCHA (Tailwind v4): plain `@theme` TREE-SHAKES unused theme vars out of the build — `color-purple`/`surface-subtle` were missing until referenced. FIX: `@theme static` always emits the declared token set (verified all present in dist CSS). Use this for canonical token layers.
- Gate green: typecheck, `pnpm test` (3/3), format:check, build (CSS 9.02 kB).
- NEXT (Phase 1): AppShell, then TopStatusBar / SidebarNav / RightInspectorPanel / StatusFooter, then the shared component library (Button, Card, Badge, Input, Select, Toggle, Tabs, Dialog, Progress, Toast, EmptyState, ErrorState). Plan to use the `frontend-design` skill for these. Will batch into small committed iterations.

## 2026-06-12T01:25:44Z - Claude Opus 4.8 - Ralph iteration 4: AppShell + layout regions
- Built the shared AppShell and all four layout regions. Phase 1 boxes for AppShell/TopStatusBar/SidebarNav/RightInspectorPanel/StatusFooter checked. Component library (Button..ErrorState) still pending.
- Files under `src/components/shell/`: `AppShell.tsx` (CSS-grid: top bar row + nav/main/optional-inspector columns sized by `--bc-left-nav-width` / `--bc-right-inspector-width`), `TopStatusBar.tsx` (brand + model pill + storage pill w/ usage bar + Settings btn), `SidebarNav.tsx` (NavLink rail, active = `bg-primary-subtle` + semibold primary), `StatusFooter.tsx` (runtime status card + version line; `RuntimeStatus` union ready/initializing/busy/error), `RightInspectorPanel.tsx` (ARIA tablist: Tool Calls/Context/Memory/Skills/Audit, local tab state), `BrandMark.tsx` (inline-SVG claw logo), `navItems.ts` (nav config → canonical routes).
- `src/AppLayout.tsx`: layout route wrapping `<Outlet/>` in AppShell; inspector shown only on `/chat` for now. `src/screens/PlaceholderScreen.tsx`: temporary stand-in (Phase 6 replaces). `router.tsx` rewritten: `/` → AppLayout with index redirect to `/chat` + a placeholder child route per nav item (nav is fully functional now, no dead links).
- Removed orphaned `App.tsx`/`App.test.tsx` (AppLayout is the new root) and did NOT add a shell barrel `index.ts` — barrels re-exporting components+consts risk a `react-refresh/only-export-components` warning (which `--max-warnings 0` would fail). Import directly from component files instead.
- DESIGN DECISION: frontend-design skill applied as *craft* (spacing rhythm, hover/active/`focus-visible` rings, subtle transitions) but NOT to override the canonical design language — kept the locked light theme + specified palette. Did add real Inter via **`@fontsource-variable/inter`** (imported in index.css; woff2 bundled locally — no CDN, fits local-first) and **`lucide-react`** for nav/status icons. `--font-sans` now `'Inter Variable', Inter, Arial, sans-serif`.
- All shell live values (model, storage, runtime status, version) are PLACEHOLDER props defaulting to the mockup values — to be wired to Redux in Phase 2.
- Tests: `src/components/shell/AppShell.test.tsx` (renders brand/nav/runtime/version/content; active-route `aria-current`; inspector tab switching via userEvent). 3 files / 5 tests pass.
- Gate green: typecheck, lint (max-warnings 0), format, test (5/5), build (CSS 16.75 kB, Inter woff2 bundled).
- NOT yet visually verified in a real browser (no Playwright installed) — jsdom tests + full build are the current evidence. Worth a real screenshot pass later.

## 2026-06-12T01:30:45Z - Claude Opus 4.8 - Ralph iteration 5: component library batch A (primitives)
- Built 6 of 12 shared components. Phase 1 sub-boxes Button/Card/Badge/Input/Select/Toggle checked; Tabs/Dialog/Progress/Toast/EmptyState/ErrorState remain (batch B).
- `src/lib/cn.ts`: tiny className joiner (filter(Boolean).join(' ')) — no tailwind-merge.
- `src/components/ui/`: `Button.tsx` (variants primary/secondary/ghost/danger, sizes sm/md, leading/trailing icon, `loading` w/ Loader2 spinner + aria-busy, disabled), `Card.tsx` (Card + CardHeader/CardTitle/CardContent/CardFooter composables), `Badge.tsx` (tones neutral/primary/success/warning/danger/purple, optional dot), `Input.tsx` + `Select.tsx` (forwardRef, label/hint/error, aria-invalid + aria-describedby; Select has appearance-none + ChevronDown), `Toggle.tsx` (real button role="switch", aria-checked, keyboard-operable).
- Style maps (VARIANTS/SIZES/TONES/DOTS) kept module-internal (not exported) to stay clear of `react-refresh/only-export-components` under `--max-warnings 0`.
- `src/screens/ComponentGallery.tsx` + route `/showcase`: living design-system gallery (also the surface for future visual review). Not a product screen.
- POLICY REMINDER honored: briefly added an `eslint-disable react/button-has-type` in Button, then REMOVED it (violates no-suppression rule; the rule isn't even enabled). No suppressions anywhere.
- Tests `src/components/ui/ui.test.tsx`: Button click + loading-disabled/aria-busy, Badge content, Input label assoc + aria-invalid, Toggle aria-checked flip. 4 files / 9 tests pass.
- Gate green: typecheck, lint, format, test (9/9), build (CSS 20.89 kB).
- NEXT (Phase 1 batch B): Tabs, Dialog (focus-trap + restore — acceptance criterion), Progress, Toast, EmptyState, ErrorState. Then Phase 1 done → Phase 2 (Redux slices + listener middleware; wire shell placeholders to real state).

## 2026-06-12T01:36:11Z - Claude Opus 4.8 - Ralph iteration 6: component library batch B → PHASE 1 COMPLETE
- Built the remaining 6 components. **All of Phase 1 is now checked** (tokens + shell + 12 components).
- `src/components/ui/`: `Tabs.tsx` (data-driven, ARIA tablist + roving tabindex + Arrow/Home/End keyboard nav), `Dialog.tsx` (portal to body; focus-trap, Escape-to-close, overlay-click close, body scroll-lock, **restores focus to the previously focused element on close** — accessibility acceptance criterion), `Progress.tsx` (determinate 0–100, role=progressbar, optional label/showValue, tones), `EmptyState.tsx` (icon/title/description/action, dashed), `ErrorState.tsx` (role=alert, danger-toned, AlertTriangle).
- Toast split across TWO files to satisfy `react-refresh/only-export-components` under `--max-warnings 0`: `toastContext.ts` (ToastContext + `useToast` hook + types — NO component) and `Toast.tsx` (ToastProvider + internal ToastViewport/ToastCard — components only). PATTERN: keep hooks/context in a separate non-component module from the provider component.
- Wired `<ToastProvider>` into `main.tsx` (wraps RouterProvider, inside Redux Provider). `useToast()` now available app-wide.
- Gallery (`/showcase`) expanded to demo every component incl. live Dialog + Toast.
- Tests: `src/components/ui/feedback.test.tsx` — Tabs click + arrow-key, Dialog focus-in/Escape/focus-restore, Progress aria-valuenow, Empty/ErrorState titles, Toast show + dismiss. Now **5 test files / 15 tests pass**.
- Gate green: typecheck, lint, format, test (15/15), build (CSS 22.82 kB, JS 337.84 kB).
- Component inventory complete. Reusable design-system import paths: `src/components/ui/{Button,Card,Badge,Input,Select,Toggle,Tabs,Dialog,Progress,Toast,EmptyState,ErrorState}.tsx`, hook `toastContext.ts#useToast`, helper `src/lib/cn.ts`. Shell in `src/components/shell/`.
- NEXT: **Phase 2 — Redux Control Plane**: configure store (replace bootstrap slice), add listener middleware, add slices (app/runtime/chat/approvals/providers/models/skills/memories/storage/audit/secrets-metadata-ONLY), runtime event/action naming conventions, and the hard rule: NO raw/decrypted secrets in Redux. Then wire the shell's placeholder props (model, storage, runtime status) to real selectors.

## 2026-06-12T01:40:54Z - Claude Opus 4.8 - Ralph iteration 7: Phase 2 foundation (store, listener mw, app+runtime slices)
- Started Phase 2. Checked: Configure Redux store, Add listener middleware, slices app + runtime, Add runtime event/action naming conventions. Remaining slices: chat/approvals/providers/models/skills/memories/storage/audit/secrets. "Ensure no secrets in Redux" stays unchecked until the secrets slice exists.
- Replaced the placeholder `bootstrapSlice` (deleted) with real slices. `src/store/slices/appSlice.ts` (hydrated/onboardingComplete/activeWorkspaceId; actions hydrated, onboardingCompleted, activeWorkspaceSet) and `src/store/slices/runtimeSlice.ts` (status `initializing|ready|busy|error` + message; actions runtimeReady/runtimeBusy/runtimeErrored/runtimeReset).
- `src/store/store.ts` now combines app+runtime and `.prepend(listenerMiddleware.middleware)`. `src/store/listenerMiddleware.ts`: `createListenerMiddleware` + typed `startAppListening`/`addAppListener` (via `.withTypes<RootState, AppDispatch>()`). store↔listener type cycle is type-only (erased by verbatimModuleSyntax) — no runtime cycle.
- `src/store/CONVENTIONS.md`: documents Redux scope, the NO-decrypted-secrets rule (secrets slice = metadata only), one-slice-per-domain, `domain/action` naming (past-tense for events, imperative for setters; runtime/* mirrors WASM events), side-effects-in-listeners-only.
- `src/lib/appMeta.ts`: APP_NAME/APP_VERSION constants (version is a constant, not Redux state).
- WIRED: `AppLayout` now reads `state.runtime.status` via `useAppSelector` and passes it (+ APP_VERSION) to the AppShell sidebar StatusFooter. Note: runtime initial status is `initializing`, so the footer reads "starting…" until something dispatches runtimeReady (genuine — runtime lands in Phase 5). StatusFooter's RuntimeStatus union matches the slice's exactly.
- Tests: appSlice.test.ts, runtimeSlice.test.ts, rewritten store.test.ts (fresh-store-per-test, composition + action routing). Now 7 files / 21 tests pass.
- Gate green: typecheck, lint, format, test (21/21), build (JS 344 kB).
- NEXT: more Phase 2 slices. Suggested grouping: (8) providers + models + storage slices, then wire TopStatusBar model/storage pills; (9) chat + approvals; (10) skills + memories + audit + secrets-metadata, then check "no secrets in Redux" + add a guard test. SecretVault itself is Phase 4.

## 2026-06-12T01:51:05Z - Claude Opus 4.8 - Added UI unit tests (user request) — coverage for Card/Select/TopStatusBar/StatusFooter
- User asked for UI unit tests. Existing suite already covered AppShell + primitives (Button/Badge/Input/Toggle) + feedback (Tabs/Dialog/Progress/Empty/Error/Toast); GAPS were Card, Select, TopStatusBar, StatusFooter.
- New files: `src/components/ui/Card.test.tsx` (composable parts render, no-description case, className passthrough via data-testid), `src/components/ui/Select.test.tsx` (label assoc, defaultValue, selectOptions change, error→aria-invalid), `src/components/shell/TopStatusBar.test.tsx` (provider/model text, storage GB formatting + progressbar aria-valuenow=25, clamp to 100, model/settings callbacks), `src/components/shell/StatusFooter.test.tsx` (it.each over 4 runtime statuses, version + view-status callback).
- Test query tips that worked here: `getByTitle('Local storage usage')` + `toHaveTextContent('2.00 GB / 8.00 GB')` for split text nodes; `getByTitle('Change active model or provider')` for the model pill; `it.each<[RuntimeStatus, RegExp]>` for status variants; match the '…' ellipsis messages with /starting/ /working/ regexes not literals.
- Suite now **11 files / 36 tests** (was 7/21). Gate green: typecheck, lint, format, test (36/36), build. No new components/deps — tests only. No TODO box ticked (Phase 13 "Run tests" is end-of-project QA; this just deepens coverage).

## 2026-06-12T01:55:48Z - Claude Opus 4.8 - Ralph iteration 8: providers + models + storage slices, TopStatusBar wired
- Phase 2 slices providers/models/storage checked. Remaining slices: chat, approvals, skills, memories, audit, secrets.
- `providersSlice.ts`: activeProviderId/Label + health Record. `ProviderHealth` = unconfigured|connected|auth_failed|cors_error|model_not_found|unreachable. Actions activeProviderSet({id,label}|null), providerHealthSet({providerId,health}). Comment notes: NO secret material (key refs in Dexie, plaintext only in SecretVault).
- `modelsSlice.ts`: activeModelId/Label + downloads Record<id,{status,progress}>. `ModelDownloadStatus` = queued|downloading|ready|error. Actions activeModelSet, modelDownloadUpdated({modelId,status,progress}), modelDownloadRemoved(id).
- `storageSlice.ts`: usedBytes/quotaBytes/persisted. Actions storageEstimateSet({usedBytes,quotaBytes}), storagePersistedSet(bool). quota 0 = "not measured".
- store.ts now has app/runtime/providers/models/storage. store.test.ts updated to assert the 5 slice keys (not deep-equal, so adding slices later won't churn it).
- WIRED TopStatusBar to store via AppLayout selectors (provider/model labels, storage used/quota). REMOVED TopStatusBar's hardcoded mockup defaults — now renders honest empty state: "No model selected" (grey dot) when no provider/model, "Storage not measured" (no progressbar) when quota 0. Pill builds provider•model conditionally. Initial app state shows the empty state (nothing dispatches selection yet — onboarding/Phase 6-7 will).
- Tests added: providersSlice/modelsSlice/storageSlice .test.ts + a TopStatusBar empty-state case. Now **14 files / 46 tests**. Gate green (typecheck, lint, format, 46/46, build).
- NEXT (iteration 9): chat + approvals slices. Then (10) skills + memories + audit + secrets-metadata-only → then can check "Ensure no decrypted secrets in Redux" + add a guard test. SecretVault itself = Phase 4.

## 2026-06-12T02:00:36Z - Claude Opus 4.8 - Ralph iteration 9: chat + approvals slices
- User directive: "keep Ralph Looping until everything is done" — now running iterations continuously without pausing to ask between them. Each iteration still fully gated + committed + pushed.
- Phase 2 slices chat + approvals checked. Remaining: skills, memories, audit, secrets.
- `chatSlice.ts`: activeConversationId, composerDraft, runState ('idle'|'thinking'|'streaming'|'awaiting_approval'|'error'), streamingMessageId. Actions activeConversationSet (clears draft), composerDraftSet, runStateSet, streamingMessageSet. (Messages/conversations themselves are durable Dexie — Phase 3.)
- `approvalsSlice.ts`: the inline approval queue (underpins "no silent side effects"). ApprovalRequest = {id,kind,title,risk,summary,payloadPreview,status}. kind = tool_call|storage_write|llm_request|skill_install|network; risk = low|med|high; status = pending|approved|rejected. Actions approvalRequested (push as pending), approvalEdited (edit payloadPreview), approvalResolved ({id,status}), approvalDismissed (remove). payloadPreview is display-safe — NEVER raw secrets.
- store now 7 slices (app/runtime/providers/models/storage/chat/approvals); store.test.ts key list updated.
- Tests: chatSlice/approvalsSlice .test.ts. Now 16 files / 53 tests. Gate green (typecheck, lint, format, 53/53, build).
- NEXT (iteration 10): skills + memories + audit + secrets (METADATA ONLY) slices → completes Phase 2 slice list. Then tick "Ensure no decrypted secrets in Redux" and add a guard test asserting the secrets slice shape carries no plaintext. SecretVault itself = Phase 4.

## 2026-06-12T02:03:49Z - Claude Opus 4.8 - Ralph iteration 10: skills/memories/audit/secrets slices → PHASE 2 COMPLETE
- All 11 slices done; **Phase 2 fully checked** (store, listener mw, conventions, 11 slices, no-secrets rule).
- `skillsSlice.ts`: selectedSkillId + enabledIds[] (enable dedupes). `memoriesSlice.ts`: searchQuery/filterTags/selectedMemoryId. `auditSlice.ts`: recent AuditEntry[] feed, newest-first via unshift, capped at 50 (MAX_RECENT); AuditEntry.at is caller-supplied (reducers deterministic — no Date.now). `secretsSlice.ts`: **METADATA ONLY** — SecretMetadata {id,label,storageMode('session'|'encrypted')}, NO value/key field; vaultLocked starts true.
- **Security acceptance criterion met + guarded**: `secretsSlice.test.ts` asserts stored metadata has none of [value,key,secret,token,plaintext,apiKey] and exactly keys [id,label,storageMode]. Ticked "Ensure raw/decrypted secrets are never stored in Redux." Decrypted secrets → SecretVault only (Phase 4).
- store now 11 slices: app/runtime/providers/models/storage/chat/approvals/skills/memories/audit/secrets. store.test.ts key list updated (11 keys).
- Tests: skills/memories/audit/secrets .test.ts. Now **20 files / 64 tests**. Gate green (typecheck, lint, format, 64/64, build).
- PHASES DONE: 0, 1, 2. NEXT = **Phase 3 — IndexedDB/OPFS Storage**: Dexie schema (17 stores: app_settings, provider_profiles, encrypted_secrets, conversations, messages, memories, todos, rules, schedules, skills, skill_files, skill_state, audit_events, runtime_snapshots, model_catalog, model_cache_index, backup_history) + migrations, quota service, persistent-storage request service, storage health checks. `encrypted_secrets` stores CIPHERTEXT only. For real Dexie open/query tests in jsdom, add `fake-indexeddb` dev dep.

## 2026-06-12T02:08:14Z - Claude Opus 4.8 - Ralph iteration 11: Phase 3 Dexie schema (17 stores)
- Started Phase 3. Checked "Create Dexie schema" + all 17 store sub-items. Left "Add migrations" UNCHECKED (honest — v1 is baseline, no migration until schema changes; pattern documented in db.ts). Services (quota/persistent/health) = iteration 12.
- Added dev dep **fake-indexeddb** (6.2.5) to test real Dexie open/query in jsdom.
- `src/db/types.ts`: row interfaces for all 17 stores (AppSettingRow, ProviderProfileRow, EncryptedSecretRow{ciphertext,iv — CIPHERTEXT ONLY, no plaintext}, ConversationRow, MessageRow, MemoryRow, TodoRow, RuleRow, ScheduleRow, SkillRow, SkillFileRow, SkillStateRow, AuditEventRow, RuntimeSnapshotRow, ModelCatalogRow, ModelCacheIndexRow, BackupHistoryRow).
- `src/db/db.ts`: rewritten from empty stub → full `version(1).stores({...})` with typed `Table<Row,Key>` props (Dexie auto-assigns by matching store name to class property). DB_NAME/DB_VERSION exported. `on('populate')` seeds `app_settings: schemaVersion`. GOTCHA: IndexedDB CANNOT index booleans — dropped `enabled`/`pinned` from indexes (filter those in memory). Compound keys for skill_files `[skillId+path]` & skill_state `[skillId+key]`; multi-entry `*tags` on memories; compound index `[conversationId+createdAt]` on messages.
- `src/db/db.test.ts`: `import 'fake-indexeddb/auto'`; asserts 17 table names, populate seed, conversation+messages round-trip (where/sortBy), memories multi-entry tag query. Now **20 files / 68 tests**.
- NOTE: `db` singleton is NOT yet imported by the app graph, so it's tree-shaken out of the bundle (JS still 348 kB). Iteration 12 storage services + later phases will wire it.
- Gate green: typecheck, lint, format, 68/68, build.
- NEXT (iteration 12): Phase 3 services — storage quota service (navigator.storage.estimate → dispatch storageEstimateSet), persistent storage request service (navigator.storage.persist/persisted → storagePersistedSet), storage health checks. Then Phase 3 done (modulo deferred migrations box).

## 2026-06-12T02:10:26Z - Claude Opus 4.8 - Ralph iteration 12: Phase 3 storage services
- Phase 3 services done (quota / persistent-request / health). **Phase 3 complete except "Add migrations"** (deferred — baseline v1, no migration yet).
- `src/services/storageService.ts` (new `src/services/` dir): `estimateStorage` (navigator.storage.estimate → {usedBytes,quotaBytes}), `isStoragePersisted`, `requestPersistentStorage`, `assessStorageHealth(usage,persisted)` → {level ok|warning|critical, usedRatio, persisted, recommendations[]} (thresholds: ≥0.9 critical, ≥0.75 warning; not-persisted → at least warning + recommendation), `refreshStorageInfo(dispatch, manager?)` dispatches storageEstimateSet + storagePersistedSet and returns health.
- DESIGN: StorageManager is injectable (param defaults to navigator.storage) so it's unit-testable without a browser and degrades to zeros/false when the API is absent. This is the pattern for all browser-API services.
- Tests `src/services/storageService.test.ts`: fakeManager injection, estimate mapping, API-absent fallbacks, all 4 health bands, refresh dispatch (vi.fn dispatch asserted against action creators). Now **21 files / 77 tests**.
- NOT yet auto-invoked on app start — refreshStorageInfo is ready but no caller wires it on boot yet (a Phase 6 onboarding/boot effect or a listener will call it). TopStatusBar storage pill will populate once it runs.
- Gate green: typecheck, lint, format, 77/77, build.
- PHASES DONE: 0,1,2,3(−migrations). NEXT = **Phase 4 — SecretVault**: in-memory vault, session-only keys, encrypted stored keys (Web Crypto AES-GCM), passphrase-derived key (PBKDF2/Argon-ish via SubtleCrypto deriveKey), lock timeout, unlock/lock audit events, and tests that secrets never reach Redux/logs. CRITICAL acceptance criterion. Web Crypto in jsdom: Node's `crypto.webcrypto`/`globalThis.crypto.subtle` is available under Vitest (Node 22) — verify in tests; may need to reference globalThis.crypto.

## 2026-06-12T02:14:27Z - Claude Opus 4.8 - Ralph iteration 13: Phase 4 crypto module (AES-GCM + PBKDF2)
- Started Phase 4 (SecretVault). Checked "Implement Web Crypto AES-GCM encryption." Vault + session/encrypted keys + passphrase-flow + lock-timeout + audit + no-leak tests = iteration 14.
- `src/secrets/crypto.ts`: `deriveKey(passphrase, saltB64)` → non-extractable AES-GCM-256 CryptoKey via PBKDF2 (SHA-256, 250k iterations). `generateSalt()` (16B base64), `randomBytes`, `encryptString(key,plaintext)` → {ciphertext,iv} base64, `decryptString(key,{ciphertext,iv})`. Keys never exported.
- GOOD NEWS: Web Crypto (globalThis.crypto.subtle) IS available in the Vitest jsdom env (Node 22) — no polyfill needed.
- GOTCHA (TS 6 / new typings): `TextEncoder().encode()` and default `Uint8Array` are `Uint8Array<ArrayBufferLike>`, which does NOT satisfy Web Crypto's ArrayBuffer-backed BufferSource ("SharedArrayBuffer not assignable to ArrayBuffer"). FIX: helpers return `Uint8Array<ArrayBuffer>`; added `encodeUtf8()` that wraps `new Uint8Array(new TextEncoder().encode(v))` to force an ArrayBuffer-backed copy. Use this pattern for any Web Crypto byte input.
- Tests `src/secrets/crypto.test.ts`: round-trip, wrong-passphrase rejects, fresh IV per encryption (non-deterministic), distinct salts. Now **22 files / 81 tests**. Gate green.
- NEXT (iteration 14): `src/secrets/secretVault.ts` — in-memory Map (module-private, NEVER serialized). unlock(passphrase) derives key (salt from app_settings 'vaultSalt'; verify via stored check-token in encrypted_secrets), lock() clears key+secrets, session-only secrets (not persisted), encrypted secrets (encrypt→Dexie encrypted_secrets ciphertext, decrypt on load), lock timeout (setTimeout auto-lock), dispatch audit (auditAppended) + secrets metadata (secretMetadataUpserted/vaultLockedSet) on lock/unlock — NEVER the plaintext. Tests must assert: plaintext never in Redux state and getState() JSON has no secret value. Tick remaining Phase 4 boxes.

## 2026-06-12T02:19:50Z - Claude Opus 4.8 - Ralph iteration 14: SecretVault → PHASE 4 COMPLETE
- **Phase 4 fully checked.** All 8 boxes done. The critical security acceptance criterion (no decrypted secrets in Redux/logs) is implemented AND test-guarded.
- `src/secrets/secretVault.ts`: `SecretVault` class. Decrypted plaintext lives ONLY in a private `#plaintext` Map (never serialized). Ports: `VaultStore` (persistence — ciphertext+config only) and `VaultObserver` (metadata+audit only — never plaintext) are injected → nothing in the class can leak plaintext + fully unit-testable. API: setup(passphrase) [salt+verifier], unlock(passphrase)→bool (verifier check token = 'browserclaw-vault-ok'; wrong pass → false + audit), openSession() [session-only, no key], lock() [clears key+plaintext], setSessionSecret (memory-only), putEncryptedSecret (encrypt→store ciphertext, plaintext in memory), getSecret (cache or lazy-decrypt), removeSecret, listSecrets (metadata). Lock timeout via setTimeout, re-armed on every op (#touch). Uses JS-native `#private` fields/methods (erasableSyntaxOnly-safe).
- `src/secrets/vaultWiring.ts`: `InMemoryVaultStore` (volatile), `createDexieVaultStore(db)` (salt/verifier in app_settings 'vaultSalt'/'vaultVerifier'; ciphertext in encrypted_secrets), `createReduxVaultObserver(dispatch)` (→ vaultLockedSet / secretMetadataUpserted / secretMetadataRemoved / auditAppended with crypto.randomUUID()+Date.now()).
- Tests: `secretVault.test.ts` (setup/encrypted round-trip, persist+fresh-instance unlock, wrong passphrase, locked throws, session-only no-persist, lock clears plaintext, auto-lock via vi.useFakeTimers, lock/unlock audit). `vaultWiring.test.ts`: **THE GUARD** — JSON.stringify(store.getState()) contains NEITHER the encrypted nor session plaintext, yet metadata IS present + vaultLocked false + audit recorded; plus Dexie store persists ciphertext only (raw row has no plaintext) and round-trips across instances. Now **24 files / 92 tests**.
- Vault not yet wired into app boot (no caller yet) — onboarding (Phase 6) + settings security screen will instantiate it with createDexieVaultStore(db)+createReduxVaultObserver(store.dispatch).
- Gate green: typecheck, lint, format, 92/92, build.
- PHASES DONE: 0,1,2,3(−migrations),4. NEXT = **Phase 5 — Rust/WASM Runtime Stub**. BIG DECISION NEEDED: full Rust/WASM toolchain (wasm-pack/cargo) may not be installed and is heavy. The TODO wants claw-core/claw-wasm/claw-schema/claw-testkit crates + wasm-bindgen effect model (llm_request, storage_get/put/search, tool_call_proposal, skill_fs_read_text, skill_state_get/put, audit_append, runtime_snapshot_save) wired to Redux listener middleware. Consider: implement the EFFECT MODEL + listener wiring in TS first (the JS-side contract the WASM will satisfy) so progress isn't blocked on the Rust toolchain, and stub the WASM boundary behind an interface. Check `which cargo wasm-pack` before committing to native build.

## 2026-06-12T02:25:15Z - Claude Opus 4.8 - Ralph iteration 15: Phase 5 Rust workspace + effect model (native)
- TOOLCHAIN: cargo/rustc/rustup installed; **wasm-pack MISSING + wasm32 target NOT installed**. wasm-bindgen crates compile NATIVELY though, so the whole workspace builds + `cargo test`s on host now; only the wasm32 browser build is deferred.
- Created Rust workspace at repo ROOT: `Cargo.toml` (workspace, resolver 2, members crates/claw-{schema,core,testkit,wasm}, shared serde/serde_json deps) + committed `Cargo.lock`. `target/` already gitignored (original Rust .gitignore).
- `crates/claw-schema`: `Effect` enum (serde tag="type" snake_case) with ALL 10 variants (llm_request, storage_get/put/search, tool_call_proposal, skill_fs_read_text, skill_state_get/put, audit_append, runtime_snapshot_save), `Command` enum (submit_user_message, resolve_effect), `RuntimeState` (next_effect_id, message_count, pending BTreeMap).
- `crates/claw-core`: `Runtime` — deterministic state machine, NO I/O/clock/random. dispatch(Command)->Vec<Effect>; submit→[AuditAppend, LlmRequest], resolve(llm)→[StoragePut, AuditAppend]; monotonic eff-N ids; snapshot()/from_snapshot() exact round-trip. 4 unit tests.
- `crates/claw-testkit`: run_script + user_message helpers (1 test). `crates/claw-wasm`: `ClawRuntime` wasm-bindgen (cdylib+rlib): new/fromSnapshot/dispatch(json)->json/snapshot(); JSON string boundary so schema stays language-neutral. 2 native tests.
- RUST GATE GREEN: `cargo test --workspace` (7 tests), `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` (zero warnings). Added these 3 to the `/qa` skill (run when crates/ changes).
- Checked Phase 5: workspace + 4 crates + wasm-bindgen API + effect model (all 10). LEFT: "Wire effects to Redux listener middleware" + "Persist snapshots" (TS-side, iteration 16).
- NEXT (iteration 16): TS-side runtime integration. DECISION: building actual wasm needs `rustup target add wasm32-unknown-unknown` + wasm-pack (slow cargo install). Plan: define a TS `Runtime` port + effect handler registry, wire claw runtime → listener middleware so effects (audit_append→auditAppended, runtime_snapshot_save→Dexie runtime_snapshots, storage_put→Dexie) get executed, persist snapshots to Dexie. Either load the wasm (if I add the target/wasm-pack) OR provide a TS reference runtime behind the port to stay unblocked. Snapshot persistence → db.runtime_snapshots.

## 2026-06-12T02:31:09Z - Claude Opus 4.8 - Ralph iteration 16: TS effect execution + snapshot persistence
- Phase 5 "Persist snapshots" checked. Only "Wire effects to Redux listener middleware" (the action→runtime trigger) remains.
- `src/runtime/effectTypes.ts`: TS mirror of claw-schema (Effect union all 10 + Command union, snake_case tags).
- `src/runtime/referenceRuntime.ts`: `ClawRuntimePort` interface + `createReferenceRuntime()` — faithful TS port of claw-core (deterministic), so the host pipeline works BEFORE the wasm build. The wasm ClawRuntime satisfies the same port → drop-in swap. `RuntimeSnapshot` = {next_effect_id, message_count, pending}.
- `src/runtime/effectExecutor.ts`: `executeEffect(effect, ctx)` routes: audit_append→dispatch(auditAppended), runtime_snapshot_save→Dexie runtime_snapshots, tool_call_proposal→dispatch(approvalRequested); llm_request/storage_*/skill_* → injected `EffectPorts` (no-op default, wired in Phases 7/3/10). `now` injectable (host-side impure clock). Risk strings normalized to audit/approval risk unions. Exhaustive switch over all 10 effects.
- `src/runtime/runtimeHost.ts`: `RuntimeHost` (submit(command)→run effects), `saveSnapshot()`→Dexie runtime_snapshots id='latest', `loadLatestSnapshot(db)`, `createRuntimeHost(ctx, snapshot?)`.
- AVOIDED `any` (no-explicit-any is an ESLint error): readText() narrows unknown; snapshot cast via `as RuntimeSnapshot | undefined`.
- Tests: referenceRuntime (matches claw-core: submit→audit+llm eff-2, resolve→storage_put, snapshot determinism), effectExecutor (audit→feed, tool→approvals, snapshot→Dexie, llm→port), runtimeHost (submit runs effects, snapshot persist+restore). Now **27 files / 101 tests**.
- Gate green: typecheck, lint, format, 101/101, build.
- NEXT (iteration 17): "Wire effects to Redux listener middleware" — add chat `userMessageSubmitted` action (sets runState 'thinking', clears draft); `src/runtime/runtimeListeners.ts` registers a startAppListening listener that calls host.submit on that action; instantiate the RuntimeHost singleton + register listeners at app boot (main.tsx) with ctx {dispatch: store.dispatch, db}. Then Phase 5 DONE. After that → Phase 6 (the 9 screens — the big UI phase; reuse shell + ui components + placeholder screens already in place).

## 2026-06-12T02:34:16Z - Claude Opus 4.8 - Ralph iteration 17: wire runtime to listener middleware → PHASE 5 COMPLETE
- **Phase 5 fully checked** (workspace, 4 crates, wasm-bindgen API, all 10 effects, listener wiring, snapshot persist). Only the wasm32 BROWSER build is deferred (needs `rustup target add wasm32-unknown-unknown` + wasm-pack) — the runtime runs via the TS reference impl, drop-in replaceable by the wasm ClawRuntime (same ClawRuntimePort).
- chatSlice: added `userMessageSubmitted({conversationId,text})` action (sets activeConversationId, clears draft, runState='thinking').
- `src/runtime/runtimeListeners.ts`: `registerRuntimeListeners(startListening, host)` — typed via RTK `TypedStartListening<RootState,AppDispatch>` (type-only, no runtime coupling to the singleton); listens for userMessageSubmitted → host.submit({type:'submit_user_message',...}). Effects flow back out through executeEffect.
- main.tsx: `bootRuntime()` async — loadLatestSnapshot(db) → createRuntimeHost({dispatch:store.dispatch, db}, snapshot) → registerRuntimeListeners(startAppListening, host); `.catch(console.error)`. Runtime now wired into app boot.
- BUNDLE: JS jumped 348→447 kB gzip 143 — db/Dexie + runtime are now in the app graph (no longer tree-shaken). Expected.
- Test: `runtimeListeners.test.ts` — fresh createListenerMiddleware + store, dispatch userMessageSubmitted → runState 'thinking' immediately + host.submit called async (vi.waitFor). `listener.startListening as never` to satisfy the typed param without `any`. Now **28 files / 102 tests**.
- Gate green: typecheck, lint, format, 102/102, build. Rust gate unchanged (still green from iter 15).
- PHASES DONE: 0,1,2,3(−migrations),4,5(−wasm32 browser build). NEXT = **Phase 6 — Screens** (the 9 real screens from svg mockups: onboarding, chat/workbench, models, storage, skills, memories, audit, settings, workflow). BIG UI phase. Building blocks ready: AppShell + 12 ui components + placeholder screens + all redux slices + storage service + vault + runtime. Replace each PlaceholderScreen route with the real screen. Read each screen's section in BROWSERCLAW_TEXT_MOCKUPS.md + BROWSERCLAW_UI_SPEC.md. One screen per iteration (or small batches). NOTE: UI still never visually verified in a real browser — increasingly worth a Playwright/screenshot pass during Phase 6.

## 2026-06-12T02:38:31Z - Claude Opus 4.8 - Ralph iteration 18: Phase 6 onboarding screen
- Started Phase 6 (screens). `/onboarding` checked. 8 screens remain (chat, models, storage, skills, memories, audit, settings, workflow).
- `src/screens/OnboardingScreen.tsx`: 4-step wizard (Choose inference mode → Set up storage → Configure model → Finish). Step 1: 3 ModeCards (wllama[Recommended]/local/remote) w/ feature lists. Step 2: storage card reading storage slice + "Enable persistent storage" → requestPersistentStorage()+refreshStorageInfo(dispatch). Step 3: per-mode config (wllama model Select / local endpoint Input / remote provider Select + browser-direct-key warning). Step 4: finish → dispatch(onboardingCompleted) + (remote) activeProviderSet + navigate('/chat'). Right panel: StepRail overview + "Need help". Renders STANDALONE (own layout, full-screen) — NOT inside AppShell.
- router.tsx: added `{ path:'/onboarding', element:<OnboardingScreen/> }` as a sibling route OUTSIDE AppLayout (first-run flow has no sidebar).
- Uses existing components (Button/Badge/Input/Select/BrandMark) + cn. Reused storageService.
- GOTCHA (test): step title appears TWICE (h2 heading + StepRail span) → getByText ambiguous. FIX: query `getByRole('heading',{name})` for step titles. General rule for these screens: prefer role-based queries.
- Test `OnboardingScreen.test.tsx`: full 4-step walk → finishes into /chat + onboardingComplete=true; Back disabled on step 1. Now **29 files / 104 tests**.
- Gate green: typecheck, lint, format, 104/104, build (456 kB).
- NEXT (iteration 19): `/chat` screen (02_chat_workbench.svg) — primary surface: message thread, composer (attach + slash hint), inline approval cards (approve/edit/reject + risk + exact data, from approvals slice), tool-call status, runtime footer. Inspector already shown on /chat via AppLayout. Wire composer submit → dispatch(userMessageSubmitted) (runtime listener already handles it). Replace the /chat PlaceholderScreen.

## 2026-06-12T03:29:01Z - Claude Opus 4.8 - Playwright visual verification set up + UI VERIFIED IN REAL BROWSER
- User asked to set up Playwright + drive a real browser. DONE and working.
- Added `@playwright/test` (1.60) + installed Chromium binary (`pnpm exec playwright install chromium`) — Chromium launches headless fine, NO missing system libs in this env.
- `playwright.config.ts`: testDir e2e/, chromium project, webServer runs `pnpm dev --port 4321` (reuseExistingServer), baseURL localhost:4321. `e2e/visual.spec.ts`: loads /onboarding,/chat,/models,/storage,/showcase → full-page screenshots to e2e/screenshots/<name>.png. Script `pnpm test:e2e` = `playwright test`.
- IMPORTANT runner separation: Vitest `include: ['src/**/*.test.{ts,tsx}']` (vite.config.ts) so it does NOT pick up Playwright's e2e/*.spec.ts. Vitest=src/*.test, Playwright=e2e/*.spec. gitignored: test-results, playwright-report, /playwright/.cache, e2e/screenshots.
- **VISUAL VERIFICATION RESULT (read the PNGs): UI looks great + on-spec.** showcase/component gallery: AppShell + sidebar + topbar all render; the honest empty states WORK ("No model selected", "Storage not measured"); runtime footer shows "starting…" (correct — runtime initial status 'initializing'); v0.7.0; claw BrandMark renders; all 12 components correct (button variants incl loading spinner, 6 badge tones w/ dots, input error state, select, toggle on, card). onboarding: matches mockup — 3 mode cards (wllama selected w/ primary ring + Recommended badge), feature checks, privacy note, Back(disabled)/Continue, right-panel StepRail (step 1 active). Inter font + tokens + spacing all polished.
- HOW TO VERIFY A SCREEN GOING FORWARD: add its route to e2e/visual.spec.ts ROUTES, run `pnpm exec playwright test`, then Read e2e/screenshots/<name>.png. (chat/models/storage currently still placeholders in the screenshots since those screens aren't built yet — chat shows the placeholder via AppShell.)
- Gate green: typecheck, lint (e2e + playwright.config lint clean), format, 104/104, build. Visual-verification debt CLEARED for what's built so far.
- NEXT: resume Phase 6 iteration 19 = /chat workbench (now I can screenshot-verify it). Then models/storage/skills/memories/audit/settings/workflow.

## 2026-06-12T05:58:48Z - Claude Opus 4.8 - Added Firefox to Playwright (both browsers verified)
- User OK'd testing with both Chromium AND Firefox. Installed Firefox binary (`pnpm exec playwright install firefox`, v150) + added a `firefox` project to playwright.config.ts (alongside chromium).
- e2e/visual.spec.ts now writes per-browser: `e2e/screenshots/<project>/<name>.png` (uses testInfo.project.name) so chromium/firefox captures don't overwrite.
- `pnpm test:e2e` runs 10 tests (5 routes × 2 browsers) — all pass. Read firefox/onboarding.png: renders IDENTICALLY to chromium (clean cross-browser parity).
- Note: prior commit eca9d88 accidentally missed this memory file's Playwright entry (cat append happened after git add); folding it in now.

## 2026-06-12T07:50:04Z - Claude Opus 4.8 - Ralph iteration 19: /chat workbench (VISUALLY VERIFIED)
- `/chat` checked. 7 screens remain (models, storage, skills, memories, audit, settings, workflow).
- Added `dexie-react-hooks` (4.4) for useLiveQuery.
- Components `src/screens/chat/`: `MessageBubble` (role-based: user=primary bg right-aligned, assistant=surface; time via toLocaleTimeString), `ApprovalCard` (pure: Proposed action + risk Badge[low→success/med→warning/high→danger] + EXACT payload in <pre>, Approve/Edit/Reject; Edit toggles textarea→onEdit; shows resolved status when not pending), `ChatComposer` (textarea, Attach, "/" hint, Send; Enter sends / Shift+Enter newline; Send disabled when empty).
- `src/screens/ChatScreen.tsx` (container): messages via useLiveQuery(db.messages where conversationId == chat.activeConversationId, sortBy createdAt); composer draft ↔ chat.composerDraft; pending approvals from approvals slice. handleSend: create conversation (crypto.randomUUID) if none → write user MessageRow to Dexie → dispatch(userMessageSubmitted) (runtime listener fires; llm_request no-op until Phase 7 so no assistant reply yet). Approve/Reject → approvalResolved + approvalDismissed; Edit → approvalEdited. EmptyState when no messages+no approvals.
- router.tsx: introduced `SCREEN_OVERRIDES` map (key→real screen, falls back to PlaceholderScreen) — drop real screens in here as built. /chat now ChatScreen.
- GOTCHAS fixed: (1) useLiveQuery two branches → annotate empty branch `Promise.resolve<MessageRow[]>([])` else TS infers never[]. (2) ApprovalCard status union: narrow via `approval.status === 'pending'` (not a separate `pending` bool) so TS knows it's approved|rejected in else. (3) TEST: userEvent.type parses `{` as key descriptor → use `fireEvent.change` for JSON textarea. (4) TEST: do NOT `db.close()` in afterAll of a file with useLiveQuery — races the subscription → DatabaseClosedError unhandled rejection; let afterEach cleanup unsubscribe.
- Tests: ApprovalCard (payload/risk shown, approve/reject/edit), ChatComposer (disabled/enabled, Enter vs Shift+Enter), ChatScreen (empty→send shows message via Dexie+useLiveQuery; pending approval → Approve clears queue). Now **32 files / 111 tests**.
- VISUAL (chromium+firefox screenshots read): /chat matches mockup — header, empty state w/ approval explanation, composer pinned bottom (attach + "/" hint + disabled Send), inspector tabs (Tool Calls active), Chat nav active. 
- Gate green: typecheck, lint, format, 111/111, build (464 kB), e2e 10/10.
- NEXT (iteration 20): /models (03_models.svg) — Remote Providers cards (OpenAI/Anthropic/Compatible: base URL, model, key mode, Test, status badges Connected/CORS/Auth failed/etc), Local Endpoints (Ollama/llama-server), Browser-Local wllama (HF repo/file, download queue, progress, load/unload, delete, quota). Inspector: Provider Health + Model Download Queue. Wire to providers + models slices.

## 2026-06-12T07:54:32Z - Claude Opus 4.8 - Ralph iteration 20: /models screen (VISUALLY VERIFIED)
- `/models` checked. 6 screens remain (storage, skills, memories, audit, settings, workflow).
- `src/screens/ModelsScreen.tsx`: own 2-col layout (main config + 300px right rail — NOT the shared inspector; AppLayout only shows inspector on /chat). Remote Providers (OpenAI/Anthropic/OpenAI-compatible) cards: status Badge from providers.health, Base URL/Model Input, API key mode Select, Test btn. Local Endpoints (Ollama/llama-server). Browser-Local Models table (SmolLM2/Mistral/Phi-3: name/size/status-from-models.downloads/Download|Load action). Right rail: Provider Health list (all 6), Model Download Queue (from models.downloads, EmptyState text), Troubleshooting.
- HEALTH_META maps ProviderHealth → {label,tone}: unconfigured→Not configured/neutral, connected→Connected/success, auth_failed→Auth failed/danger, cors_error→CORS issue/warning, model_not_found→warning, unreachable→danger.
- Test btn = PLACEHOLDER (dispatches providerHealthSet 'connected' + activeProviderSet) — Phase 7 replaces with real network test. Marked with comment.
- router SCREEN_OVERRIDES: added models→ModelsScreen.
- GOTCHA (test): heading.closest('div') returns the header flex div (heading+badge), NOT the card div with the Test button → use `getAllByRole('button',{name:'Test'})[0]` for the OpenAI card. General: for repeated card actions, index getAllByRole rather than closest().
- Test: lists providers + browser-local models + empty download queue; Test → providers.health.openai='connected' + activeProviderId='openai'. Now **33 files / 113 tests**.
- VISUAL (chromium screenshot): matches mockup — provider cards, local endpoints, right rail (Provider Health all 6 'Not configured', Download Queue empty, Troubleshooting), Models nav active.
- Gate green: typecheck, lint, format, 113/113, build (470 kB), e2e.
- NEXT (iteration 21): /storage (04_storage_backup.svg) — Storage Overview cards (IndexedDB usage/Model Cache/Persist from storage slice + storageService), Backup & Restore (Export/Import/Request persistent btns), what's-included list; right rail: Recent Backups, Local Data Health checks, Storage Recommendations. Wire to storage slice + refreshStorageInfo/requestPersistentStorage. (.clawbackup export is Phase 9 — Phase 6 just the UI shell + live storage numbers.)

## 2026-06-12T07:57:33Z - Claude Opus 4.8 - Ralph iteration 21: /storage screen (VISUALLY VERIFIED, real storage data)
- `/storage` checked. 5 screens remain (skills, memories, audit, settings, workflow).
- `src/screens/StorageScreen.tsx`: own 2-col layout. Storage Overview 3 StatCards (IndexedDB usedBytes+quota% from storage slice [REAL], Model Cache placeholder 0 B, Persistent storage Granted/Not granted + Request btn → requestPersistentStorage+refresh). Backup & Restore: Export/Import btns → toast "arrives in Phase 9" (honest placeholder; .clawbackup is Phase 9). What's-included list, Future Integrations (Google Drive). Right rail: Recent Backups (useLiveQuery db.backup_history, "No backups yet"), Local Data Health (5 ✓; last = health.level==='ok'?'Quota good':'Quota tight'), Storage Recommendations (assessStorageHealth.recommendations or "Usage normal").
- useEffect on mount → refreshStorageInfo(dispatch) populates storage slice from navigator.storage.estimate. WIN: in Chromium this returns REAL data (IndexedDB 14.2 KB, quota ~5GB) → top-bar storage pill now shows "0.00 GB / 5.00 GB" live (no longer "not measured"). assessStorageHealth → 'warning' when not persisted (shows "Quota tight" + recommendation) — correct.
- formatBytes helper (B/KB/MB/GB). router SCREEN_OVERRIDES: storage→StorageScreen.
- Uses useToast → test wraps in ToastProvider.
- Test: shows Storage Overview + IndexedDB + Backup & Restore + "No backups yet"; Export → toast "arrives in Phase 9". Now **34 files / 115 tests**.
- Gate green: typecheck, lint, format, 115/115, build, e2e.
- NEXT (iteration 22): /skills (05_skills.svg) — 3-col-ish: Installed Skills list (toggle ON/OFF wired to skills slice enabledIds + selectedSkillSet), Bundled Skills, Import Skill dropzone (.clawskill/SKILL.md — import is Phase 10, just UI); middle: Selected Skill detail with Tabs (Overview/Instructions/Files/Permissions/State/Audit — use the Tabs component), Permissions (allowed tools, skill fs access read/write paths, network). Wire skills slice (selectedSkillId, enabledIds). Skill data: static sample skills for now (durable skills store populated in Phase 10).

## 2026-06-12T08:03:34Z - Claude Opus 4.8 - Ralph iteration 22: /skills screen (VISUALLY VERIFIED)
- `/skills` checked. 4 screens remain (memories, audit, settings, workflow).
- `src/screens/SkillsScreen.tsx`: 2-col [320px + 1fr]. Left: Installed Skills + Bundled Skills lists (SkillRow = select-button[name+risk] + Toggle), Import Skill dropzone (.clawskill/SKILL.md — Phase 10). Right: selected skill detail — name + Enabled/Disabled Badge + `Tabs` (Overview/Instructions/Files/Permissions/State/Audit); Permissions tab shows Allowed tools (Badge Allowed), Skill filesystem access read/write paths, Network access.
- Wired to skills slice: enabledIds (Toggle), selectedSkillId (selectedSkillSet). useEffect selects 'summarize-pdf' by default if none. SAMPLE `SKILLS` static (durable skills store populated in Phase 10). Did NOT seed enabledIds → toggles start off (honest).
- ENHANCEMENT: added `ariaLabel?: string` to `Toggle` component (for standalone switches without a visible label; applied as aria-label when no labelledby). SkillRow uses ariaLabel={`Enable ${name}`}.
- e2e/visual.spec.ts ROUTES: added skills/memories/audit/settings/workflow (so future screens auto-screenshot; currently placeholders except skills).
- Test: lists installed+bundled, default-selects summarize-pdf (heading); toggle 'Enable web-search' → enabledIds; tab switch to Permissions shows Allowed tools/Network access. Now **35 files / 118 tests**.
- VISUAL (chromium): matches mockup — skill lists w/ toggles+risk, summarize-pdf selected/highlighted, Import dropzone, detail tabs, Skills nav active.
- Gate green: typecheck, lint, format, 118/118, build, e2e 10/10.
- NEXT (iteration 23): /memories (06_memories.svg) — search + filters (tags/source/sensitivity), memory list, editable detail panel w/ provenance (source conversation/message, created by/at, last used), pin/delete/edit, related memories, retrieval history. Wire memories slice (searchQuery/filterTags/selectedMemoryId) + Dexie memories store (useLiveQuery). Memory data: seed a couple sample memories OR empty state. Right rail tabs: Overview/Recently/History.

## 2026-06-12T08:08:02Z - Claude Opus 4.8 - Ralph iteration 23: /memories screen (VISUALLY VERIFIED, real Dexie data)
- `/memories` checked. 3 screens remain (audit, settings, workflow).
- ADDED `title: string` to MemoryRow (db/types.ts) — spec requires title + summary/body. Schemaless field, NO Dexie version bump needed. Updated db.test.ts memory put to include title.
- `src/screens/MemoriesScreen.tsx`: 2-col [1fr + 280px]. Search Input (→ memoriesSlice.searchQuery) + Sensitivity Select. Master/detail grid: memory list (buttons, pin icon if pinned, tags) + detail panel (title, body, tag Badges, Source/Created by/Sensitivity rows, Pin/Unpin + Delete). Right rail: Memory stats (Total/Pinned counts), Recently used (memories w/ lastUsedAt).
- Wired Dexie: seeds 3 SAMPLE_MEMORIES on mount if db.memories empty; useLiveQuery(db.memories.orderBy('createdAt')); client-side search filter (title/text/tags); selectedMemorySet; pin → db.memories.update; delete → db.memories.delete. REAL persistence + live query working in browser.
- GOTCHA (test): memory titles appear in BOTH the list buttons AND the "Recently used" rail <li> → getByText ambiguous. FIX: query list via `getByRole('button',{name:/title/})`. Also search only filters the LIST not the rail (rail shows all w/ lastUsedAt) — assert on buttons not text.
- Test: seeds+lists memories (waitFor live query), search 'ownership' filters list. Now **36 files / 119 tests**.
- VISUAL (chromium): real data — list w/ pinned Rust/WASM selected, detail panel w/ tags+provenance+Unpin/Delete, stats Total 3/Pinned 1, Recently used. Memories nav active.
- Gate green: typecheck, lint, format, 119/119, build, e2e 10/10.
- NEXT (iteration 24): /audit (07_audit.svg) — filters (event type/risk/conversation/provider/tool/date/clear), event table (Time/Event/Source/Risk/Status), expandable detail + JSON panel, summary metrics, risk breakdown, recent approvals, CSV export. Wire to audit slice (recent feed) — already has entries from runtime/vault audit events. Seed sample audit events OR show live audit.recent. Right rail: Audit summary, Risk breakdown.

## 2026-06-12T08:11:26Z - Claude Opus 4.8 - Ralph iteration 24: /audit screen (VISUALLY VERIFIED)
- `/audit` checked. 2 screens remain (settings, workflow).
- `src/screens/AuditScreen.tsx`: 2-col. Risk filter Select + Clear btn (auditCleared). Event table (Time/Event/Risk/Status) from audit.recent; rows clickable → expand JSON detail (<pre> JSON.stringify). Right rail: Audit summary (Total/Success/Failed), Risk breakdown (Progress bars per risk %), Export CSV.
- Wired audit slice. Seeds 5 SAMPLE_AUDIT entries on mount via seededRef pattern (useRef guard + deps [recent.length, dispatch] — satisfies exhaustive-deps WITHOUT reseeding after Clear; ref persists across renders in same mount). Export CSV builds Blob + anchor download (guarded if URL.createObjectURL missing in jsdom).
- formatTime: at<1e9 → "#N" (sample fixed timestamps), else toLocaleTimeString.
- Test: seeds+lists events; row click → "Details JSON" + JSON content; Clear → recent empty + "No audit events." Now **37 files / 122 tests**.
- VISUAL (chromium): event table w/ risk badges + OK status, summary Total 5, risk breakdown bars (low 40/medium 40/high 0/info 20), Export CSV, Audit nav active.
- Gate green: typecheck, lint, format, 122/122, build, e2e 10/10.
- NEXT (iteration 25): /settings (08_settings.svg) — sections General/Models/Security/Storage/Skills/Developer as cards (Theme/Language/Auto-start; Default provider/model/Fallback; Security: key storage mode/lock timeout/require approval/browser-direct key warning/network allowlist; Storage: data location/auto-backup/frequency; Skills: install policy/unsigned/auto-update; Developer: export logs/log level/dev mode/reset runtime). Mostly presentational controls (Toggle/Select/Input). Could wire a few to app slice (e.g. onboarding reset). Right rail: Environment (provider/model/runtime/mode) + Version.

## 2026-06-12T08:14:16Z - Claude Opus 4.8 - Ralph iteration 25: /settings screen (VISUALLY VERIFIED)
- `/settings` checked. ONLY /workflow remains in Phase 6.
- `src/screens/SettingsScreen.tsx`: 2-col. 6 Section cards in a 2-col grid: General (Theme/Auto-start), Models (Default provider/model), Security (Key storage mode/Lock timeout/Require approval/Warn browser-direct keys), Storage (Data location/Auto-backup), Skills (Allow unsigned/Auto-update), Developer (Log level/Dev mode/Reset runtime danger btn). Reusable Section + Field helpers. Right rail: Environment (provider/model/runtime status from slices, Mode/Data static) + Version (APP_VERSION x2, Up to date Badge).
- Toggles use local `flags` state (typed Flags interface — avoids noUncheckedIndexedAccess undefined). Reset runtime → dispatch(runtimeReset). All Toggles use ariaLabel.
- Test: 6 section headings render; toggle 'Require approval by default' flips aria-checked; Reset runtime → runtime.status 'ready'→'initializing'. Now **38 files / 124 tests**.
- VISUAL (chromium): 6 cards w/ controls, Environment rail (Runtime initializing), Version (Up to date), Settings nav active. Matches mockup.
- Gate green: typecheck, lint, format, 124/124, build, e2e 10/10.
- NEXT (iteration 26 — LAST Phase 6 screen): /workflow (09_user_workflow.svg) — user workflow explainer. Read mockup section ~line 497+ in BROWSERCLAW_TEXT_MOCKUPS.md. Likely a static explainer of the BrowserClaw workflow (steps/diagram). Then Phase 6 DONE → Phase 7 (provider system: normalized LLM interface, mock + OpenAI-compatible + presets, health tests, CORS/auth/error normalization) which makes chat actually converse.

## 2026-06-12T08:17:06Z - Claude Opus 4.8 - Ralph iteration 26: /workflow screen → PHASE 6 COMPLETE (all 9 screens)
- `/workflow` checked. **PHASE 6 DONE — all 9 screens built + visually verified in Chromium/Firefox.**
- `src/screens/WorkflowScreen.tsx`: static explainer. 8 numbered step cards (First-run setup → Choose model → Start chat → Retrieve memory/skills → Agent proposes → You approve → Save to memory/audit → Backup/restore) in a responsive grid (2/4 cols) w/ lucide icons. Privacy note (success-toned). Right rail: Typical user journey (5-item ordered list). No slice wiring (pure presentational).
- ROUTING: /workflow is NOT a nav item (sidebar = chat/models/storage/skills/memories/audit/settings). Added as explicit child route under AppLayout: `{ path:'workflow', element:<WorkflowScreen/> }` — NOT in SCREEN_OVERRIDES (that's only for NAV_ITEMS placeholders).
- Test: heading + step headings (First-run setup, You approve) + Typical user journey + Export backups. Now **39 files / 125 tests**.
- VISUAL (chromium): 8 step cards w/ icons+numbers, journey rail, privacy note. Matches mockup.
- Gate green: typecheck, lint, format, 125/125, build, e2e 10/10. NOTE: build now warns chunk >500KB (informational only, exit 0) — could add code-splitting (manualChunks/lazy routes) later; not blocking.
- PHASES DONE: 0,1,2,3(−migrations),4,5(−wasm32 browser build),6. NEXT = **Phase 7 — Provider System**: normalized LLM provider interface, mock provider, OpenAI-compatible provider, OpenAI/Anthropic/Ollama/llama-server presets, provider health tests (replaces the ModelsScreen Test placeholder w/ real fetch), normalize CORS/auth/model/network errors. THIS MAKES CHAT ACTUALLY CONVERSE: wire llmRequest effect port (effectExecutor) → provider call → resolve_effect back into runtime → assistant message persisted. Uses SecretVault for keys (getSecret), providers slice for active provider, providerHealthSet for health.

## 2026-06-12T08:20:27Z - Claude Opus 4.8 - Ralph iteration 27: Phase 7 provider library
- Started Phase 7. Checked: normalized LLM provider interface, mock provider, OpenAI-compatible provider, OpenAI/Ollama/llama-server presets, CORS/auth/model/network error normalization. REMAINING Phase 7: Anthropic preset, provider health tests (wire to ModelsScreen real Test), + wire llmRequest into chat.
- `src/providers/types.ts`: `LlmProvider` { id, complete(req, opts?), checkHealth(opts?) }. ChatMessage/CompletionRequest/CompletionResult. ProviderCallOptions { apiKey?, signal? } — apiKey passed PER-CALL from SecretVault, never stored on provider.
- `src/providers/errors.ts`: `ProviderError` {kind: cors|auth|model_not_found|unreachable|unknown}. httpStatusToKind (401/403→auth, 404→model_not_found, else unknown), kindToHealth (→ providers slice ProviderHealth), fetchOrThrow (wraps fetch; TypeError→unreachable; browser can't distinguish CORS from network).
- `src/providers/mockProvider.ts`: deterministic, echoes last user msg, health connected. Default until real provider configured + used in tests.
- `src/providers/openAiCompatible.ts`: createOpenAICompatibleProvider(config{id,baseUrl,model,fetchImpl?,headers?}) → POST {baseUrl}/chat/completions, Bearer auth from opts.apiKey, parses choices[0].message.content. !ok → ProviderError(httpStatusToKind). checkHealth = complete('ping') → connected/mapped.
- `src/providers/presets.ts`: createOpenAI (api.openai.com/v1, gpt-4o), createOllama (localhost:11434/v1), createLlamaServer (localhost:8080/v1) — all OpenAI-compatible shims. fetchImpl injectable.
- exactOptionalPropertyTypes gotcha: spread conditional props `...(opts.signal ? {signal: opts.signal} : {})` instead of `signal: opts.signal` (can't assign undefined to optional). Same for fetchImpl in presets.
- Tests `providers.test.ts`: mock echo; openai-compat posts to right URL + Bearer header + parses reply (vi.fn fetch); 401→auth kind + auth_failed health; TypeError→ProviderError + unreachable health; httpStatusToKind/kindToHealth. Now **40 files / 130 tests**.
- Gate green: typecheck, lint, format, 130/130, build.
- NEXT (iteration 28): Anthropic preset (different API: POST /v1/messages, x-api-key + anthropic-version header, content[].text response — separate adapter from openai-compat), provider REGISTRY (resolve active provider from providers slice + SecretVault key), wire ModelsScreen Test → real checkHealth → providerHealthSet, WIRE CHAT: effectExecutor llmRequest port → active provider.complete(messages) → dispatch resolve_effect back into runtime → runtime emits storage_put(assistant msg) → persist to Dexie messages → appears in chat. This completes Phase 7 + makes chat converse (mock provider by default).

## 2026-06-12T08:27:09Z - Claude Opus 4.8 - Ralph iteration 28: Anthropic + registry + CHAT CONVERSES → PHASE 7 COMPLETE
- **Phase 7 DONE.** All provider boxes checked. CHAT NOW CONVERSES end-to-end (verified in real browser).
- `src/providers/anthropic.ts`: createAnthropicProvider — POST {baseUrl}/v1/messages, headers x-api-key + anthropic-version + anthropic-dangerous-direct-browser-access:true; hoists system msgs to `system` field; max_tokens 1024; parses content[].find(type==='text').text. Separate from openai-compat (different API shape).
- `src/providers/registry.ts`: resolveProvider(activeProviderId) → openai/anthropic/ollama/llama-server presets, DEFAULT → mock (so chat works out of the box w/ no config).
- `src/runtime/llmRunner.ts`: createLlmRequestHandler(deps{db,dispatch,getProvider,submit}) — the llm_request EFFECT PORT. Loads conversation history from Dexie → provider.complete(messages) → persists assistant MessageRow to Dexie (chat live-query shows it) → runStateSet('idle') → submit(resolve_effect) back into runtime (for audit). Provider errors → friendly "(kind)" message.
- WIRED in main.tsx bootRuntime: build ctx (mutable), createRuntimeHost(ctx, snapshot), THEN ctx.ports = { llmRequest: handler(getProvider: ()=>resolveProvider(state.providers.activeProviderId), submit: host.submit) }. KEY TRICK: executeEffect reads ctx.ports at call-time, so assigning ports AFTER host creation works (breaks the host↔port circular dep).
- THE LOOP: composer send → userMessageSubmitted → runtimeListener → host.submit(submit_user_message) → runtime emits [audit_append, llm_request] → executeEffect routes llm_request → llmRunner → provider reply → assistant msg in Dexie → useLiveQuery renders it.
- FIXED: ChatScreen approvals selector `state.approvals.queue.filter(...)` returned new array each render → react-redux unmemoized-selector warning (seen in browser console). FIX: select stable `queue` then filter in render body. (Pattern for all screens: select stable refs, derive in render.)
- Tests: anthropic (/v1/messages, x-api-key, system hoist), registry (mock default + presets), llmRunner (seeds user msg → handler → assistant reply persisted + resolve_effect submitted + runState idle). Now **41 test files / 133 tests**. NEW e2e `e2e/chat.spec.ts`: type 'hello there' → Send → asserts 'Mock response to: hello there' visible (REAL browser, passes).
- NOTE: ModelsScreen Test button still placeholder (dispatches 'connected') — real checkHealth wiring from UI deferred (network-dependent); the checkHealth logic itself is implemented + unit-tested per provider.
- Gate green: typecheck, lint, format, 133/133, build, e2e (visual 10 + chat 1).
- PHASES DONE: 0,1,2,3(−migr),4,5(−wasm32),6,7. NEXT = **Phase 8 — wllama**: browser-local GGUF via wllama in a Web Worker, HF model refs, download queue + progress UI, load/unload, cache delete, quota handling, compat warnings. wllama is a real npm dep (@wllama/wllama) — heavy (WASM). Consider: add a wllamaProvider implementing LlmProvider behind the same interface + a worker; wire downloads to models slice (modelDownloadUpdated) + ModelsScreen table. May need careful WASM/worker setup in Vite. Check if @wllama/wllama installs cleanly first.

## 2026-06-12T08:40:42Z - Claude Opus 4.8 - Ralph iteration 29: Phase 8 wllama (browser-local GGUF) → PHASE 8 COMPLETE
- **Phase 8 DONE** (all boxes). Real wllama engine wired but needs real-browser verification (can't run WASM/download in jsdom).
- `src/wllama/catalog.ts`: MODEL_CATALOG — HF GGUF refs (SmolLM2-1.7B, Qwen2.5-0.5B; repo/file/sizeLabel).
- `src/wllama/engine.ts`: `WllamaEngine` interface (download w/ progress, load, unload, deleteCache, complete, loadedModelId) — INJECTABLE for tests. `isWllamaSupported()` (WebAssembly + Worker). `createWllamaEngine()` REAL: lazy `await import('@wllama/wllama/esm/index.js')` → new Wllama(CDN wasm paths jsdelivr) → loadModelFromHF (progressCallback) / createChatCompletion (max_tokens 256) / exit. `getWllamaEngine()` singleton.
- CRITICAL GOTCHA: @wllama/wllama ships UNTYPED .ts SOURCE (no types field/exports map) → `import('@wllama/wllama')` resolved to src/*.ts and tsc type-checked THEM → errors under our strict config (skipLibCheck only covers .d.ts!). FIX: import the BUILT ESM `'@wllama/wllama/esm/index.js'` (sibling esm/*.d.ts → skipLibCheck'd) + cast `as unknown as WllamaModule` (minimal local interface decoupling our typecheck from wllama). wllama is a separate dynamic chunk (not in main bundle).
- `src/wllama/modelManager.ts`: createModelManager(dispatch, engine) — download (queue→downloading w/ % → ready, error+rethrow on fail), load (→activeModelSet), remove (→modelDownloadRemoved). Wired to models slice.
- `src/providers/wllamaProvider.ts`: createWllamaProvider(engine) — complete via engine, health connected/unconfigured by loadedModelId. Registered 'wllama' in registry (getWllamaEngine singleton).
- ModelsScreen wllama table now uses MODEL_CATALOG + manager: Download/Load/Delete buttons (runModelAction → toast on error), progress from models.downloads, compat warning banner when !isWllamaSupported, Download disabled when unsupported/downloading. ModelsScreen now uses useToast → test wrapped in ToastProvider.
- Tests `wllama/wllama.test.ts`: modelManager (download progress→ready, error path, load/remove), wllamaProvider (complete+health), isWllamaSupported. fakeEngine injection. Now **42 files / 139 tests**.
- Gate green: typecheck, lint, format, 139/139, build (wllama dynamic chunk), e2e.
- WLLAMA REAL-BROWSER VERIFICATION PENDING: actual GGUF download + WASM inference untested (needs real browser; single-thread works w/o COOP/COEP, multi-thread needs crossOriginIsolated). CDN wasm paths (jsdelivr @3.4.1/esm) are best-effort — verify/vendor later.
- PHASES DONE: 0,1,2,3(−migr),4,5(−wasm32 of OUR rust),6,7,8(−real-browser-verify). NEXT = **Phase 9 — Backup/Restore**: .clawbackup export (manifest + JSONL collections + optional encrypted secrets + model refs not files + skills/state), import validation, restore preview, conflict handling, backup history. Wire StorageScreen Export/Import (currently toast placeholders) to a real backup service over Dexie. Testable with fake-indexeddb.

## 2026-06-12T08:46:44Z - Claude Opus 4.8 - Ralph iteration 30: Phase 9 Backup/Restore
- Phase 9 essentially done (9/10 boxes). LEFT UNCHECKED (honest): "Export collections as JSONL" — I used a structured JSON document (manifest + collections as arrays) instead of newline-delimited JSON. JSONL is a streaming optimization for large collections; the JSON object round-trips fine + is simpler/robust. Convert later if needed.
- `src/backup/backupService.ts`: ClawBackup = { manifest{format:'clawbackup', schemaVersion:DB_VERSION, appVersion, createdAt, includesSecrets}, collections: Record<string,unknown[]> }. exportBackup(db, {includeSecrets?, now?}) reads 14 durable collections (app_settings/provider_profiles/conversations/messages/memories/todos/rules/schedules/skills/skill_files/skill_state/audit_events/model_catalog/model_cache_index) + optional encrypted_secrets (CIPHERTEXT only). NO model files (only model_cache_index references). serializeBackup (JSON). validateBackup(unknown)→{valid,error?,summary(counts),backup?}. importBackup(db,backup,strategy 'merge'|'replace') via db.table(name).bulkPut (merge=overwrite by key; replace=clear+put). recordBackupHistory→db.backup_history.
- StorageScreen wired: Export → exportBackup→serialize→recordBackupHistory→download .clawbackup (Blob+anchor, guarded URL.createObjectURL)→toast. Import → hidden file input → handleFile reads+validates → opens RESTORE PREVIEW Dialog (per-collection counts) → confirmRestore → importBackup(merge)+toast. Recent Backups rail (useLiveQuery db.backup_history) now populates after export.
- exactOptionalPropertyTypes gotcha: toast description: result.error is string|undefined → `result.error ?? 'fallback'` (can't pass undefined to optional-but-not-undefined prop).
- Tests `backup/backupService.test.ts`: export→validate→wipe→import round-trip (conversations+memories restored); secrets excluded by default/included on request; rejects non-backup; history recorded. StorageScreen export test updated → asserts "Backup exported". Now **43 files / 143 tests**.
- Gate green: typecheck, lint, format, 143/143, build.
- PHASES DONE: 0,1,2,3(−migr),4,5,6,7,8(−browser-verify),9(−JSONL). NEXT = **Phase 10 — Skills functionality**: .clawskill + SKILL.md import (parse frontmatter), install permission review, enable/disable, SkillPackageStore/SkillAssetStore/SkillStateStore (Dexie skills/skill_files/skill_state), safe SkillFs API (path-based reads/writes ONLY inside approved namespaces — NO arbitrary path/POSIX/raw IndexedDB), enforce permissions, skill audit events. Wire SkillsScreen (currently static SAMPLE_SKILLS) to real Dexie skills store. Testable w/ fake-indexeddb.

## 2026-06-12T08:50:54Z - Claude Opus 4.8 - Ralph iteration 31: Phase 10 skill system core (parse/fs/stores/manager)
- Started Phase 10. Core done: parsing, SkillFs (path enforcement), stores, manager+audit. UI wiring (SkillsScreen real data + import permission review) = iteration 32.
- `src/skills/skillTypes.ts`: SkillPermissions {tools[], read[], write[], network}, SkillManifest, ParsedSkill {manifest, instructions, files}.
- `src/skills/parseSkill.ts`: parseSkillMd (YAML-ish frontmatter parser: key:value, inline [a,b] arrays, true/false; body = instructions) + parseClawskill (JSON package; rejects missing manifest.name).
- `src/skills/skillFs.ts`: `isPathAllowed(path, namespaces)` — namespace 'skills/x/data/**' → prefix match; DENIES '..' traversal, paths outside namespaces, empty, absolute escapes. `SkillFs` class (#private fields — erasableSyntaxOnly forbids `private` param props!): readText (read namespace enforced), writeText (write namespace enforced), getState/setState (skill_state key-value). NO raw IndexedDB/arbitrary paths.
- `src/skills/skillManager.ts`: createSkillManager({db,dispatch,now?}) → install (skills + skill_files bulkPut + permissions in skill_state '__permissions__' + audit skill_installed), setEnabled (skills.update + audit), uninstall (delete skills+skill_files+skill_state + audit), fsFor(id) → SkillFs scoped to that skill's stored permissions. All emit auditAppended.
- Tests: parseSkill (frontmatter→manifest+body, defaults, clawskill JSON, reject), skillFs (allow inside / deny outside+traversal+empty), skillManager (install→audit+SkillFs read-denied-outside/write-allowed-inside/state, enable, uninstall+cleanup). Now **46 files / 151 tests**.
- Gate green: typecheck, lint, format, 151/151.
- NEXT (iteration 32): wire SkillsScreen to REAL Dexie skills (useLiveQuery db.skills, replace static SAMPLE_SKILLS), seed bundled skills on mount via manager, Import button → file → parse (clawskill/SKILL.md) → PERMISSION REVIEW Dialog (show tools/read/write/network) → manager.install, enable/disable toggle → manager.setEnabled. Detail panel from real SkillRow + permissions. Then Phase 10 DONE.

## 2026-06-12T08:59:50Z - Claude Opus 4.8 - Ralph iteration 32: Skills screen wired → PHASE 10 COMPLETE
- **Phase 10 DONE** (all boxes). SkillsScreen now fully wired to the real Dexie skill stores.
- Added `description: string` to SkillRow (db/types.ts); manager.install sets it from manifest.
- SkillsScreen rewritten: useLiveQuery(db.skills) for installed list (replaced static SAMPLE_SKILLS); seeds 2 bundled skills (web-search, summarize-pdf as SKILL.md) on mount if empty via manager.install (seededRef guard); detail panel useLiveQuery loads permissions (skill_state __permissions__) + files (skill_files) for selected skill; enable Toggle → manager.setEnabled (durable + audit); Uninstall → manager.uninstall; Import button → hidden file input → handleFile (parseClawskill if .clawskill else parseSkillMd) → PERMISSION REVIEW Dialog (PermissionView: tools/read/write/network) → confirmInstall → manager.install. Detail tabs Overview/Files/Permissions/State from real data.
- selectedSkillId still from skills slice; enabled now durable on SkillRow (not slice enabledIds).
- GOTCHA (test): seeding installs skills sequentially → web-search appears before summarize-pdf → synchronous getByRole races. FIX: `await findByRole` (retries) for BOTH. Test wraps in ToastProvider + fake-indexeddb + skills/audit reducers.
- Test: seeds bundled skills (both via findByRole); enable web-search → db.skills enabled true + Permissions tab shows Network access. Now **46 files / 150 tests**.
- VISUAL (chromium): real skills (summarize-pdf/web-search bundled v1.0.0), detail w/ real description + Uninstall + tabs, Import dropzone. Skills nav active.
- Gate green: typecheck, lint, format, 150/150, build, e2e 10/10.
- PHASES DONE: 0,1,2,3(−migr),4,5,6,7,8(−verify),9(−JSONL),10. NEXT = **Phase 11 — Memories functionality**: search, filters, detail editor, provenance, pin/delete/edit, retrieval history. MOSTLY DONE in iteration 23 (MemoriesScreen has search/filter/list/detail/pin/delete via Dexie). Remaining: memory detail EDITOR (edit text/title inline → db.memories.update), retrieval history (could be a memories-used log). Quick phase. Then Phase 12 (audit — also mostly done iter 24: filters/table/expandable/JSON/summary/risk/CSV; add a couple) + Phase 13 (QA).

## 2026-06-12T09:09:33Z - Claude Opus 4.8 - Ralph iteration 33: Memories editor + retrieval history → PHASES 11 & 12 COMPLETE
- **Phase 11 DONE** (added the 2 gaps) + **Phase 12 DONE** (audit was fully built in iter 24 — filters/table/expandable-JSON/summary/risk-breakdown/CSV; ticked now).
- MemoriesScreen: added INLINE EDITOR — Edit btn → editing mode w/ Input(title) + textarea(body) + Save/Cancel; saveEdit → db.memories.update(id,{title,text}). Added RETRIEVAL HISTORY rail (memories w/ lastUsedAt sorted desc, showing title + #lastUsedAt). Provenance/pin/delete/search/filter already done iter 23.
- Test: edit selected memory title → Save → heading shows new title (waitFor live query). Now **46 files / 151 tests**.
- Gate green: typecheck, lint, format, 151/151, build.
- PHASES DONE: 0,1,2,3(−migr),4,5,6,7,8(−verify),9(−JSONL),10,11,12. ONLY **Phase 13 — QA** remains:
  - Run typecheck/lint/tests ✓ (all green; will run final).
  - Test keyboard navigation — components use focus-visible + role/tab/switch; could add an e2e keyboard test.
  - Test reload persistence — Dexie persists; could add an e2e (reload, data survives).
  - Test backup export/import ✓ (unit tested).
  - Test provider error states ✓ (provider tests).
  - Test no decrypted secrets in Redux ✓ (vault guard test).
  - Test skill filesystem isolation ✓ (skillFs tests).
  - Test model quota error handling ✓ (modelManager error test).
- NEXT (iteration 34, FINAL): Phase 13 QA — add e2e for keyboard nav + reload persistence; run the FULL gate (JS typecheck/lint/test, Rust cargo test/fmt/clippy, Playwright both browsers); tick Phase 13; final summary. After that the TODO is COMPLETE (modulo documented deferrals: DB migrations baseline, JSONL backup, wasm32 browser build of our Rust runtime, wllama real-browser verification).

## 2026-06-12T09:17:05Z - Claude Opus 4.8 - Ralph iteration 34: Phase 13 QA → 🎉 ALL PHASES COMPLETE
- **Phase 13 DONE. The entire BROWSERCLAW_UI_TODO is complete** (161 boxes checked) except TWO intentional, documented deferrals: "Add migrations" (DB v1 is the baseline; no migration needed yet) and "Export collections as JSONL" (used a JSON document instead — round-trips fully).
- Added `e2e/qa.spec.ts`: keyboard nav (focus Models nav link → Enter → navigates) + reload persistence (edit a memory → reload → edit survives from IndexedDB). The other Phase 13 "Test X" items map to existing unit tests: backup export/import (backupService.test), provider error states (providers.test), no-secrets-in-Redux (vaultWiring.test guard), skill fs isolation (skillFs.test), model quota (wllama modelManager error test).
- E2E STRICT-MODE GOTCHA (both surfaced as Firefox-only flakes from slower timing): getByText substring matches → used `{exact:true}` for 'hello there' (chat user msg vs "Mock response to: hello there") and `{name, exact:true}` for the 'Models' heading (vs "Browser-Local Models").
- FINAL FULL GATE — ALL GREEN:
  - JS: typecheck ✓, lint (--max-warnings 0) ✓, format ✓, vitest **151 tests / 46 files** ✓, build ✓.
  - Rust: cargo test **7** ✓, cargo fmt --check ✓, clippy -D warnings ✓.
  - Playwright: **26 tests** (Chromium + Firefox: 11 visual×2 + chat×2 ... actually 13 specs ×2 = 26) ✓.
- ACCEPTANCE CRITERIA (BROWSERCLAW_UI_SPEC.md) all met: AppShell on all routes, Redux control plane, durable Dexie persistence through reload, runtime snapshots persisted, NO decrypted secrets in Redux/logs/audit, backup export/import, provider error states, wllama download/load/delete, skill import + permission review, audit log.
- DOCUMENTED DEFERRALS (need future/real-env work, all noted): DB migrations (v1 baseline), JSONL backup serialization, wasm32 BROWSER build of our Rust runtime (TS reference runtime works + is drop-in swappable), wllama real-browser GGUF download/inference verification (lazy engine wired; can't run WASM in jsdom), ModelsScreen provider Test button is a placeholder (real network health check from UI deferred; checkHealth logic implemented + unit-tested).
- 34 Ralph iterations total, all committed + pushed to GitHub. Project went greenfield → full app.

## 2026-06-12T09:39:55Z - Claude Opus 4.8 - Ticked "Add migrations" (mechanism in place, nothing to migrate at v1)
- Per user: "Add migrations" — nothing to migrate at v1, so the migration MECHANISM being established IS the deliverable. Agreed + ticked.
- In place: Dexie versioned schema `this.version(1).stores({...})` (Dexie's migration mechanism), documented forward-upgrade pattern (`version(n).stores().upgrade()`), and `on('populate')` stamping schemaVersion. A migration only gets WRITTEN at the first schema change (future v2).
- NOW only ONE TODO box unchecked: "Export collections as JSONL" (Phase 9) — intentional: backups use a structured JSON document that round-trips fully; JSONL is a streaming optimization. Everything else (163 boxes) checked.

## 2026-06-12T09:45:54Z - Claude Opus 4.8 - JSONL backup serialization → 🏁 ENTIRE TODO COMPLETE (164/164)
- Implemented the LAST box: backup export is now JSONL (first line manifest, then one `{"collection":name,"row":{...}}` per line — streams well for large collections). `parseBackup(text)` reads JSONL AND legacy single-JSON (back-compat), throws on no-manifest/unreadable. importBackup unchanged. StorageScreen import uses parseBackup instead of JSON.parse.
- Tests: serializeBackup produces multi-line JSONL w/ manifest first line; round-trip via parseBackup; legacy JSON still parses; garbage + manifest-less JSONL throw. Now **46 files / 152 tests**.
- **ALL 164 TODO BOXES CHECKED. 0 remaining.** Gate green: typecheck, lint, format, 152 tests, build.
- Remaining real-env caveats (NOT TODO boxes — documented): wasm32 browser build of our Rust runtime (TS reference runtime is the live impl, drop-in swap), wllama real-browser GGUF download/inference (lazy engine wired), ModelsScreen provider Test button placeholder (checkHealth logic implemented+tested, UI network-test deferred).

## 2026-06-12T10:22:57Z - Claude Opus 4.8 - Closing caveat #1: wasm32 toolchain + REAL WASM runtime in browser (Chromium+Firefox)
- User pushed back (rightly): I CAN test WASM in real browsers via Playwright even though jsdom can't. Done.
- TOOLCHAIN: `rustup target add wasm32-unknown-unknown` + installed wasm-pack 0.13.1 via the prebuilt-binary installer (curl init.sh | sh — fast, NOT cargo install).
- BUILT artifact: `pnpm build:wasm` = `wasm-pack build crates/claw-wasm --target web --out-dir ../../src/runtime/wasm --out-name claw_wasm`. Output committed (175KB claw_wasm_bg.wasm + 4KB glue + d.ts) — removed wasm-pack's generated .gitignore so it's tracked. Tooling: added src/runtime/wasm to .prettierignore + eslint ignores (wasm-pack output has its own eslint-disable but we ignore the dir).
- `src/runtime/wasmRuntime.ts`: createWasmRuntime(snapshot?) — lazy `import('./wasm/claw_wasm.js')`, `await wasm.default({module_or_path: wasmUrl})` (object form — string form is deprecated/warns), `new ClawRuntime()` or `ClawRuntime.fromSnapshot(JSON.stringify(snapshot))`. Adapts to the SAME ClawRuntimePort (dispatch JSON↔Effect[], snapshot). wasm asset via `import wasmUrl from './wasm/claw_wasm_bg.wasm?url'` (vite/client types cover *?url). Separate chunk — not in main bundle.
- WIRED AS DEFAULT: main.tsx bootRuntime now PREFERS the wasm runtime (createWasmRuntime), FALLS BACK to createReferenceRuntime if it can't load. Uses RuntimeHost(port, ctx) directly. So the app actually runs the Rust-compiled-to-WASM runtime in-browser; chat e2e now exercises it.
- `src/screens/WasmProbe.tsx` + route /wasm-probe: loads wasm runtime, dispatches a command, checks effects (audit+llm) + snapshot.message_count===1 + determinism, renders data-testid=wasm-probe "wasm-ok ...".
- EXTENDED TEST INFRA: base playwright.config testIgnore '**/*.extended.spec.ts'; new `playwright.extended.config.ts` (testMatch *.extended.spec.ts, timeout 600s, chromium+firefox); script `test:e2e:extended` = `playwright test -c playwright.extended.config.ts`. Normal `test:e2e` excludes extended.
- `e2e/wasm.extended.spec.ts`: /wasm-probe → expect 'wasm-ok' + 'mc=1'. **PASSES in Chromium AND Firefox.** Normal e2e still 26/26 (and now boots through wasm).
- Gate green: typecheck, lint, format, 152 tests, normal e2e 26/26 both browsers + wasm extended 2/2 both browsers.
- STILL TODO this session: caveat #2 (wllama real GGUF download+inference extended test, both browsers — use wllama's own CDN wasm config), caveat #3 (implement Models Test button to call real checkHealth — NO api key needed for reachability; auth_failed on 401 is useful; local endpoints need no key; + extended test).

## 2026-06-12T10:30:04Z - Claude Opus 4.8 - Closing caveat #3: Models Test button does REAL health check (no API key)
- User was right: a reachability/health check needs NO API key. Implemented.
- ModelsScreen testProvider rewritten: `await resolveProvider(id).checkHealth()` → dispatch providerHealthSet(result); only activeProviderSet on 'connected'. Per-id `testingIds` state → Button `loading` spinner while in flight. No key needed: 401 from a remote provider → auth_failed ("reachable, needs auth" — useful); local endpoints (Ollama/llama-server) need no key.
- Unit test updated: stub global fetch to reject (TypeError) → checkHealth → 'unreachable'; afterEach vi.unstubAllGlobals. (Was the old placeholder asserting 'connected'.)
- `e2e/models.extended.spec.ts`: click Ollama Test (4th Test button) → assert the Provider-Health rail Ollama status moves AWAY from "Not configured" (robust — actual outcome depends on what's listening locally; in THIS env localhost:11434 returned 404 → "Model not found", proving a real request was made). PASSES Chromium + Firefox.
- Gate green: typecheck, lint, format, 152 tests. Models extended 2/2 both browsers.
- ONE caveat left: #2 wllama real GGUF download + inference extended test (both browsers).

## 2026-06-12T11:37:09Z - Claude Opus 4.8 - Closing caveat #2: REAL wllama GGUF download + WASM inference (Chromium+Firefox) → ALL 3 CAVEATS CLOSED
- Real browser-local inference now VERIFIED in both browsers via extended e2e.
- ENGINE FIXES:
  - WASM path bug: my hardcoded `esm/single-thread/wllama.wasm` was WRONG — wllama v3 ships ONE wasm at `esm/wasm/wllama.wasm`. Fixed: new Wllama({ default: 'https://cdn.jsdelivr.net/npm/@wllama/wllama@3.4.1/esm/wasm/wllama.wasm' }).
  - FIREFOX OPFS bug: wllama caches models via OPFS `createSyncAccessHandle()` in a worker → Firefox automation throws "No modification allowed". useCache:false did NOT help (still uses the OPFS download worker). REAL FIX: added `loadModel(Blob[])` to the interface + `fetchGguf()` that streams the GGUF from HF (`https://huggingface.co/{repo}/resolve/main/{file}`) into an in-memory Blob; engine tries loadModelFromHF(useCache:true) and on the OPFS error falls back to fetchGguf→loadModel([blob]) (no OPFS). So Chromium caches (OPFS), Firefox runs from memory — both work.
  - SPEED: switched the probe to a TINY model SmolLM2-135M Q4 (~105MB, bartowski/SmolLM2-135M-Instruct-GGUF) — Qwen 0.5B (415MB) was too slow in Firefox's blob path (>9min). Added smollm2-135m to MODEL_CATALOG (now 3 models).
- `src/screens/WllamaProbe.tsx` + route /wllama-probe: download (progress) → complete → data-testid wllama-probe "done:<text>". `e2e/wllama.extended.spec.ts`: expect /^done:/ (540s timeout). PASSES Chromium (3.5m, OPFS) + Firefox (3.9m, blob). llama.cpp logs confirm real inference.
- 
- === ALL 3 CAVEATS NOW CLOSED (each Chromium + Firefox) ===
  1. WASM runtime: built (wasm-pack), wired as default (fallback to TS reference), e2e/wasm.extended ✓ both.
  2. wllama real GGUF download+inference: e2e/wllama.extended ✓ both (OPFS on Chromium, blob fallback on Firefox).
  3. Models Test button: real checkHealth (NO api key), e2e/models.extended ✓ both.
- Extended suite: `pnpm test:e2e:extended` (playwright.extended.config.ts) — opt-in, excluded from normal `test:e2e`. 6 extended tests (3 specs × 2 browsers).
- FULL GATE GREEN: typecheck, lint, format, 152 vitest, build, normal e2e 26/26, extended e2e 6/6. Rust unchanged (7 tests, fmt, clippy).
- Project status: ALL 164 TODO boxes + all 3 caveats done. Nothing left.

## 2026-06-12T14:15:47Z - Claude Opus 4.8 - IndexedDB model-blob cache (wllama hardening) + load coalescing
- WHY: Firefox can't use wllama's OPFS cache, so the blob fallback re-downloaded the GGUF every session. User asked: "Add an IndexedDB blob cache."
- Dexie schema **v2 migration** (DB_VERSION 1→2): new `model_blobs` store (`modelId, cachedAt`), `ModelBlobRow {modelId, blob: Blob, cachedAt}` in `src/db/types.ts`. Used `this.version(1).stores({...all 17...}); this.version(2).stores({ model_blobs: '...' })` — Dexie carries v1 tables forward, v2 declares only the delta. Now 18 stores (`db.test.ts` updated 17→18).
- NEW `src/wllama/modelCache.ts`: `ModelBlobStore` iface + `createDexieModelBlobStore(db)` (get/put/delete on `db.model_blobs`), `fetchGguf()` (moved here, injectable `fetchImpl`), `getOrFetchModelBlob(store, model, {fetchImpl?, onProgress?})` = cache hit returns blob, miss downloads + `store.put`. Unit-tested in `modelCache.test.ts` (round-trip + download-once-then-cache-hit). **Test needs `// @vitest-environment node`** — jsdom's Blob doesn't round-trip through fake-indexeddb's structuredClone, and undici's Response rejects jsdom Blobs ("object.stream is not a function").
- `engine.ts`: `WllamaEngineOptions {blobStore?}`; OPFS-error fallback now calls `getOrFetchModelBlob` (was bare `fetchGguf`); `deleteCache(modelId?)` deletes the cached blob; `getWllamaEngine()` wires `createDexieModelBlobStore(db)`. `modelManager.remove` → `engine.deleteCache(model.id)`.
- **BUG found + fixed: OPFS double-handle collision on Chromium.** After pointing WllamaProbe at the singleton `getWllamaEngine()`, React **StrictMode**'s double-effect called `loadModelFromHF` twice on the SAME wllama instance → Chromium threw `createSyncAccessHandle ... another open Access Handle`. FIX (production-valuable): `engine.loadFromHF` now **coalesces** concurrent same-model loads via an `inFlight {id, promise}` guard (runLoad does the real work); also `WllamaProbe`/`ModelCacheProbe` use a `useRef` one-shot guard. The old per-call engine had masked this real race.
- **Verification lesson: don't assert on wllama's flaky OPFS.** First attempt asserted `model_blobs > 0` after a wllama load on Firefox — FLAKY: Firefox OPFS sometimes WORKS (then the blob fallback never runs, count=0). Replaced with a **deterministic** probe: NEW `src/screens/ModelCacheProbe.tsx` + route `/model-cache-probe` drives `getOrFetchModelBlob` directly (no wllama/OPFS), and `wllama.extended.spec.ts` counts `huggingface.co/.../resolve/` requests → downloads once, 0 new requests after reload. Runs in BOTH browsers.
- Extended e2e GREEN both browsers (4 passed, 3.5m): wllama download+inference (chromium 1.8m, firefox 2.7m) + blob-cache-persists-across-reload (chromium 47s, firefox 50s). Unit gate green: typecheck, lint, format, **154 vitest** (was 152, +2 modelCache), build.
- STILL deferred (not requested now): "vendor the wasm" = copy `node_modules/@wllama/wllama/esm/wasm/wllama.wasm` into the app + serve via Vite `?url` instead of the jsdelivr CDN (removes the runtime CDN dependency). WASM_URL is still the CDN.

## 2026-06-12T19:59:41Z - Claude Opus 4.8 - Hardening pass Pass 1 (Phase 0): fail-closed safety config policy + banner
- CONTEXT: New hardening pass started (Ralph loop on docs/BROWSERCLAW_RUNTIME_STORAGE_SECURITY_HARDENING_TODO.md). ChatGPT 5.5's canonical decisions are in docs/replies1.md; my review/questions in docs/responses1.md. Reordered Q1 plan: security/correctness-critical first. Pass 1 = TODO Phase 0 (config policy + guardrails).
- PRINCIPLE (canonical, repeated everywhere): a user must NEVER believe BrowserClaw used the real runtime/provider/audit/memory/skill-permissions when it actually used a mock/fallback/no-op/seeded path. App fails closed by default.
- NEW `src/config/appConfig.ts`: `parseAppConfig(env)` (pure, total) → `AppConfig {isDemoMode, isDevFallbackAllowed, isMockProviderAllowed, isSafetyOverrideActive}`. Flags off unless exact `true` (string OR boolean). `VITE_DEMO_MODE` implies both dev overrides. Singleton `appConfig = parseAppConfig(import.meta.env)`. Env vars typed in `src/vite-env.d.ts` (ImportMetaEnv augmentation): VITE_DEMO_MODE, VITE_ALLOW_REFERENCE_RUNTIME_FALLBACK, VITE_ALLOW_MOCK_PROVIDER.
- NEW `src/components/shell/SafetyOverrideBanner.tsx`: persistent amber banner (warning/warning-subtle tokens, data-testid=safety-override-banner) listing active unsafe paths; renders null when fail-closed. Takes `config?: AppConfig` (defaults to appConfig) for testability. Wired into `AppLayout.tsx` above `<Outlet/>`.
- NEW `HARDENING_NOTES.md` (repo root): the 5 ground-rule invariants + the flag table.
- DELIBERATELY did NOT change main.tsx bootRuntime or providers/registry.ts this pass — flipping the WASM fallback (Phase 1.1) and mock-provider resolver (Phase 2.2) lands WITH their paired behavior tests so every commit stays green (can't commit red guardrail tests under zero-tolerance gate). appConfig is the single source those later phases will consume.
- TESTS: appConfig.test.ts (fail-closed defaults, non-"true" values off, per-flag opt-in, demo implies both) + SafetyOverrideBanner.test.tsx (null by default, lists active). Gate GREEN: typecheck, lint (--max-warnings 0), 164 vitest (was 154, +10).
- NEXT (Pass 2 = Phase 1.1): WASM runtime fail-closed — bootRuntime hard-blocks on WASM load failure (status error, blocking card, durable audit failure); reference runtime ONLY when appConfig.isDevFallbackAllowed, with banner + runtime.reference_fallback_used audit. Must ensure e2e/dev env builds WASM (pnpm build:wasm) or the gate trips on the new blocking card.

## 2026-06-12T20:10:47Z - Claude Opus 4.8 - Hardening Pass 2 (Phase 1.1): WASM runtime fails closed
- WHY: main.tsx silently fell back WASM→TS reference runtime on load failure (only a console.warn) — a user could believe the real runtime ran when it didn't. Spec §1 / TODO 1.1.
- runtimeSlice.ts: added `mode: 'wasm'|'reference'|null` and `fatal: boolean` to RuntimeState; new actions `runtimeLoaded({mode})` (status ready + records mode) and `runtimeFailed(msg)` (status error, mode null, fatal true). Updated runtimeReset/test for new shape. NOTE: pre-existing gap — NOTHING dispatched runtimeReady before, so the StatusFooter showed 'initializing' forever; boot now dispatches runtimeLoaded so status is real.
- NEW `src/runtime/runtimeBoot.ts`: `loadRuntimePort(deps, snapshot)` = the fail-closed resolver (testable seam, deps-injected: config/createWasm/createReference/onLoaded/onFallback/onFailed). Default: WASM fails → port null + onFailed (NO reference). Reference runtime used ONLY when `appConfig.isDevFallbackAllowed` (the VITE_ALLOW_REFERENCE_RUNTIME_FALLBACK / demo flag from Pass 1). Unit-tested all 3 branches in runtimeBoot.test.ts.
- main.tsx bootRuntime rewritten to use loadRuntimePort with Redux+audit callbacks; `if (!port) return` skips host/listener wiring on fatal. Audit (Redux feed only for now — durable is Phase 3): runtime.loaded (info), runtime.reference_fallback_used (high), runtime.load_failed (high). Used risk 'high' not 'critical' (AuditRisk enum still info|low|medium|high; 'critical' added in Phase 3 reconciliation).
- NEW `src/components/shell/RuntimeBlockedScreen.tsx` (full-screen ErrorState + Reload, data-testid=runtime-blocked); AppLayout returns it when `runtime.fatal`. SettingsScreen Runtime row now shows `status (mode)`.
- e2e CONFIRMED WASM loads in Playwright chromium+firefox (26/26 normal e2e pass) so fail-closed boot does NOT trip the block. WASM artifacts are git-tracked in src/runtime/wasm/, so dev/e2e boot works without rebuilding.
- Gate GREEN: typecheck, lint, 172 vitest (was 164, +8), 26 e2e both browsers.
- NEXT (Pass 3 candidates per Q1 order): fail-closed PROVIDER — Phase 2.2 (remove implicit mock fallback in providers/registry.ts; unknown/no provider → error; mock only when appConfig.isMockProviderAllowed + explicitly selected) and Phase 2.4 (provider error → effect error card, not fake assistant message). NOTE: e2e/chat.spec.ts currently relies on the implicit mock provider — Phase 2.2 must update it (TODO 11.1).

## 2026-06-12T20:20:18Z - Claude Opus 4.8 - Hardening Pass 3 (Phase 2.2): provider resolution fails closed (no implicit mock)
- WHY: providers/registry.ts resolveProvider ended in `default: return createMockProvider()` — unknown/null provider IDs silently became mock (a user could believe a real provider answered). Spec §3 / TODO 2.2.
- registry.ts: resolveProvider now takes (activeProviderId, config: Pick<AppConfig,'isMockProviderAllowed'>) and returns a discriminated `ResolvedProvider = {ok:true,provider} | {ok:false, reason:'not_configured'|'unknown_provider'}`. null→not_configured, unknown id→unknown_provider, 'mock'→ok only if isMockProviderAllowed. NEW helpers: `isProviderConfigured(id, isMockAllowed)` (cheap, side-effect-free — does NOT instantiate; resolving 'wllama' would spin up an engine) and `defaultActiveProviderId(config)` ('mock' iff allowed else null).
- main.tsx: getProvider now throws if resolution !ok (DEFENSIVE only — UI blocks first). Pre-selects mock at boot via activeProviderSet({id:'mock'}) iff defaultActiveProviderId(appConfig) (i.e. mock allowed) AND no provider set — so demo/dev gets a working console behind the banner; production stays null.
- ChatScreen.tsx: reads activeProviderId + computes providerReady=isProviderConfigured(...). When NOT ready: shows "No provider configured" EmptyState with a Link to /models (data-testid=chat-setup-cta), disables the composer, and handleSend early-returns. (Honest "chat blocks + setup CTA" per 2.2 acceptance — implemented at the UI layer, no runtime changes.)
- ModelsScreen.tsx testProvider: updated to new resolveProvider result shape (real ids always ok).
- DID NOT touch llmRunner's provider-error path (the "provider could not respond" fake assistant message) — that's Phase 2.4 / Pass 4. 2.2 adds NO new lying path: in production no provider → chat is blocked before llmRunner is ever reached.
- e2e: playwright.config.ts webServer now sets `env: { VITE_ALLOW_MOCK_PROVIDER: 'true' }` so the dev server auto-selects mock and the chat spec has a provider. (Vite exposes shell VITE_* via process.env.) Note reuseExistingServer:true — kill a stale :4321 dev server if the flag doesn't apply.
- Tests updated (TODO 11.1): providers.test.ts registry block rewritten (null/unknown do NOT become mock; mock gated by flag; + isProviderConfigured/defaultActiveProviderId). ChatScreen.test.tsx now includes providers reducer + MemoryRouter (CTA uses <Link>), sets providerId:'openai' for the chat-flow tests, + new "blocks chat with setup CTA" test.
- Gate GREEN: typecheck, lint, 177 vitest, 26 e2e both browsers (incl. mock chat).
- NEXT (Pass 4 = Phase 2.4): provider failures (and the defensive no-provider throw) → honest chat error CARD, not a fake assistant message. Add chat error state (chatSlice), llmRunner stops writing the "provider could not respond" message, audit provider failure, CORS classification. May need runtime-contract thought (resolve_effect currently always emits "Assistant message stored" audit — true effect-error propagation into Rust runtime likely deferred to Phase 5).

## 2026-06-12T20:26:33Z - Claude Opus 4.8 - Hardening Pass 4 (Phase 2.4): provider failures = honest error card, not fake reply
- WHY: llmRunner.ts caught provider errors and wrote them as an ASSISTANT message ("The provider could not respond (kind)") — a fake reply. Spec §3 / TODO 2.4.
- chatSlice.ts: added `error: ChatError|null` ({kind,message}) + actions `chatErrored` (runState='error') and `chatErrorCleared`; userMessageSubmitted now clears error. (initialState gained error:null — chatSlice.test updated.)
- providers/errors.ts: NEW `providerErrorMessage(kind)` (user-facing strings; 'unreachable' → "possible CORS or network issue") + `describeProviderError(error)` → {kind, message} (non-ProviderError → 'unknown'). No secrets in messages.
- llmRunner.ts: on provider throw → NO db.messages.put; dispatch chatErrored(failure) + auditAppended('provider.request_failed', risk medium); resolve_effect with `{ok:false, error}`. Success path now resolves with `{ok:true, text}` (still writes the message directly).
- **RUNTIME CONTRACT CHANGE (TS reference + Rust, kept in lockstep for determinism):** resolve_effect of an llm_request now branches on failure. NEW helper isFailure(result) = `result.ok===false || 'error' in result`. On failure: emit ONLY an audit_append `llm_request_failed` (risk medium), NO storage_put, NO message_count++. On success: unchanged (storage_put + 'llm_response_received'). Mirrored in crates/claw-core/src/lib.rs (`result.get("ok")==Some(false) || result.get("error").is_some()`). **Rebuilt WASM (`pnpm build:wasm`)** so the real runtime has it — claw_wasm_bg.wasm is committed. This resolves the earlier worry that the runtime always emitted "Assistant message stored" even on failure.
- ChatScreen.tsx: renders `<ErrorState>` (wrapped in div data-testid=chat-error) when chat.error set, below messages. (ErrorState doesn't pass through arbitrary props, hence the wrapper div.)
- Tests: chatSlice (error set/clear), llmRunner (failure → no assistant msg + error state + provider.request_failed audit + resolve {ok:false}), referenceRuntime + claw-core (failure resolve → 1 audit, no storage_put). Gate GREEN: typecheck, lint, 181 vitest (+4), cargo test (5, incl new) + clippy -D warnings + fmt --all, WASM rebuilt, 26 e2e both browsers.
- Phase 2 (provider) now COMPLETE end-to-end: 2.1 profiles still pending (Phase 9.3/2.1), 2.2 done (Pass 3), 2.3 SecretVault wiring pending, 2.4 done (Pass 4).
- NEXT (per Q1 order, pass 3 = durable audit MVP): Phase 3.1 — durable IndexedDB audit service (appendAuditEvent/queryAuditEvents/getAuditEvent/exportAuditCsv) + reconciled AuditEventRow schema (Q2: numeric `at`, risk info|low|medium|high|critical, source/status/correlation IDs, Dexie migration). Then 3.2 remove fake audit seeding, 3.3 wire events. NOTE: AuditRisk enum currently lacks 'critical' — add in 3.1. Also: Phase 1.2 (effect ports fatal) still deferred until storage/snapshot handlers exist (Phase 5.1/Phase 4).

## 2026-06-12T20:34:06Z - Claude Opus 4.8 - Hardening Pass 5 (Phase 3.1): durable audit service + reconciled schema + v3 migration
- WHY: audit was Redux-only (transient, capped ~50) — no durable write-path. Building the durable service is the largest plan item. Spec §4 / TODO 3.1. (This pass = service+schema only; 3.2 remove-seeding and 3.3 wire-events are separate passes.)
- db/types.ts AuditEventRow RECONCILED (Q2 canonical): `at:number` stays canonical; added `timestamp?:string` (derived ISO), `source:AuditSource` (user|runtime|provider|storage|skill|backup|model|system), `status:AuditStatus` (success|failure|pending|rejected|cancelled), correlation ids (conversationId/providerId/skillId/toolName/modelId/effectId/runId), `details?`, `demo?`. **AuditRiskLevel now includes 'critical'** (info|low|medium|high|critical).
- db.ts: DB_VERSION 2→3. `this.version(3).stores({ audit_events: 'id, type, risk, at, source, status' }).upgrade(...)` backfills existing rows via `backfillAuditDefaults` (source??='system', status??='success'). No new store (still 18) so db.test.ts unaffected.
- NEW `src/audit/auditService.ts`: appendAuditEvent / queryAuditEvents (filter type|risk|source|status + limit, newest-first via orderBy('at').reverse()) / getAuditEvent / exportAuditCsv + auditEventsToCsv (core columns only, never `details`) + buildAuditRow (defaults risk 'info'/status 'success'/timestamp ISO; omits undefined optionals for exactOptionalPropertyTypes) + redactDetails (deep-strips keys matching /api[-_]?key|token|authorization|secret|password|bearer|credential/i → '[redacted]') + backfillAuditDefaults. NOTE: db.ts imports backfillAuditDefaults (value) from auditService; auditService imports BrowserClawDB type-only → no runtime cycle (verbatimModuleSyntax erases it).
- redaction gotcha fixed: pattern first had `access[-_]?token` which missed plain 'token'; broadened to `token`.
- Tests: auditService.test.ts (buildAuditRow defaults/omits, redactDetails depth+arrays, backfill, durable append+get+redact, query by each field + limit + ordering, CSV escaping + no-details). Uses fake-indexeddb + new BrowserClawDB() (all share DB_NAME 'browserclaw') with afterEach db.delete().
- Gate GREEN: typecheck, lint, 189 vitest (+8), 26 e2e both browsers.
- NEXT (Pass 6 = Phase 3.3 then 3.2): wire appendAuditEvent into real emit sites (runtime load/fail/fallback already dispatch Redux audit in main.tsx — switch/augment to durable; provider test, LLM start/success/failure, effect lifecycle, memory CRUD, skills, secrets, backup, model). The effectExecutor audit_append handler (Redux) + main.tsx appendAudit (Redux) should ALSO write durable via auditService. Then 3.2: AuditScreen reads durable (useLiveQuery on db.audit_events) instead of Redux SAMPLE_AUDIT seeding; empty state; demo-gate. Consider: keep Redux feed as live tail OR drive AuditScreen from Dexie directly.

## 2026-06-12T20:42:00Z - Claude Opus 4.8 - Hardening Pass 6 (Phase 3.2 + existing 3.3 emit points): durable audit write-path + AuditScreen reads durable; fake seeding removed
- WHY: AuditScreen SEEDED fake SAMPLE_AUDIT into Redux on first view (a lie) and nothing wrote durably during normal op. Spec §4 / TODO 3.2 + 3.3.
- NEW `src/audit/auditSink.ts` `recordAudit(db, dispatch, input)`: builds row (buildAuditRow, sync, has id), dispatches Redux tail SYNC (so callers needing only the live feed don't await), returns the durable `db.audit_events.put(row)` promise. This dual-writes durable + live tail from ONE call.
- auditService: added `normalizeRiskLevel(str)` → AuditRiskLevel (5). auditSlice AuditRisk widened to include 'critical'.
- WIRED to recordAudit: effectExecutor audit_append (source 'runtime', awaited — removed old normalizeAuditRisk/auditAppended), main.tsx appendAudit (source 'runtime', now takes status arg: loaded=success, fallback=success, load_failed=failure), llmRunner provider.request_failed (source 'provider', status failure, +conversationId), skillManager audit() (source 'skill', fire-and-forget void). **DEFERRED: vaultWiring** secret audits stay Redux-only (its observer has dispatch but no db) — wire in Phase 2.3.
- DURABLE AUDIT ID = random (buildAuditRow), NOT the effect id — avoids cross-session eff-N collisions overwriting old audit rows. Updated effectExecutor.test to assert on type/risk/at (+durable row) instead of literal id 'a1'.
- AuditScreen.tsx REWRITTEN: useLiveQuery(db.audit_events.orderBy('at').reverse()) — no Redux, no SAMPLE_AUDIT/seededRef/useEffect. 5-level risk filter + RISK_TONE(+critical), real per-row STATUS_META (success/failure/pending/rejected/cancelled), summary success/failed counts from row.status, Clear → db.audit_events.clear() (durable), Export CSV → auditEventsToCsv(durable rows). Empty state "No audit events." when empty.
- AuditScreen.test REWRITTEN: seeds durable via appendAuditEvent, asserts render + empty state + expand JSON + Clear empties durable (db.audit_events.count()===0). beforeEach clears db.audit_events.
- Gate GREEN: typecheck, lint, 190 vitest (+1 net; AuditScreen test restructured), 26 e2e both browsers. NOTE: Clear permanently wipes audit history (parity with old mockup) — revisit append-only-ness in Phase 10 if desired.
- Phase 3 now ~complete: 3.1 service (Pass 5), 3.2 remove seeding + read durable (Pass 6), 3.3 PARTIAL (runtime/provider/skill wired; memory/backup/model/secret events wire in their own phases as those flows get hardened). 
- NEXT (Pass 7): options — (a) Phase 4 runtime snapshot persistence (debounced saves, before-unload flush, restore audit) [Q5]; (b) Phase 5.1 storage effect handlers (then unblocks Phase 1.2 effect-ports-fatal); (c) Phase 2.3 SecretVault provider wiring (+ durable secret audits). Per Q1 order pass 4 = provider profiles + SecretVault wiring → lean toward 2.3/2.1 next, but 2.3 is sizable. Phase 4 snapshots is a cleaner bounded next step.

## 2026-06-12T20:48:06Z - Claude Opus 4.8 - Hardening Pass 7 (Phase 5.2 + Q6): remove fake memory seeding; demo-gate + one-time cleanup
- WHY: MemoriesScreen did `db.memories.bulkPut(SAMPLE_MEMORIES)` on first visit — fake data written to DURABLE Dexie. Spec §6 / TODO 5.2 / replies1.md Q6.
- db/types.ts MemoryRow: added `demo?: boolean`.
- NEW `src/memories/sampleMemories.ts`: SAMPLE_MEMORIES (the 3 samples, now `demo:true`) moved out of the screen + `isUnmodifiedSampleMemory(row)` = matches a seed by id+title+createdBy+source (Q6's criteria; ignores text/tags/pinned/lastUsedAt so pin-toggles still count as untouched, and an edited title/source is preserved).
- db.ts: DB_VERSION 3→4. v4 upgrade = ONE-TIME cleanup: for each sample id, delete the row IFF isUnmodifiedSampleMemory (removes old unconditionally-seeded samples from existing installs; leaves edited ones). Fresh DBs (e2e) skip upgrades so nothing deleted there.
- MemoriesScreen.tsx: seeding now gated `if (!appConfig.isDemoMode) return;` (normal build starts empty → existing "No memories found" EmptyState). Imports SAMPLE_MEMORIES from the module. Detail pane shows a "Demo" Badge when `selected.demo` — placed OUTSIDE the <h2> so it doesn't pollute the heading's accessible name (Testing Library getByRole name = exact; Playwright = substring, so e2e heading checks still pass).
- playwright.config.ts: e2e webServer env changed VITE_ALLOW_MOCK_PROVIDER→**VITE_DEMO_MODE=true** (demo implies mock AND seeds demo memories) so the memory specs (qa edit-persists, visual renders memories) and chat spec all work. Safety banner now shows in e2e (honest; no spec asserts its absence).
- Tests: MemoriesScreen.test seeds durable in beforeEach (db.memories.clear+bulkPut SAMPLE_MEMORIES) since no auto-seed; renamed "seeds and lists"→"lists". NEW sampleMemories.test (all demo-tagged; isUnmodifiedSampleMemory matches untouched incl. no-demo-field original, rejects edited title/source + unknown id).
- Gate GREEN: typecheck, lint, 194 vitest (+4), 26 e2e both browsers (demo mode).
- Audit/memory fake-seeding now BOTH removed (Pass 6 audit, Pass 7 memory). 
- NEXT (Pass 8): remaining big items — Phase 2.1 provider profiles persistence (durable ProviderProfile CRUD + Models screen controlled/persisted form + status bar active provider) and/or Phase 2.3 SecretVault provider wiring (secret refs, key UI, retrieve-from-vault in test+runtime, secret_locked/secret_missing, + durable secret audits deferred from Pass 6). Also still pending: Phase 4 snapshots, Phase 1.2 effect-ports-fatal (needs storage handlers Phase 5.1 first), Phase 6 backup hardening, Phase 7 skills hardening, Phase 8 wllama CDN, Phase 9/10 UI persistence/honesty.

## 2026-06-12T20:52:58Z - Claude Opus 4.8 - Hardening Pass 8 (Phase 4): runtime snapshot persistence (debounced) + restore/save audits + corrupt-snapshot fallback
- WHY: RuntimeHost.saveSnapshot() existed but was NEVER called during normal op; restore wasn't audited; a corrupt snapshot could crash boot. Spec §5 / TODO Phase 4 / replies1.md Q5.
- NEW `src/runtime/snapshotScheduler.ts` `SnapshotScheduler(save, delayMs=500, onError?)`: schedule() debounces (coalesces many→1 write); flush() persists immediately + cancels timer; save failure → onError + KEEP pending (next flush retries), NEVER silently dropped; #running guard prevents overlap.
- runtimeHost.ts: RuntimeHost ctor takes `options?: { snapshot?: { delayMs?, onError? } }`; when set, builds a SnapshotScheduler(()=>this.saveSnapshot()). submit() calls this.#snapshots?.schedule() after effects run. NEW flushSnapshot() for idle/unload. createRuntimeHost (no options) → snapshots OFF (existing tests unaffected; they call saveSnapshot manually).
- runtimeBoot.ts: createReference type now `=> ClawRuntimePort | Promise<ClawRuntimePort>` and `await deps.createReference(snapshot)` — so the async restore wrapper can wrap BOTH factories.
- main.tsx: `withSnapshotRestore(create)` wraps createWasm+createReference — if create(snapshot) throws (corrupt/incompatible), audit 'runtime.snapshot_restore_failed' (medium/failure) + retry create(undefined) [fresh; if THAT throws it's a real runtime failure → propagates → fail-closed]. Success with snapshot → audit 'runtime.snapshot_restored' (info/success). Host built with `{ snapshot: { delayMs:500, onError: onSnapshotSaveError } }` (onError audits 'runtime.snapshot_save_failed' medium/failure + console.error). `window.addEventListener('pagehide', ()=>void host.flushSnapshot())`. Q5 honored: don't audit every debounced save — only restore success/failure + save failure.
- Snapshots use RuntimeHost.saveSnapshot → runtime_snapshots key 'latest' (loadLatestSnapshot reads it). The runtime_snapshot_save EFFECT (effectExecutor, keyed by effect.id) is a SEPARATE unused path — reference/WASM runtime never emits it; not wired here.
- Tests: snapshotScheduler.test (coalesce, no-op when nothing scheduled, flush-immediate+cancel, failure→onError+retry-on-flush; vi.useFakeTimers + advanceTimersByTimeAsync). runtimeHost.test +1 (submit then flushSnapshot persists message_count=1). Gate GREEN: typecheck, lint, 199 vitest (+5), 26 e2e both browsers.
- Phase 4 acceptance "reload restores runtime state" proven at unit level; a real-browser reload→snapshot_restored-audit e2e deferred to Phase 11.3 integration tests.
- NEXT (Pass 9): big remaining — Phase 2.1 provider profiles persistence (Models screen controlled/persisted form + ProviderProfile CRUD + status bar) and Phase 2.3 SecretVault provider wiring (security-critical; + durable secret audits deferred from Pass 6). Also pending: Phase 1.2 (effect-ports-fatal, needs Phase 5.1 storage handlers first), Phase 6 backup hardening, Phase 7 skills hardening, Phase 8.1 wllama CDN, Phase 9/10 UI persistence/honesty, Phase 11 QA.

## 2026-06-12T21:23:56Z - Claude Opus 4.8 - Hardening Pass 9 (Phase 2.1): durable provider profiles — CRUD service + persisted Models form + registry overrides + boot restore
- WHY: ProviderProfileRow + provider_profiles table existed but were UNUSED. Models screen fields were `defaultValue`-only (edits evaporated); resolveProvider/runtime ignored persisted base URL/model; status bar didn't reflect the active profile's model. TODO 2.1.
- NEW `src/providers/providerProfiles.ts`: DEFAULT_PROVIDER_PROFILES (openai, anthropic, compatible, ollama, llama-server — NOT auto-seeded, merged over persisted). CRUD over db.provider_profiles: list/get/saveProviderProfile(put-upsert)/deleteProviderProfile. Active id persisted in app_settings key 'activeProviderId' via get/setActiveProviderId. getActiveProviderProfile = active id → persisted row ?? template. mergeProviderProfiles(persisted) overlays saved rows on templates in template order (edits win, untouched fall back).
- registry.ts: resolveProvider gained optional 3rd arg `overrides?: {baseUrl?,model?}` (ProviderOverrides). presetOptions() drops blank fields (so cleared field → preset default). Threaded into openai/anthropic/ollama/llama-server presets. ADDED 'compatible' case (createOpenAICompatibleProvider with override baseUrl/model) + 'compatible' to REAL_PROVIDER_IDS. mock/wllama ignore overrides. Backward-compatible (3rd arg optional).
- providersSlice: added activeProviderModel + activeProviderBaseUrl (null init); activeProviderSet payload gained optional model+baseUrl. AppLayout status bar modelLabel = activeProviderModel ?? models.activeModelLabel (remote model preferred, local wllama model fallback).
- ModelsScreen REWRITE: removed REMOTE_PROVIDERS/LOCAL_ENDPOINTS/ALL_PROVIDERS consts. useLiveQuery(listProviderProfiles)→mergeProviderProfiles; renders provider sections ONLY after persisted loads (else "Loading…") so each ProviderCard's controlled useState seeds from the SAVED row not a default (fixes stale-init→reload bug). New local ProviderCard (variant remote|local): controlled baseUrl/model/apiKeyMode; Save→saveProviderProfile(db,row) + toast (preserves encryptedSecretId for Phase 2.3); Test→resolveProvider(id,appConfig,{baseUrl,model}) then checkHealth, on 'connected' dispatch activeProviderSet({id,label,model,baseUrl}) + setActiveProviderId(db,id). Provider Health aside now uses REAL profile ids (was buggy label.toLowerCase().replace(/[^a-z]/g,'') → 'openaicompatible'/'llamaserver' mismatches; Phase 9.3 "fix health ID mapping" effectively done here).
- main.tsx: getProvider now passes {baseUrl:activeProviderBaseUrl, model:activeProviderModel} from providers state → RUNTIME calls use persisted values. Module-level default-mock block replaced by async restoreActiveProvider(): getActiveProviderProfile(db) → if found dispatch activeProviderSet(with model/baseUrl) [restores active provider + status bar after reload]; else fall back to defaultActiveProviderId mock (demo/dev). void restoreActiveProvider().
- Tests: NEW providerProfiles.test (CRUD upsert/delete, merge order+fallback, active id persist+template fallback+saved override). providers.test +1 (resolveProvider 'compatible' with overrides → checkHealth hits saved baseUrl via stubbed global fetch). ModelsScreen.test REWRITE (now imports fake-indexeddb/auto + db singleton; findBy for async cards; +3: edited base URL persists to IDB, edited model persists, Test uses edited base URL not default). providersSlice.test updated for new init fields + model/baseUrl in set/clear. Commit 24ba361→(this).
- Gate GREEN: typecheck, lint, 206 vitest (+7), 26 e2e both browsers. No Rust/WASM change.
- NOTE: provider 'compatible' now resolvable + counts as configured; chat won't block it even with empty baseUrl (health test reveals unreachable) — acceptable/honest. 'session'/'encrypted' apiKeyMode persisted but NO key retrieval yet (Phase 2.3).
- NEXT (Pass 10): Phase 2.3 SecretVault provider wiring (security-critical: secret refs, session/encrypted key UI on ProviderCard, retrieve key from in-memory SecretVault in BOTH test + runtime LLM flows, secret_locked/secret_missing errors, durable secret audits deferred from Pass 6; security regression tests: raw key NOT in Redux/audit/localStorage). Also pending: Phase 1.2 effect-ports-fatal (needs Phase 5.1 storage handlers first), Phase 5.1 storage handlers, Phase 5.3 memory filters/provenance, Phase 6 backup hardening, Phase 7 skills hardening, Phase 8 wllama/model manager, Phase 9/10 UI persistence/honesty, Phase 11 QA.

## 2026-06-12T21:54:15Z - Claude Opus 4.8 - Hardening Pass 10 (Phase 2.3 core): wire SecretVault into provider calls (test + runtime) + secret_locked/secret_missing + leak regression
- WHY: SecretVault (secretVault.ts) + vaultWiring (Dexie store + Redux observer) were fully built+tested but NEVER instantiated anywhere — no singleton, no UI, and llmRunner called `complete({messages})` with NO apiKey at all. So real remote providers (OpenAI/Anthropic) couldn't authenticate. TODO 2.3.
- NEW `src/secrets/vault.ts`: app-wide `secretVault` singleton = new SecretVault({ store: createDexieVaultStore(db), observer: createReduxVaultObserver(store.dispatch) }). The ONLY place decrypted keys live. Starts LOCKED (no unlock UI yet → remote providers fail closed with secret_locked; honest).
- NEW `src/providers/providerKey.ts`: `resolveApiKey(source: KeySource, profile|null): ApiKeyResolution`. KeySource = {isUnlocked, getSecret} (SecretVault satisfies it; lets tests use a fake). Logic: apiKeyMode 'none' OR null profile → {ok:true} (no key; mock+local+mock work). vault locked → {ok:false, kind:'secret_locked'}. unlocked + getSecret(providerSecretId(id)) undefined/'' → 'secret_missing'. else {ok:true, apiKey}. `providerSecretId(id) = 'provider:'+id` (deterministic vault id per provider).
- llmRunner.ts: deps gained OPTIONAL `getApiKey?: () => Promise<ApiKeyResolution>` (absent → {ok:true} no key, so old tests/mock path unchanged). Before complete: resolve key; !ok → chatErrored({kind,message}) + recordAudit('provider.secret_unavailable', source 'provider', medium/failure, conversationId) + resolve_effect {ok:false, error:{kind,message}} + RETURN (provider NEVER called, no message written). ok → complete({messages}, apiKey!==undefined ? {apiKey} : undefined).
- main.tsx: llmRunner deps getApiKey = () => getActiveProviderProfile(db).then(p => resolveApiKey(secretVault, p)). Imports resolveApiKey + secretVault.
- ModelsScreen.tsx handleTest: after resolveProvider, resolveApiKey(secretVault, buildRow()) best-effort → pass apiKey to checkHealth if available (locked vault → unauthenticated health check still meaningful; 401=reachable-needs-auth). Imports secretVault + resolveApiKey.
- Tests: providerKey.test (none/null→ok, locked→secret_locked, missing→secret_missing, present→key). llmRunner.test +2 (secret_locked blocks: complete NOT called, no assistant msg, chat.error.kind secret_locked, audit provider.secret_unavailable, resolve_effect failure; AND apiKey passed: complete called with {apiKey:'sk-runtime'}). NEW secretLeak.test (SECURITY REGRESSION TODO 11.2): real SecretVault+InMemoryVaultStore+createReduxVaultObserver(test store), setup+putEncryptedSecret(RAW_KEY), resolveApiKey returns it, but JSON.stringify(store.getState()) does NOT contain RAW_KEY, secrets slice has metadata-only (no `value`), no audit event contains RAW_KEY.
- Gate GREEN: typecheck, lint, 213 vitest (+7), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED to Pass 11: the secrets/security SCREEN UI (enter session-only key / encrypted key / delete / lock / unlock), "secret prompts must not leak", "deleting key prevents future call" test (needs delete UI), and DURABLE secret audits (vault observer still uses live-tail auditAppended via createReduxVaultObserver, NOT recordAudit — upgrade when wiring the screen, pass db in). Without the unlock UI, remote providers always return secret_locked (correct fail-closed); mock/local unaffected.
- NEXT (Pass 11): secrets/security screen — unlock/setup/lock vault, per-provider key entry (session vs encrypted) writing to secretVault under providerSecretId(id), delete key, + upgrade vault observer to durable recordAudit (secret unlocked/locked/deleted audits, TODO 3.3). Then remaining: Phase 5.1 storage handlers → unblocks 1.2 effect-ports-fatal; Phase 5.3 memory filters/provenance; Phase 6 backup hardening; Phase 7 skills hardening; Phase 8 wllama/model manager; Phase 9/10 onboarding/settings/models UI persistence + honesty; Phase 11 QA/integration/security-regression sweep.

## 2026-06-12T22:00:20Z - Claude Opus 4.8 - Hardening Pass 11 (Phase 7.2): skill filesystem path safety + reserved system-state keys
- WHY (chosen over 2.3-UI and 5.1): self-contained pure-logic security task, no routes/store-coupling/WASM. Old isPathAllowed only checked `path.includes('..')` and STRIPPED leading '/' (so absolute paths were normalized-and-allowed); skill_state had NO key protection, so a skill could fs.setState('__permissions__', …) and OVERWRITE its own permissions row (escalation). [NOTE: 5.1 storage handlers deferred — runtime's storage_put(store:'messages') lacks conversationId + double-writes vs llmRunner; needs a claw-schema/WASM contract decision first, NOT a clean bounded task. 2.3 secrets UI deferred — no /security route in canonical nav; inline-on-Models couples to the vault singleton's app-store-bound observer, needs care.]
- skillFs.ts: NEW `isPathSafe(path)` — rejects empty, null byte (\0 + %00), backslash (\\), absolute (startsWith '/'), '.'/'..' segments, AND malformed/percent-encoded traversal: decodeURIComponent in try/catch (malformed %ZZ → reject), then checks BOTH raw + decoded forms for the same escapes (so %2e%2e / %2e%2e%2f can't slip past). isPathAllowed now = isPathSafe(path) && namespace-prefix match (no more leading-slash stripping → absolute rejected, not normalized-in).
- NEW `RESERVED_STATE_KEYS` = [__permissions__,__manifest__,__system__,__audit__,__secrets__]; `isReservedStateKey(key)` = key.startsWith('__') (whole `__…__` namespace reserved → future system keys safe by default). SkillFs.getState AND setState now throw `…reserved…` for any '__'-prefixed key. So skill can't read __secrets__/__system__ nor overwrite __permissions__. Manager still reads/writes __permissions__ via RAW db.skill_state (createSkillManager.fsFor / install), bypassing the SkillFs guard — only the skill-facing API is gated. "Move permissions out of mutable skill state" satisfied via reserved-row protection (perms still physically in skill_state['__permissions__'] but unreachable by the skill).
- skillFs.test REWRITE: kept original isPathAllowed cases; added isPathSafe matrix (8 escape techniques incl. encoded/null/backslash/malformed), isReservedStateKey, and SkillFs runtime-enforcement (traversal denied on read+write, in-namespace round-trip + state, reserved read/write throws + __permissions__ row untouched after rejected write). Existing skillManager.test fs.setState('count'…) unaffected (non-reserved).
- Gate GREEN: typecheck, lint, 219 vitest (+6), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED in 7.2: "package files read-only" (needs a package-file flag on SkillFileRow + writeText guard) — left unchecked.
- NEXT candidates (pick a self-contained one): Phase 7.1 strict skill import (validate SKILL.md frontmatter name/desc/version, reject unknown permissions, imported skills disabled-by-default [already enabled:false in install], permission review before install) — pure validation, see parseSkill.ts/skillManager. Phase 7.3 install/reinstall (stale files removed, enable/disable fails if skill missing, audit only true success). Phase 7.4 runtime permission enforcement (skill can't call undeclared tool/path — tie tool_call to permissions.tools). Phase 6.1 backup validation (manifest schema, allowlist, row validators, size/count limits, reject raw-secret-looking) — check for existing backup service first. Still pending big: 2.3 secrets UI (+durable secret audits), 5.1 storage handlers (needs contract decision)→1.2 effect-ports-fatal, 5.3 memory filters/provenance, 8 wllama/model-manager, 9/10 onboarding/settings UI persistence + honesty, 11 QA/integration/security sweep.

## 2026-06-12T22:04:46Z - Claude Opus 4.8 - Hardening Pass 12 (Phase 7.1): strict skill import validation, enforced at install
- WHY: parseSkillMd INVENTED defaults for missing fields (name → 'untitled-skill', version → '0.0.0'), so a nameless/unversioned skill installed silently. No permission-field allowlist. TODO 7.1.
- parseSkill.ts: removed sentinel defaults — parseSkillMd now leaves name/version EMPTY ('') when absent (parsing is lenient, validation is the gate). parseClawskill version default '0.0.0'→'' too (still throws if manifest.name missing).
- NEW src/skills/validateSkill.ts: `validateSkillImport(parsed): {ok:true}|{ok:false,errors[]}`. Rules: name required + NAME_PATTERN /^[a-z0-9][a-z0-9._-]{0,63}$/; version required + VERSION_PATTERN /^\d+\.\d+\.\d+([-.][0-9A-Za-z-]+)*$/; description required (non-blank); permission keys must be ⊆ {tools,read,write,network} (rejects unknown fields — clawskill spreads arbitrary JSON, so checked at runtime via Object.keys; cast manifest.permissions `as unknown as Record<string,unknown>` for exactOptionalPropertyTypes).
- skillManager.install: ENFORCES — runs validateSkillImport first; !ok → recordAudit('skill_import_failed', source 'skill', medium/FAILURE) + throw `Invalid skill: …` BEFORE persisting anything. (Defense-in-depth: even if a future import UI forgets to validate, install won't persist a bad skill.) Imported skills already enabled:false (disabled-by-default).
- Tests: validateSkill.test (valid passes; missing/invalid name; blank description; missing+non-semver version; unknown permission). parseSkill.test: "falls back to defaults" → "leaves required fields empty" (name '' , version ''). skillManager.test: added 'description: Summarizes PDFs' to SKILL_MD fixture (else install now rejects it!) + NEW test (install bad skill → throws /Invalid/, persists nothing, audits skill_import_failed).
- Gate GREEN: typecheck, lint, 226 vitest (+7), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED 7.1: "show permission review before install" (needs skills import UI). 
- THIS WINDOW did Passes 9,10,11,12 (Phase 2.1, 2.3-core, 7.2, 7.1). Commits: 68a5cd3, 3ec95b2, 4daff41, + this. Context getting deep — RECOMMEND fresh context for next iteration.
- NEXT candidates (self-contained first): Phase 7.3 skill install/reinstall/enable (reinstall removes stale package files; enable/disable THROWS if skill missing — currently db.skills.update(id) silently no-ops on missing; audit only true success/failure). Phase 7.4 runtime permission enforcement (tool_call must be in permissions.tools — tie effectExecutor tool_call_proposal / approval to the skill's declared tools; skill can't call undeclared tool/read/write — partly done for fs). Phase 6.1 backup validation (CHECK for existing backup service first: grep backup/export/import). Then: 2.3 secrets UI (+durable secret audits via observer w/ db), 5.1 storage handlers (needs claw-schema/WASM contract decision)→1.2 effect-ports-fatal, 5.3 memory filters/provenance, 8 wllama/model-manager honesty, 9/10 onboarding/settings/models UI persistence + honesty, 11 QA/integration/security sweep.

## 2026-06-12T22:12:21Z - Claude Opus 4.8 - Hardening Pass 13 (Phase 7.3): skill install/reinstall/enable honesty
- WHY: install() upserted skills.put + skill_files.bulkPut but NEVER deleted prior package files → a file dropped from a new package version lingered (stale-file read risk). setEnabled used db.skills.update(id) which silently no-ops (returns 0) on a missing id → a stale UI could "enable a phantom skill" and we'd audit success for a skill that doesn't exist. TODO 7.3.
- skillManager.install: now reads existing = db.skills.get(id) → isReinstall. On reinstall: db.skill_files.where('skillId').equals(id).delete() BEFORE bulkPut (stale files gone). enabled now = existing?.enabled ?? false (reinstall preserves prior enabled state; fresh install disabled). __permissions__ ALWAYS refreshed from new manifest. NEW 3rd arg `options: InstallOptions = { clearState? }`: default PRESERVES skill_state (version bump keeps user data); clearState:true wipes all skill_state for the id (then re-puts permissions). Audit event is `skill_reinstalled` vs `skill_installed` accordingly (both success, +skillId).
- skillManager.setEnabled: db.skills.update returns affected-row count; if 0 → audit `skill_enable_failed`/`skill_disable_failed` (source skill, risk MEDIUM, status FAILURE, +skillId) + THROW `Cannot {enable|disable} skill: <id> is not installed`. Only a real toggle audits `skill_enabled`/`skill_disabled` (success). uninstall audit also gained skillId.
- audit() helper extended: 4th opt arg { risk?, status?, skillId? } (was hardcoded info/success). Default still info/success so existing callers unchanged.
- Tests (skillManager.test, +4 → 230 total): stale files removed on reinstall (old.txt gone, keep.txt kept, version 2.0.0, audits skill_reinstalled); state preserved by default + cleared on { clearState:true }; enabling missing 'ghost' rejects /not installed/ + audits skill_enable_failed, NOT skill_enabled; same for disable. NOTE shared db singleton across the file's tests — test 3 (enable/disable/uninstall) now reinstalls summarize-pdf from test 1 but only asserts enabled flag, unaffected.
- Gate GREEN: typecheck, lint, 230 vitest (+4), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED 7.3: UI surfacing of the preserve-vs-clear-state choice — install() takes the flag now, but no reinstall flow/UI exists to prompt the user; checkbox ticked with an HTML-comment caveat in the TODO.
- NEXT candidates (self-contained first): Phase 7.4 runtime permission enforcement (tool_call must be ⊆ permissions.tools — tie effectExecutor's tool_call_proposal/approval to the active skill's declared tools; permission denial audited). Phase 6.1 backup validation (CHECK first: grep backup/export/import for an existing service). Then larger: 2.3 secrets UI (+durable secret audits via observer w/ db), 5.1 storage handlers (needs claw-schema/WASM contract decision)→1.2 effect-ports-fatal, 5.3 memory filters/provenance, 8 wllama/model-manager honesty, 9/10 onboarding/settings/models UI persistence + honesty, 11 QA/integration/security sweep.
- THIS WINDOW (post-compact) did Pass 13 only so far.

## 2026-06-12T22:17:08Z - Claude Opus 4.8 - Hardening Pass 14 (Phase 7.4 partial): runtime skill fs/state permission enforcement
- WHY: ctx.ports.skill was an unwired NO-OP (effectExecutor routes skill_fs_read_text/skill_state_get/skill_state_put to ctx.ports?.skill?.() which was undefined). So runtime-emitted skill fs/state effects got ZERO enforcement and never resolved. SkillFs already enforces namespace/reserved-key rules (Pass 11) but nothing on the runtime path used it. TODO 7.4 ("do not rely only on UI display").
- NEW src/runtime/skillRunner.ts: createSkillEffectHandler({db,dispatch,fsFor,submit}) — mirrors llmRunner pattern. (1) fail-closed guard: db.skills.get(skill_id) must exist AND be enabled, else deny. (2) routes through fsFor(skill_id)→SkillFs: readText/getState/setState (which THROW on out-of-namespace path or reserved __-key). (3) on success → submit resolve_effect {ok:true, content|value|∅}; on SkillFs throw or guard fail → recordAudit('skill.permission_denied', source skill, MEDIUM/FAILURE, skillId) + resolve_effect {ok:false, error:{kind:'permission_denied', message}}. Runtime always learns the real outcome; never proceeds as if access succeeded.
- main.tsx: wired ctx.ports.skill = createSkillEffectHandler({ db, dispatch, fsFor: (id)=>createSkillManager({db,dispatch}).fsFor(id), submit: host.submit }). (Imports createSkillEffectHandler + createSkillManager.)
- Tests (NEW skillRunner.test.ts, +6 → 236 total): in-namespace read served (seed data/** file directly, ok:true content); out-of-namespace read denied + audited; reserved __permissions__ write denied + audited + real perms row UNTOUCHED (still network:false); ordinary state put/get round-trip; disabled skill denied; unknown skill denied. Uses fake-indexeddb + db.clear() in beforeEach.
- Gate GREEN: typecheck, lint, 236 vitest (+6), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED 7.4 (documented in TODO with HTML comments): "Enforce skill permissions during TOOL execution" + "skill cannot call undeclared tool" test → BLOCKED: tool_call_proposal effect (effectTypes.ts) carries NO skill_id, so the runtime tool path can't be tied to a skill's declared permissions.tools yet. Needs a claw-schema/WASM contract change to add skill_id to tool_call_proposal (same class of gap as 5.1 storage handlers' missing conversationId). Left those two boxes unchecked.
- NEXT candidates (self-contained first): Phase 6.1 backup validation — FIRST grep for an existing backup/export/import service (audit CSV export exists in auditService.ts; check for a full-DB backup). Phase 5.3 memory filters/provenance. Then larger/blocked: 5.1 storage handlers + 7.4-tool + need a WASM/claw-schema contract decision (add conversationId to storage_put, skill_id to tool_call_proposal) → unblocks 1.2 effect-ports-fatal. 2.3 secrets UI (+durable secret audits via observer w/ db). 8 wllama/model-manager honesty. 9/10 onboarding/settings/models UI persistence + honesty. 11 QA/integration/security sweep.
- THIS WINDOW (post-compact) did Passes 13, 14. Commits: e335489, + this.

## 2026-06-12T22:22:15Z - Claude Opus 4.8 - Hardening Pass 15 (Phase 6.1): backup import validation
- WHY: src/backup/backupService.ts already existed (export/serialize/parse/import/recordBackupHistory + a WEAK validateBackup that only checked manifest.format + collections-is-object). No allowlist, no row validators, no version/count/size limits, no raw-secret guard → a hostile/foreign .clawbackup could write unknown tables, malformed rows, or smuggle plaintext keys on import. TODO 6.1.
- validateBackup REWRITE (same signature + new optional 2nd arg `limits: Partial<BackupLimits>`, defaults DEFAULT_BACKUP_LIMITS exported; StorageScreen's 1-arg call unaffected). New checks, all return {valid:false,error}: (1) schemaVersion must be finite number AND ≤ DB_VERSION (reject NEWER — can't import a future schema; older OK, Dexie migrates). (2) collections must be a non-array object. (3) per collection: name ∈ ALLOWED_COLLECTIONS (COLLECTIONS + 'encrypted_secrets') else reject; must be an array; ≤ maxRowsPerCollection. (4) per row: must be plain object (isPlainRow) with all KEY_FIELDS present as non-empty strings (app_settings[key], skill_files[skillId,path], skill_state[skillId,key], else [id]); JSON size ≤ maxRowBytes; running totalBytes ≤ maxTotalBytes; running totalRows ≤ maxTotalRows.
- NEW exported `containsLikelyRawSecret(value)` — recursive: flags a prop whose NORMALIZED name (lowercase, strip non-alnum) ∈ PLAINTEXT_SECRET_FIELDS {apikey,accesstoken,refreshtoken,clientsecret,secretkey,privatekey,password,passwd,plaintext} with non-empty string value, OR any string VALUE matching SECRET_VALUE_PATTERNS (sk-ant-, sk-, xox[baprs]-, AKIA…, ghp_…, ya29.…, JWT eyJ…). CRUCIAL false-positive avoidance: apiKeyMode (enum) and encryptedSecretId (reference id) normalize to 'apikeymode'/'encryptedsecretid' — NOT in the set; encrypted_secrets {ciphertext,iv,label,storageMode,id} has no secret-named field and base64 ciphertext doesn't match value patterns → legit rows accepted. DEFAULT limits generous (200k rows/coll, 1M total, 2MB/row, 200MB total) so real backups pass; tests pass tiny limits to exercise the caps.
- Tests (backupService.test.ts, +12 → 248 total): unknown collection; non-array collection; missing key-field row; non-object row; newer schemaVersion (9999); missing schemaVersion; row-count limit (maxRowsPerCollection:2); total-size limit (maxTotalBytes:10); raw secret by field name (apiKey); credential-shaped value anywhere (sk-ant-… inside memories.text); LEGIT provider profile w/ apiKeyMode+encryptedSecretId ACCEPTED (false-positive guard); ciphertext-only encrypted_secrets ACCEPTED. Existing export/round-trip tests still green (manifest.schemaVersion=DB_VERSION=4 ≤ 4).
- Gate GREEN: typecheck, lint, 248 vitest (+12), 26 e2e both browsers (StorageScreen import path exercised). No Rust/WASM change.
- NOT touched (later phases): 6.2 transactional import (preview/confirm/merge-replace/rollback — importBackup still non-transactional, no preview-conflict UI), 6.3 export accuracy (record history only after download succeeds; audit export failure). 6.1's validateBackup is the gate they build on.
- NEXT candidates (self-contained first): 6.2 transactional import (wrap importBackup in db.transaction, roll back on failure; merge vs replace already partially there) OR 6.3 export-accuracy (recordBackupHistory only after createObjectURL/download succeeds + audit) — check StorageScreen.tsx flow. Then 5.3 memory filters/provenance. Blocked-on-contract: 5.1 storage handlers + 7.4-tool (need claw-schema/WASM: conversationId on storage_put, skill_id on tool_call_proposal) → unblocks 1.2. Bigger: 2.3 secrets UI, 8 wllama/model-manager honesty, 9/10 onboarding/settings/models UI honesty, 11 QA sweep.
- THIS WINDOW (post-compact) did Passes 13,14,15. Commits: e335489, 2ca7417, + this.

## 2026-06-12T22:26:29Z - Claude Opus 4.8 - Hardening Pass 16 (Phase 6.3): honest backup export
- WHY: StorageScreen.handleExport recorded backup history (recordBackupHistory) BEFORE the download AND unconditionally toasted "Backup exported" even when URL.createObjectURL was absent (no file written). Violates the canonical "never believe BrowserClaw did X when it didn't" — Recent Backups would show a "Saved" entry for a backup that never downloaded. TODO 6.3.
- NEW backupService.runBackupExport({db, dispatch, download:(filename,json)=>void [MUST throw on failure], includeSecrets?, now?}): builds+serializes, computes sizeBytes (Blob), calls deps.download; on THROW → recordAudit('backup.export_failed', source 'backup', low/FAILURE) + return {ok:false,error} and record NOTHING; on success → recordBackupHistory THEN recordAudit('backup.exported', source 'backup', plaintext?low:medium / success, summary notes "plaintext" vs "includes encrypted secrets") + return {ok:true,sizeBytes}. backupService now imports recordAudit + AppDispatch.
- StorageScreen.handleExport REWRITE: calls runBackupExport with download that THROWS 'Downloads are not supported in this browser.' when createObjectURL missing (was a silent if-guard that skipped download but still toasted success). ok→success toast; !ok→danger toast w/ error. Removed now-unused exportBackup/serializeBackup/recordBackupHistory imports. Added a text-warning line under the Export/Import buttons: "Exported backups are unencrypted plaintext JSON (any included secrets stay encrypted). Store the file somewhere safe." (labels plaintext clearly).
- Tests (backupService.test.ts, +2 → 250 total): success path records 1 backup_history row + audits backup.exported (inject no-op download); failure path (download throws) → ok:false, /not supported/, backup_history count 0, audits backup.export_failed and NOT backup.exported. Uses configureStore({audit}) + dispatch cast as AppDispatch.
- Gate GREEN: typecheck, lint, 250 vitest (+2), 26 e2e both browsers. No Rust/WASM change.
- DEFERRED 6.3: "optional encrypted backup export" + "encrypted export can be imported" — current export is LABELED plaintext (not claiming encryption), so no false claim; a real encrypted .clawbackup needs a passphrase-KDF flow = separate task. Left unchecked w/ TODO comments.
- NEXT candidates (self-contained first): 6.2 transactional import (wrap importBackup in db.transaction('rw', tables, …) so a mid-import failure rolls back; add preview conflict counts + explicit confirm — Dialog already confirms; merge/replace strategies already in importBackup) — assert failed import leaves DB unchanged. Then 5.3 memory filters/provenance. Blocked-on-contract (need claw-schema/WASM): 5.1 storage handlers (conversationId on storage_put), 7.4-tool (skill_id on tool_call_proposal) → unblock 1.2 effect-ports-fatal. Bigger UI/honesty: 2.3 secrets UI, 8 wllama/model-manager, 9/10 onboarding/settings/models honesty, 11 QA sweep.
- THIS WINDOW (post-compact) did Passes 13,14,15,16. Commits: e335489, 2ca7417, e0aa7a9, + this. Context getting deep again — fresh context advisable after this pass.

## 2026-06-12T22:29:45Z - Claude Opus 4.8 - Hardening Pass 17 (Phase 6.2): transactional backup import
- WHY: importBackup looped per-collection with separate await table.clear()/bulkPut() — a failure mid-import (e.g. a bad row in collection N) left collections 1..N-1 already written = a half-restored DB. TODO 6.2 ("run import in transaction; roll back on failure").
- importBackup REWRITE: derive names = Object.keys(collections).filter(Array.isArray); resolve `tables = names.map(db.table)` UP FRONT (unknown collection throws before any write); wrap the whole loop in `db.transaction('rw', tables, async () => { for name: if replace→clear, then bulkPut(rows) })`. Dexie auto-rolls-back the transaction if any write rejects → all-or-nothing. replace still clears ONLY collections present in the backup (others untouched). Same signature (db, backup, strategy='merge').
- StorageScreen restore Dialog: added a line under the summary — "This backup includes encrypted secrets (ciphertext only)." when summary.encrypted_secrets>0, else "This backup contains no secrets." (6.2 "show encrypted secrets present or absent").
- Tests (backupService.test.ts, +3 → 253 total): (1) rollback — backup with memories[{id:'keep'}] THEN skill_state[{bogus:true}] (missing [skillId+key] compound key → bulkPut rejects); importBackup rejects AND memories.get('keep') is undefined (earlier write rolled back). (2) merge preserves a pre-seeded {id:'existing'} alongside imported {id:'fresh'}. (3) replace with only memories clears memories' old row + adds new, but a pre-seeded conversations {id:'c-keep'} (NOT in backup) survives. Local `backup()` helper + ClawBackup type import.
- Gate GREEN: typecheck, lint, 253 vitest (+3), 26 e2e both browsers. No Rust/WASM change. (Confirms Dexie fake-indexeddb honors transaction rollback.)
- DEFERRED 6.2 (TODO comments): per-record CONFLICT counts in preview (needs reading existing keys per collection) + dedicated MODEL REFERENCES callout (model_catalog/model_cache_index counts already show in summary). Preview shows collections+counts+secrets-presence + explicit confirm today.
- PHASE 6 (Backup/Restore) NOW COMPLETE except the two deferred preview-detail bullets + 6.3 encrypted-export. 6.1+6.2+6.3 cores all landed (Passes 15,16,17).
- NEXT candidates (self-contained first): 5.3 memory filters/provenance (check MemoriesScreen + memories slice/service for source/provenance display + filter honesty). 5.2 already done (v4 seed cleanup). Blocked-on-contract (need claw-schema/WASM contract change): 5.1 storage handlers (conversationId on storage_put), 7.4-tool (skill_id on tool_call_proposal) → unblock 1.2 effect-ports-fatal. Bigger UI/honesty: 2.3 secrets UI (+durable secret audits), 8 wllama/model-manager honesty, 9/10 onboarding/settings/models persistence+honesty, 11 QA/integration/security sweep.
- THIS WINDOW (post-compact) did Passes 13,14,15,16,17 (Phase 7.3, 7.4-partial, 6.1, 6.3, 6.2). Commits: e335489,2ca7417,e0aa7a9,ab3c1ad,+this. Context DEEP — RECOMMEND fresh context (/compact) before Pass 18.

## 2026-06-12T22:55:01Z - Claude Opus 4.8 - Pass 18: Phase 5.3 memory filters (real, wired)
- **Task:** make the Memories screen's filters honest. The old "Sensitivity" `<Select>` was a DECORATIVE NO-OP (`defaultValue="all"`, no `onChange`, not bound to anything) — a control that pretended to filter. Replaced with real, wired filters.
- New pure module `src/memories/filterMemories.ts`: `filterMemories(rows, MemoryFilters)` (query+sensitivity+source+createdBy+tag+pinnedOnly, AND semantics) + `deriveMemoryFacets(rows)` (distinct sorted sources/creators/tags for the dropdowns) + `DEFAULT_MEMORY_FILTERS`.
- `memoriesSlice` reworked: dropped the unused `filterTags`; added `sensitivityFilter`/`sourceFilter`/`createdByFilter`/`tagFilter`/`pinnedOnly` + setters + `filtersCleared` (keeps selection). `searchQuery`/`selectedMemoryId` unchanged.
- `MemoriesScreen`: search + Sensitivity/Source/Created-by/Tag selects (options derived from the live record set) + "Pinned only" checkbox + "Clear filters" (shown only when `filtersActive`). Empty-state copy now distinguishes "no memories match the current filters" vs "no memories yet".
- **DEFERRED (documented in TODO with caveats):** provenance persistence (conversation/message/skill-source IDs) and real retrieval-history. Both BLOCKED — no memory-WRITE or memory-RETRIEVAL pipeline exists yet (no runtime/effect path creates or reads memories). `lastUsedAt` is never written by real code (grep-confirmed: only demo seeds set it), so "Recently used"/"Retrieval history" panels are correctly EMPTY in a normal build and only show seed data in demo mode — honest-when-empty. These need a claw-schema/WASM memory effect first (same class of gap as 5.1 storage handlers / 7.4 tool-execution).
- Tests +12: `filterMemories.test.ts` (NEW, 9 — per-filter + AND + facets), `memoriesSlice.test.ts` (rewritten for new shape + filtersCleared), `MemoriesScreen.test.tsx` (+1 — wired created-by filter actually hides rows, Clear restores).
- Gate GREEN: typecheck clean, lint clean (`--max-warnings 0`), **265 vitest** (253→265), **26 e2e** both browsers. Commit/push next.
- NEXT candidates (self-contained first): **2.3 secrets UI** (durable secret audits + honest empty/locked states), **Phase 8** (wllama/model-manager honesty — real download/cache state vs claimed), **9/10** (onboarding/settings/models persistence + honesty), **11** (QA/integration/security sweep). Contract-blocked cluster (needs claw-schema/WASM): 5.1 storage handlers + 7.4 tool-execution → unblock 1.2 effect-ports-fatal.

## 2026-06-12T22:59:26Z - Claude Opus 4.8 - Pass 19: Phase 5.2 close-out (TODO checkboxes lag code!)
- **Discovery:** the HARDENING_TODO checkboxes are STALE — several phases are already implemented in code but still show `[ ]`. Verified via an Explore subagent reading source (not docs).
- **5.2 (Remove Fake Memory Seeding): behaviorally COMPLETE already.** Seeding gated by `appConfig.isDemoMode` (MemoriesScreen.tsx ~L50), default false (only `VITE_DEMO_MODE=true` enables). One-time **v4 migration** (db.ts ~L108) deletes only UNTOUCHED prior seeds via `isUnmodifiedSampleMemory()` (matches id/title/createdBy/source; preserves user edits). SAMPLE_MEMORIES all carry `demo:true` + show a "Demo" Badge. Only gap was a missing guardrail TEST.
- **This pass:** added `MemoriesScreen.test.tsx` test "a fresh non-demo DB shows the empty state and seeds nothing" (asserts empty-state text + `db.memories.count()===0` after render + no sample button). Ticked all 5.2 boxes with file-ref caveats. Tests 265→266.
- **2.4 (Provider Error Handling): ALSO already COMPLETE** (verified, NOT yet ticked — will reconcile next pass). Evidence: `llmRunner.ts` ~L80-99 catches provider errors, dispatches `chatErrored` (error CARD via ErrorState in ChatScreen ~L138), audits failure, resolves effect `{ok:false}` — NEVER writes a fake assistant message (test "surfaces a provider failure as an error, never a fake reply" asserts no assistant msg). Normalized `ProviderError {kind}` in providers/errors.ts: kinds = cors|auth|model_not_found|unreachable|unknown (incl. CORS classification). Tests in llmRunner.test.ts / chatSlice.test.ts / providers.test.ts.
- Gate GREEN: lint clean, **266 vitest**, **26 e2e** both browsers. Commit/push next.
- **IMPORTANT for next passes:** Don't trust TODO checkboxes — verify against code first (many phase 1-4 boxes may already be done from pre-hardening windows). NEXT: reconcile/verify 2.4 boxes; then check real status of phases 3 (audit), 4 (snapshot persistence) before assuming work is needed. Genuinely-unbuilt + self-contained UI honesty: **2.3 secrets UI** (session/encrypted key entry, lock/unlock/delete — Pass 11 placeholder), **Phase 8** wllama/model-manager. Contract-blocked: 5.1 storage + 7.4 tool → 1.2.

## 2026-06-13T00:24:43Z - Claude Opus 4.8 - Pass 20: Phase 2.3 SecretVault UI (lock lifecycle)
- **Context:** the SecretVault BACKEND was already fully built+tested in earlier passes (secretVault.ts class, crypto.ts PBKDF2/AES-GCM, encrypted_secrets Dexie table, secretsSlice metadata-only, vaultWiring redux/dexie bridge, providerKey resolution, audit redaction). The ONLY gap in 2.3 was the UI. Verified via Explore subagent.
- **This pass = the LOCK LIFECYCLE half of 2.3.** New `src/screens/SecurityScreen.tsx` + `/security` route (router.tsx SCREEN_OVERRIDES) + nav item (navItems.ts, ShieldCheck icon) + e2e visual route. Flows: first-run SETUP (passphrase ≥8 + confirm → `secretVault.setup`), UNLOCK (passphrase → `secretVault.unlock`; wrong → stays Locked, fail-closed), SESSION-ONLY (`openSession`, persists nothing), LOCK (`secretVault.lock`). Shows Locked/Unlocked Badge + secret COUNT (metadata only).
- **Added `SecretVault.isConfigured()`** (reads only salt presence, no plaintext) so the UI picks setup-vs-unlock. The singleton `secretVault` (vault.ts) is bound to the REAL app store + auto-dispatches lock state/metadata/audit via its observer — so the screen just calls vault methods and reads `state.secrets`.
- **TESTING wrinkle (important for Pass 21):** because the singleton is wired to the REAL store (store.ts), the screen test must render `<Provider store={store}>` (the real store), NOT a fresh configureStore — else vault observer dispatches never reach the component. beforeEach: `db.app_settings.clear()` + `encrypted_secrets.clear()` + `secretVault.lock()` to reset to pristine/unconfigured/locked. afterEach lock() clears the 15-min auto-lock timer.
- **SECURITY:** screen never calls getSecret, never logs/persists key text; only the user's vault PASSPHRASE is transient component state (cleared after use). No decrypted API key ever touches Redux/DOM/audit. (secretLeak.test.ts still green.)
- Gotchas fixed: Badge has no 'muted' tone (used 'neutral'); toast has no 'default' tone (used 'info').
- Tests +5: secretVault.test.ts (isConfigured unit), SecurityScreen.test.tsx (NEW: setup-unlocks, wrong-passphrase-stays-locked, session-only, lock). 266→271 vitest. e2e 26→28 (security visual route x2 browsers). Lint clean.
- **NEXT = Pass 21: 2.3 key CRUD** — add a key-entry form on SecurityScreen (label + key value + storage mode session|encrypted), wired to `setSessionSecret` / `putEncryptedSecret`, plus per-key DELETE (`removeSecret`). Provider id convention: `providerSecretId(id)='provider:${id}'` (providers/providerKey.ts). Then "deleting key prevents future provider call" test + "secret prompts must not leak key text". After that: reconcile 2.4 boxes (already complete — see Pass 19 note); then Phase 8 wllama/model-manager honesty.

## 2026-06-13T04:20:19Z - Claude Opus 4.8 - Pass 21: Phase 2.3 SecretVault key CRUD (provider-targeted) — 2.3 COMPLETE
- Completed the second half of 2.3 on SecurityScreen: per-provider API key entry + delete. **Phase 2.3 is now fully done.**
- **Design (provider-targeted, not generic):** "Provider keys" section (visible only when unlocked). Provider picker = `mergeProviderProfiles(persisted)` filtered to `apiKeyMode !== 'none'` (openai/anthropic/compatible; excludes local ollama/llama-server). No seeding needed — templates always populate the picker.
- **Add key:** stores the vault secret at `providerSecretId(id)='provider:${id}'` via `setSessionSecret` (session) or `putEncryptedSecret` (encrypted, only offered when `secretVault.canStoreEncrypted()`), THEN persists the profile's `apiKeyMode` (+`encryptedSecretId` for encrypted) so reloads + Models screen stay honest. Key input is `type=password`, `autoComplete=off`; cleared after save.
- **Delete key:** `removeSecret(secretId)` ONLY — deliberately leaves the profile's `apiKeyMode` untouched so a future provider call fails CLOSED with `secret_missing` (the acceptance criterion), not silently runs keyless. This is why delete does NOT reset apiKeyMode to 'none'.
- **Why it actually wires:** `resolveApiKey` (providerKey.ts) ignores `encryptedSecretId` and looks up `getSecret('provider:${profile.id}')` whenever `apiKeyMode !== 'none'`. So storing at that id + non-none mode = runtime finds the key.
- **SECURITY (critical rule held):** key text goes straight to vault methods; never to Redux/DOM/audit/profile row. New SecurityScreen test asserts the raw key string is absent from `document.body.textContent` AND from `JSON.stringify(store secrets metadata)`.
- Tests +2 (271→273): providerKey.test "deleting a key prevents future provider calls" (real SecretVault+InMemoryVaultStore: put→ok, remove→secret_missing); SecurityScreen.test "adds a session key... then deletes it" (+no-leak asserts). beforeEach now also clears `provider_profiles` and any leftover store secrets metadata (shared real store). Gate GREEN: lint, 273 vitest, 28 e2e both browsers.
- TODO 2.3 ALL boxes ticked (incl. "secret prompts must not leak key text", "deleting key prevents future provider call").
- **NEXT candidates:** reconcile **2.4** boxes (verified COMPLETE in Pass 19 — error cards/normalized ProviderError/CORS/tests all exist; just tick with evidence). Then genuinely-new self-contained: **Phase 8** wllama/model-manager honesty (8.1 runtime assets, 8.2 download manager real progress/cache vs claimed, 8.3 HF model support), **Phase 9** onboarding/settings/models persistence+honesty. Contract-blocked cluster still: 5.1 storage + 7.4 tool → 1.2 effect-ports-fatal (need claw-schema/WASM to add conversationId/skill_id to effects).

## 2026-06-13T05:51:49Z - Claude Opus 4.8 - Pass 22: Phase 2.4 CORS classification (completes 2.4)
- **Lesson reinforced (don't trust checkboxes):** my prior note said 2.4 was "verified COMPLETE in Pass 19, just tick boxes." An Explore re-verification THIS pass found that was WRONG — CORS classification was only PARTIAL: the `'cors'` kind existed as a type but NOTHING ever produced it (all network throws mapped to `'unreachable'`). Always re-verify against source before ticking.
- **What was genuinely missing & built:** `classifyFetchFailure(url)` in `src/providers/errors.ts` — a browser CORS reject and a true network failure both surface as a bare `TypeError`, so distinguish by ORIGIN: cross-origin throw → `'cors'` (likely CORS block), same-origin OR no `globalThis.location` (non-browser/tests) → `'unreachable'`. Threaded the request URL through `fetchOrThrow(url, run)`; updated both callers (openAiCompatible.ts, anthropic.ts) to compute `url` once and pass it.
- Softened the `cors` user message to be honest ("most likely a CORS restriction... may not allow direct browser requests") since classification is heuristic, not certain.
- **Test fallout (expected & correct):** the old "network failure → unreachable" tests used the default cross-origin OpenAI URL, so they now correctly classify as CORS. Updated providers.test ("maps a cross-origin network failure to a likely CORS block" → kind 'cors'/health 'cors_error') + added same-origin counterpart (→ 'unreachable') + direct `classifyFetchFailure` unit test. ModelsScreen.test "runs a real health check" now asserts `health.openai === 'cors_error'` (honest browser-direct result). Added ChatScreen.test "shows a provider error card, not a fake assistant reply" (the missing UI-level error-card test from 2.4's list).
- jsdom env => `globalThis.location.origin` is `http://localhost:3000`, so origin comparison works in unit tests.
- Tests +3 (273→276). Gate GREEN: typecheck, lint (--max-warnings 0), 276 vitest, 28 e2e both browsers. TODO 2.4 ALL boxes ticked with evidence comments. **Phase 2 (2.1–2.4) now fully complete except 2.2** (remove implicit mock provider — verify next; registry.ts already fails closed per existing tests, may just need reconciliation).
- **NEXT candidates:** verify/reconcile **2.2** (registry "fails closed" tests already exist — check chat-blocks-when-no-provider + setup CTA are wired, likely done). Then genuinely-new: **Phase 8** wllama/model-manager honesty, **Phase 9** onboarding/settings persistence. Contract-blocked still: 5.1 storage + 7.4 → 1.2 effect-ports-fatal (needs claw-schema/WASM conversationId/skill_id).

## 2026-06-13T05:59:52Z - Claude Opus 4.8 - Passes 23-25: reconcile Phase 2.2/0/1.1 + close 1.1 UI-mode test gap
- **Phase 2 is now fully complete (2.1-2.4).** Pass 22 closed 2.4 (CORS classification — see prior entry). Pass 23 reconciled 2.2 (registry.ts resolveProvider already fails closed: unknown->unknown_provider, null->not_configured, mock gated on isMockProviderAllowed; ChatScreen blocks with /models setup CTA) — doc-only, all evidenced by existing passing tests.
- **Pass 24: Phase 0 fully reconciled (doc-only).** Verified via Explore that ALL Phase 0 items already exist: HARDENING_NOTES.md (5 invariants), src/config/appConfig.ts (AppConfig { isDemoMode, isDevFallbackAllowed, isMockProviderAllowed, isSafetyOverrideActive }, parseAppConfig(import.meta.env), all default false, only exact 'true' flag enables), SafetyOverrideBanner.tsx in AppLayout, appConfig.test.ts fail-closed tests.
- **Pass 25: Phase 1.1 — found ONE genuine gap and filled it.** Explore confirmed 1.1 fully built (runtimeBoot.ts fails closed: WASM fail + dev flag off => {port:null,mode:null}, never createReference; main.tsx onLoaded/onFailed/onFallback dispatch runtimeLoaded/runtimeFailed + appendAudit 'runtime.load_failed'/'runtime.reference_fallback_used'; AppLayout renders RuntimeBlockedScreen when runtime.fatal; SettingsScreen Environment "Runtime" row shows `${status} (${mode})`). The ONLY missing piece was the Phase 1.1 test "UI displays the correct runtime mode." Added SettingsScreen.test "shows the actual runtime mode, not a hardcoded label" (dispatch runtimeLoaded wasm -> 'ready (wasm)', then reference -> 'ready (reference)' and asserts wasm label is gone). NOTE: post-render store.dispatch must be wrapped in act() or React doesn't flush + getByText fails.
- Tests +1 (276->277). Gate GREEN all three passes (typecheck, lint, 277 vitest, 28 e2e both browsers). Commits: 642124e (2.2), a92a38b (Phase 0), + this pass for 1.1.
- **Reconciliation pattern confirmed efficient:** checkboxes lag code badly; many "phases" are already built. Verify with an Explore against SOURCE (not just tests), tick with `<!-- evidence -->` comments, and only write code for the genuine gaps the Explore surfaces (2.4 CORS, 1.1 UI-mode test).
- **NEXT candidates:** Phase 1.2 (effect ports fatal) is CONTRACT-BLOCKED (needs claw-schema/WASM conversationId/skill_id). Phase 3 (Durable Audit Log) — recordAudit/auditSink/audit_events table exist; likely mostly done, verify+reconcile next. Then genuinely-new: Phase 8 (wllama/model-manager honesty), Phase 9 (onboarding/settings persistence). Also Phase 3.2 (remove fake audit seeding) parallels the 5.2 memory work already done.

## 2026-06-13T06:12:29Z - Claude Opus 4.8 - Pass 26: Phase 3 (Durable Audit Log) verify+reconcile; wire provider-test audit
- **Phase 3.1 verified FULLY COMPLETE (doc-only reconcile).** Explore + direct read confirmed src/audit/auditService.ts has the whole durable service: appendAuditEvent (durable-only), queryAuditEvents (type/risk/source/status/limit), getAuditEvent (line 157), exportAuditCsv/auditEventsToCsv (never emits `details`), redactDetails (strips credential-like keys at any depth, SECRET_KEY_PATTERN), AuditEventRow with source/status/risk (db/types.ts:146), Dexie v3 migration adds source/status indices + backfillAuditDefaults. src/audit/auditSink.ts recordAudit = Redux live-tail (auditSlice, 50-cap) + durable db.audit_events.put. AuditScreen reads the DURABLE table via useLiveQuery (not Redux). Tests already cover persist/query-by-type/query-by-risk/CSV (auditService.test.ts) + durability (effectExecutor.test.ts). Ticked all 3.1 boxes w/ evidence.
- **Phase 3.2 mostly already satisfied (built clean, NOT a removal).** There is NO fake audit seeding anywhere — db.ts populate() seeds ONLY app_settings.schemaVersion; AuditScreen shows "No audit events." empty state. AuditEventRow.demo flag exists + buildAuditRow honors it, but NO demo-seeding path is wired (intentionally unused). Ticked "remove fake seeding"/"replace w/ empty state"/"empty DB shows empty state test". LEFT UNTICKED (honest): "fake audit not inserted in default mode" (no explicit boot-empty assertion yet) + the demo-mode subitems (no demo seeding to mark).
- **Phase 3.3 — found + filled ONE genuine gap (the bounded task this pass): provider test success/failure was NOT audited.** ModelsScreen handleTest ran a real health check but emitted zero audit events (grep confirmed). Wired recordAudit in handleTest: 'provider.test_succeeded' (connected, risk info/status success) vs 'provider.test_failed' (any other health verdict OR unresolvable provider, risk low/status failure); summary carries provider id + verdict ONLY, never the key. Paired guardrail tests in ModelsScreen.test: success path asserts a durable row (type/source/status/providerId + no 'sk-' substring) and failure path (cross-origin TypeError -> provider.test_failed/status failure). afterEach now clears db.audit_events.
- Ticked verified-wired 3.3 items: runtime loaded/failed/fallback (main.tsx), provider test (this pass), LLM start/success/failure (referenceRuntime + llmRunner), "no raw secrets recorded" (redaction + guardrail). LEFT UNTICKED (partial/blocked): effect lifecycle (Phase 1.2 contract-blocked), tool events (7.4 contract-blocked), memory CRUD (only a test fixture, not app-wired), skill install/enable/disable (only import_failed + permission_denied wired), secret unlock/lock/delete (unwired), backup import (only export wired), model download/load/delete (Phase 8).
- Tests +2 (277->279). Gate GREEN: typecheck, lint --max-warnings 0, 279 vitest, 28 e2e both browsers. No Rust changes (cargo not needed).
- **NEXT candidates:** Phase 4 (runtime snapshot persistence — main.tsx already references runtime.snapshot_* audit events, so partly wired; verify+reconcile next). Genuinely-new self-contained: Phase 8 (wllama model-manager download honesty), Phase 9 (onboarding/settings/models persistence). Still contract-blocked: 1.2 effect-ports-fatal + 5.1 storage + 7.4 tool (need claw-schema/WASM conversationId/skill_id on effects). Incremental 3.3 wiring (skill lifecycle, secret events, backup import, model events) can ride along as Phase 8/9 land.

## 2026-06-13T06:20:32Z - Claude Opus 4.8 - Pass 27: Phase 4 snapshot compatibility check (version gate)
- **Bounded task: snapshot schema-version gate.** Before this pass, loadLatestSnapshot returned the raw snapshot regardless of version, and a bad snapshot stayed in IndexedDB to fail forever — a snapshot from an incompatible runtime could silently restore wrong state (anti-deception violation).
- **What I built:** `SNAPSHOT_SCHEMA_VERSION = 1` in src/runtime/referenceRuntime.ts (home of RuntimeSnapshot; NOT runtimeHost.ts — that would make a circular import since runtimeHost imports executeEffect from effectExecutor and effectExecutor needed the const). Both writers stamp it: RuntimeHost.saveSnapshot (id 'latest') + effectExecutor 'runtime_snapshot_save' handler. `RuntimeSnapshotRow.version?: number` added (optional so pre-versioning rows read back undefined). `loadLatestSnapshot` now returns a discriminated `SnapshotLoad = {status:'none'} | {status:'ok',snapshot} | {status:'incompatible',foundVersion}`; on version mismatch (incl. undefined/unversioned) it DELETEs the row and returns 'incompatible'. main.tsx bootRuntime handles the union: 'incompatible' -> appendAudit('runtime.snapshot_incompatible', ...) + start fresh; 'ok' -> restore.
- **Ripple:** changing loadLatestSnapshot's return type touched 3 call sites (main.tsx + 2 in runtimeHost.test.ts — updated to unwrap `.status==='ok' && .snapshot`). Added 2 guardrail tests: "discards an incompatible snapshot" (version+1 -> incompatible + row deleted) and "treats a pre-versioning snapshot as incompatible" (no version field -> incompatible + deleted). Pre-versioning rows being discarded on first upgrade is the CORRECT honest behavior (can't prove compatible -> don't trust).
- **Deliberately NOT done (noted in TODO):** per-effect/idle/error save triggers (blocked on effect-port contract, Phase 1.2); 'snapshot_saved' audit (would spam the log every turn); VISIBLE UI restore warning (incompatible/restore_failed are audited + console-logged but not surfaced as a toast/banner yet — this is the clear NEXT Phase-4 task, self-contained, needs a Redux signal + a banner/toast in AppLayout or the chat).
- Tests +2 (279->281). Gate GREEN: typecheck, lint --max-warnings 0, 281 vitest, 28 e2e both browsers. No Rust changes.
- **NEXT candidates:** Phase 4 "visible restore warning" (self-contained UI follow-up to this pass). Then genuinely-new: Phase 8 (wllama model-manager download honesty), Phase 9 (onboarding/settings/models persistence). Still contract-blocked: 1.2 effect-ports-fatal + 5.1 storage + 7.4 tool (need claw-schema/WASM conversationId/skill_id on effects), which also gates the per-effect snapshot triggers + effect-lifecycle audit (3.3).

## 2026-06-13T06:24:31Z - Claude Opus 4.8 - Pass 28: Phase 4 visible snapshot-restore warning (UI)
- **Bounded task: surface the snapshot restore problem to the user.** Pass 27 added the version gate + audit, but incompatible/restore_failed were only audited + console-logged — invisible to the user, who'd silently lose their previous session. Anti-deception: a dropped session must be VISIBLE.
- **What I built:** runtimeSlice gains `snapshotIssue: SnapshotRestoreIssue | null` ('incompatible'|'restore_failed') + actions snapshotRestoreWarned/snapshotWarningDismissed. CRITICAL ordering: main.tsx dispatches snapshotRestoreWarned at boot BEFORE loadRuntimePort fires onLoaded->runtimeLoaded; runtimeLoaded/runtimeReady deliberately DO NOT clear snapshotIssue (else the warning is wiped instantly). runtimeReset + explicit dismiss clear it. New SnapshotRestoreBanner.tsx (role=alert, dismissible X button, distinct message per kind) rendered in AppLayout right after SafetyOverrideBanner. main.tsx dispatches it in two spots: incompatible branch (next to the snapshot_incompatible audit) + withSnapshotRestore catch (next to snapshot_restore_failed audit).
- **Ripple:** adding required field `snapshotIssue` to RuntimeState broke 4 `toEqual(full-shape)` assertions in runtimeSlice.test.ts — added `snapshotIssue: null` to each. Added 2 slice tests (warned survives a later runtimeLoaded; dismiss + reset both clear) + 3 banner component tests (renders nothing when null; incompatible message + dismiss clears state & hides; restore_failed distinct message).
- Tests +5 (281->286: 3 banner + 2 slice). Gate GREEN: typecheck, lint --max-warnings 0, 286 vitest, 28 e2e both browsers. No Rust changes. Phase 4 "visible restore warning" box ticked; Phase 4 now COMPLETE except per-effect/idle/error save triggers (blocked on effect-port contract Phase 1.2) and the deliberately-omitted per-turn 'snapshot_saved' audit.
- **NEXT candidates:** Phase 4 is effectively done (remaining items contract-blocked/deliberately-omitted). Move to genuinely-new self-contained work: **Phase 8** (wllama model-manager download honesty — real progress/cache vs claimed; ModelsScreen already uses createModelManager + MODEL_CATALOG + isWllamaSupported/getWllamaEngine) or **Phase 9** (onboarding/settings/models persistence + honesty). Verify with an Explore against source first (checkboxes lag — many phases already built). Still contract-blocked: 1.2 effect-ports-fatal + 5.1 storage + 7.4 tool (need claw-schema/WASM conversationId/skill_id on effects).

## 2026-06-13T07:19:03Z - Claude Opus 4.8 - Pass 29: Phase 3.3 — audit model download/load/delete (Phase 8 honesty)
- **Bounded task: model operations were invisible to the audit log.** Two Explores (Phase 8 + Phase 9) confirmed against SOURCE that createModelManager (src/wllama/modelManager.ts) ran download/load/delete but emitted ZERO audit events — violating CLAUDE.md "every meaningful action emits an audit event". The audit schema was already ready: AuditSource includes 'model' and AppendAuditInput/AuditEventRow have a `modelId` field (db/types.ts).
- **What I built:** threaded `db` into createModelManager(db, dispatch, engine) and added a small `audit()` helper (void recordAudit, source:'model'). Six event types: model.download_succeeded/_failed, model.loaded/.load_failed, model.deleted/.delete_failed. load() and remove() (previously bare awaits) now wrap engine calls in try/catch to audit failure then rethrow. Summaries carry model.name + modelId ONLY — never the HF download URL or any credential. Updated the single call site (ModelsScreen.tsx useMemo) to pass db (already imported there).
- **Tests +3 (286->289):** wllama.test.ts — needed `import 'fake-indexeddb/auto'` at top (it runs under the default jsdom env which lacks indexedDB; db-backed tests like auditService.test.ts/ModelsScreen.test.tsx all do this). Updated all 3 existing createModelManager call sites to pass db; afterEach clears db.audit_events. New tests: successful-download audit (asserts durable row type/status/modelId + `JSON.stringify(event)` has no 'huggingface' leak), failed-download audit (status failure), load+delete audit (types contain model.loaded + model.deleted).
- Gate GREEN: typecheck, lint --max-warnings 0, 289 vitest, 28 e2e both browsers. No Rust changes. Ticked TODO 3.3 "model download/load/delete success/failure" w/ evidence; updated 3.3 status footer (model events now wired; remaining = effect/tool/memory/skill-lifecycle/secret/backup-import).
- **NOTE on Explores' larger findings (next candidates):** Phase 8 still has REAL gaps — no download cancel/pause, NO storage-quota preflight, wllama WASM auto-loads from jsdelivr CDN with NO integrity/consent gate (8.1 "stop silently loading from CDN"), no model audit on the WASM-runtime load itself, no user-HF-model form (8.3). Phase 9: OnboardingScreen + SettingsScreen EXIST but are Redux/local-state only — NO IndexedDB persistence (onboarding completion not saved/rehydrated, no routing guard; Settings toggles are decorative/stub, no app_settings writes). ModelsScreen persistence is already REAL (controlled form, saveProviderProfile, real health). app_settings keys written today: schemaVersion, vaultSalt, vaultVerifier, activeProviderId. Top-bar Settings button is unwired (onOpenSettings never called) though sidebar /settings link works.
- **NEXT bounded candidates:** (a) Phase 8.1 wllama CDN consent/availability honesty; (b) Phase 9.1 onboarding-completion persistence + boot rehydrate + routing guard (self-contained, high-value, needs an app_settings write + a listener + a guard); (c) Phase 9.2 settings persistence (app_settings-backed). Verify each with an Explore first.

## 2026-06-13T07:38:37Z - Claude Opus 4.8 - Pass 30: Phase 9.1 — persist onboarding completion + index-route guard
- **Bounded task: onboarding was orphaned dead UI.** Explore confirmed the /onboarding route existed but NOTHING routed users to it — the index '/' did `<Navigate to="/chat">`, and app.onboardingComplete (comment claims it "gates the /onboarding route") had ZERO consumers. Completion lived only in Redux, lost on reload. So onboarding never showed on first run and "don't repeat if completed" was impossible.
- **What I built:** (1) NEW src/settings/appSettings.ts — generic durable getSetting<T>/setSetting over the app_settings table (mirrors providerProfiles' activeProviderId pattern) + typed getOnboardingComplete/setOnboardingComplete + ONBOARDING_COMPLETE_KEY='onboardingComplete'. This is the shared settings layer Phase 9.2 will reuse. (2) OnboardingScreen.finish() is now async: `await setOnboardingComplete(db,true)` before dispatch+navigate; onClick wrapped as `() => void finish()`. (3) main.tsx restoreOnboardingState(): reads the flag at boot, dispatches onboardingCompleted() if true, THEN hydrated() in a finally (order matters — see below). (4) NEW IndexRedirect.tsx gates ONLY the index route: renders null until app.hydrated, then Navigate to /chat (completed) or /onboarding (first run). router.tsx index element swapped from Navigate to <IndexRedirect/>.
- **Race handled:** the index guard must not bounce a returning user to onboarding during the first paint before the persisted flag is read. Fix = the `hydrated` flag (was defined in appSlice but never dispatched until now). IndexRedirect renders nothing while !hydrated; boot dispatches onboardingCompleted() BEFORE hydrated() so the guard sees a consistent state in one render.
- **e2e safety:** gating ONLY '/' is deliberate — no e2e spec navigates to bare '/' (all deep-link to /chat, /models, etc., grep-confirmed), so deep links are untouched and all 28 e2e stayed green. A first-run user landing on '/' now correctly sees onboarding.
- **Tests +6 (289->295):** NEW appSettings.test.ts (round-trip + onboarding default-false/persist-true, 3), NEW IndexRedirect.test.tsx (null before hydrated; ->onboarding first-run; ->chat returning, 3 — uses MemoryRouter+Routes with marker elements, findByText since Navigate fires in an effect after dispatch). Extended OnboardingScreen.test.tsx: added `import 'fake-indexeddb/auto'` + afterEach clear; finish test now asserts getOnboardingComplete(db) is true (findByText('Chat screen') since finish is async).
- Gate GREEN: typecheck, lint --max-warnings 0, 295 vitest, 28 e2e both browsers. No Rust changes. Ticked 9.1 "persist completion" + "don't repeat if completed" + "completion persists test"; left mode/provider/storage/step persistence honestly unticked (each a small app_settings write to add later via the now-landed appSettings.ts).
- **NEXT bounded candidates:** (a) Phase 9.1 finish-up — persist inference mode + active provider (setActiveProviderId in onboarding) + storage result + step-resume, all via appSettings.ts; (b) Phase 9.2 Settings persistence — SettingsScreen toggles are decorative/Redux-only (theme/default-provider/lock-timeout/approval-policy/dev-mode); wire to app_settings via getSetting/setSetting; ALSO top-bar Settings button is unwired (onOpenSettings never called). (c) Phase 8.1 wllama CDN consent/availability honesty (WASM auto-loads from jsdelivr CDN with no consent/integrity gate — genuine "browser-local but silently hits CDN" deception). Verify each with an Explore first.

## 2026-06-13T07:45:48Z - Claude Opus 4.8 - Pass 31: Phase 9.1 — persist onboarding's remote provider durably
- **Bounded task (continuation of Pass 30):** onboarding's remote-mode provider pick was dispatched to Redux (activeProviderSet) but NEVER persisted — setActiveProviderId(db) was only ever called from ModelsScreen (on a successful provider test). So restoreActiveProvider() (main.tsx, reads activeProviderId from app_settings on boot) found nothing → the user's onboarding provider choice silently vanished on reload.
- **Fix:** OnboardingScreen.finish() remote branch now `await setActiveProviderId(db, provider)` before the Redux dispatch. One-line behavior fix + import.
- **Deliberately SKIPPED inference mode persistence:** grep confirmed `mode` (wllama/local/remote) has NO consumer anywhere post-onboarding — persisting it would be a speculative/inert field. Honest call: don't store data nothing reads. Revisit when a consumer exists. (Noted in TODO 9.1 status.)
- **Test +1 (295->296):** OnboardingScreen.test "persists the remote provider selection durably" — clicks the remote ModeCard (button name /Use OpenAI \/ Anthropic/i), walks to finish, asserts the Provider combobox defaults to 'anthropic', finishes, then waitFor getActiveProviderId(db)==='anthropic'. afterEach app_settings.clear() already covers the ACTIVE_PROVIDER_SETTING key.
- Gate GREEN: typecheck, lint --max-warnings 0, 296 vitest, 28 e2e both browsers. No Rust changes. Ticked 9.1 "persist selected active provider"; updated 9.1 status note.
- **Phase 9.1 now: completion + active provider persist + index-route guard all done.** Remaining 9.1 (honest-unticked): storage-persistence result + step-resume (small app_settings writes); inference mode deliberately omitted (no consumer).
- **NEXT bounded candidates:** (a) Phase 9.2 Settings persistence — SettingsScreen toggles are decorative/Redux-only (theme/default-provider/lock-timeout/approval-policy/dev-mode), wire to app_settings via getSetting/setSetting; top-bar Settings button also unwired (onOpenSettings never called). (b) Phase 8.1 wllama CDN consent/availability honesty (WASM auto-loads from jsdelivr w/ no consent/integrity gate — "browser-local but silently hits CDN" deception). (c) Phase 9.1 storage-result persistence. Verify each with an Explore first. NOTE: 3 passes done this window (29 model audit, 30 onboarding persistence+guard, 31 provider persistence) — consider a /compact checkpoint before a deeper Phase 9.2/8.1 pass.

## 2026-06-13T07:53:00Z - Claude Opus 4.8 - Pass 32: Phase 9.2 — wire top-bar Settings button to /settings
- **Bounded task (first slice of Phase 9.2):** the top-bar Settings button (TopStatusBar.tsx, onClick={onOpenSettings}) was a SILENT NO-OP — AppLayout.tsx built the topBar prop object but never passed onOpenSettings, so clicking did nothing. Broken affordance / quiet deception. (Settings was only reachable via the left SidebarNav NavLink.)
- **Fix:** AppLayout.tsx now imports useNavigate and passes `onOpenSettings: () => navigate('/settings')` into the topBar object that AppShell spreads into TopStatusBar. One small wiring change.
- **Deliberately SCOPED OUT (this pass):** the model-selector button (onSelectModel) is the SAME class of broken affordance but is out of Phase 9.2 scope (belongs to Models phase) and needs its own test/route reasoning — left for a follow-up to keep this bounded. Also did NOT touch the 14 decorative SettingsScreen controls (theme/provider/lock-timeout/approval-policy/dev-mode etc.) — that's the bigger 9.2 persistence slice (needs a settings layer/slice + boot rehydrate; reuse src/settings/appSettings.ts getSetting/setSetting from Pass 30).
- **Test +1 (296->297):** NEW src/AppLayout.test.tsx — renders AppLayout in MemoryRouter (child routes /chat + /settings) using the real `store` singleton (has all reducers; runtime initialState fatal:false so the top bar renders); clicks `getByRole('button', {name:/settings/i})` (the SidebarNav "Settings" is role=link, so the button match is unique) and asserts findByText('SETTINGS SCREEN'). No store factory needed.
- Gate GREEN: typecheck clean, lint --max-warnings 0, 297 vitest, 28 e2e both browsers. No Rust changes. Ticked 9.2 "Settings button in top bar navigates" + test "top bar Settings button navigates".
- **NEXT bounded candidates:** (a) Phase 9.2 SETTINGS PERSISTENCE proper — make SettingsScreen controls read from + write to app_settings (start with ONE setting end-to-end, e.g. theme: settings layer/slice + boot rehydrate in main.tsx + SettingsScreen wiring + persist-after-reload test). (b) wire onSelectModel (model button) to /models. (c) Phase 8.1 wllama CDN consent/integrity gate. (d) Phase 9.1 storage-result persistence. Verify each with an Explore first. NOTE: 4 passes this window (29/30/31/32) — STRONGLY consider a /compact checkpoint before the deeper 9.2 persistence or 8.1 pass.

## 2026-06-13T08:06:48Z - Claude Opus 4.8 - Pass 33: Phase 9.2 — lock-timeout setting wired END-TO-END (first real persisted setting)
- **What & why:** Chose lock timeout as the first Phase 9.2 setting to wire because it has a GENUINE existing consumer — SecretVault's `#lockTimeoutMs` drives a real `setTimeout(() => this.lock(), …)` auto-lock timer (secretVault.ts:243). Theme was the obvious candidate but is a TRAP: the app is single-theme (Tailwind v4 `@theme static`, light tokens only, no darkMode config), so persisting a theme value that changes nothing visible would itself be a hollow affordance — exactly the deception this effort kills. dev/demo/fallback mode is env-gated (VITE_DEMO_MODE/appConfig), not runtime-mutable. So lock timeout was the only honest end-to-end unit.
- **Changes (commit pending):**
  - `secretVault.ts`: made `#lockTimeoutMs` mutable (was readonly); added `setLockTimeout(ms)` — updates the field and re-arms the live timer via `#armLockTimer()` ONLY if currently unlocked (while locked, value applies on next unlock; 0 disables).
  - `appSettings.ts`: added `LOCK_TIMEOUT_MINUTES_KEY='lockTimeoutMinutes'`, `getLockTimeoutMinutes(db)` (default 15), `setLockTimeoutMinutes(db,n)` over the same app_settings table.
  - `main.tsx`: added `restoreLockTimeout()` boot fn (parallel to restoreActiveProvider/restoreOnboardingState) — reads minutes, calls `secretVault.setLockTimeout(minutes*60_000)` so the persisted timeout governs idle-locking after reload.
  - `SettingsScreen.tsx`: lock-timeout `<Select>` is now CONTROLLED — useEffect reads getLockTimeoutMinutes(db) on mount (active-guard prevents post-unmount setState), onChange writes setLockTimeoutMinutes(db,n) + secretVault.setLockTimeout(n*60_000). Added `aria-label="Lock timeout"` so it's queryable as combobox. The other ~13 controls remain decorative/local-only (documented).
- **Tests +5 (297->302), all paired same commit:** secretVault.test.ts ×2 (re-arms when shortened while unlocked; honours timeout set before unlock — both fake-timer + advanceTimersByTime, matching the existing auto-lock test). appSettings.test.ts ×2 (default 15; persists 60). SettingsScreen.test.tsx ×1 (seed 60 -> control shows '60' on mount -> selectOptions '5' -> app_settings now 5). Added `import 'fake-indexeddb/auto'` + `afterEach db.app_settings.clear()` to SettingsScreen.test.tsx (it now reads IndexedDB on mount).
- Gate GREEN: typecheck clean, lint --max-warnings 0, 302 vitest, 28 e2e both browsers. No Rust changes.
- **TODO ticked (9.2):** `lock timeout;` (end-to-end, with evidence) + test `settings persist after reload;` (proven for the lock-timeout setting). Left UNTICKED the two umbrella boxes (read-from / write-to IndexedDB) since only one control is wired so far — honest partial state.
- **PRE-EXISTING format debt (NOT mine, NOT in the gate):** `pnpm format:check` flags 5 files I didn't touch (SnapshotRestoreBanner.test.tsx, runtimeHost.ts, OnboardingScreen.test.tsx, runtimeSlice.test.ts, runtimeSlice.ts). The gate is `pnpm test` = pretest(lint)+vitest; format:check is NOT wired into it, so these don't fail the build. Worth a cleanup pass someday, or wiring format:check into pretest.
- **NEXT bounded candidates:** (a) continue Phase 9.2 — wire the NEXT real-consumer setting (approval policy once a decision-path consumer exists; or backup settings). Avoid theme until dark mode is actually implemented. (b) wire onSelectModel (model button) to /models. (c) Phase 8.1 wllama CDN consent/integrity gate. (d) Phase 9.1 storage-result persistence. Verify each with an Explore first. NOTE: this is Pass 33 (5th deep pass since the last compact was at the start of THIS window) — a /compact checkpoint before the next pass is reasonable.

## 2026-06-13T08:26:56Z - Claude Opus 4.8 - Pass 34: Phase 9.2 — wire top-bar provider/model button to /models (hollow-affordance fix)
- Explore-first confirmed the SAME hollow-affordance class as the Pass-32 Settings button: `TopStatusBar` already fires `onSelectModel` (and TopStatusBar.test.tsx proves the callback fires), and both `/models` route + a fully-implemented `ModelsScreen` exist — but `AppLayout.tsx` only passed `onOpenSettings`, never `onSelectModel`, so the combined provider/model button (title "Change active model or provider") was a SILENT no-op.
- **Fix (1 line of behavior):** `AppLayout.tsx` topBar now also wires `onSelectModel: () => navigate('/models')`, mirroring the Settings-button pattern exactly (useNavigate at top of the layout route).
- **Test +1 (302->303), paired same commit:** AppLayout.test.tsx — added a `/models` route to the MemoryRouter harness and a test clicking the provider/model button (queried by title) → lands on MODELS SCREEN. Mirrors the existing Settings-nav test.
- Gate GREEN: typecheck clean, lint --max-warnings 0, 303 vitest, 28 e2e both browsers. No Rust changes. (Prettier collapsed my test's click to one line; format:check clean on both touched files.)
- **TODO ticked (9.2):** added two parallel ticked entries next to the Settings-button ones — "Provider/model button in top bar navigates to /models" + test "top bar provider/model button navigates;" (these weren't pre-existing checkboxes; added as the matching hollow-affordance cleanup).
- **Both top-bar buttons (Settings + provider/model) are now real affordances.** The remaining decorative Settings controls are unchanged (theme still BLOCKED until dark mode exists; approval policy/backup/skill-policy/default-provider/dev-demo-mode still local-only).
- **NEXT bounded candidates:** (a) Phase 9.2 next real-consumer setting (approval policy once a decision-path consumer exists; or backup settings) — avoid theme until dark mode lands, avoid dev/demo (env-gated). (b) Phase 8.1 wllama CDN consent/integrity gate. (c) Phase 9.1 storage-result persistence + step-resume. (d) Phase 9.3 Models screen: replace defaultValue-only fields with controlled persisted state. Explore-first each. NOTE: Pass 34 = 1st deep pass since the /compact at the start of THIS window.
- **PRE-EXISTING format debt unchanged:** `pnpm format:check` still flags 5 files I didn't touch (not in the gate).

## 2026-06-13T08:54:56Z - Claude Opus 4.8 - Pass 35: Phase 8.1 — fail-closed wllama CDN consent gate
- Explore-first confirmed a GENUINE, fully-unimplemented gap: wllama runtime WASM auto-loaded from `https://cdn.jsdelivr.net/npm/@wllama/wllama@3.4.1/.../wllama.wasm` (engine.ts WASM_URL) the first time a browser-local model was downloaded/loaded/inferred — NO consent gate, NO integrity check, NO audit. A "browser-local" app silently fetching remote code = fail-closed violation. wllama is fully wired (engine, provider, ModelsScreen Download/Load/Delete, catalog, real-browser e2e), so the gap was real, not premature.
- BOUNDED FIX (one coherent vertical slice, fail closed + a real grant path so no half-built feature ships):
  1. `appSettings.ts`: `WLLAMA_CDN_CONSENT_KEY` + `getWllamaCdnConsent`/`setWllamaCdnConsent`, **default false** (fail closed).
  2. `engine.ts`: new exported `WllamaCdnConsentError`; `WllamaEngineOptions.requireCdnConsent?: () => Promise<boolean>`; `getInstance()` checks it BEFORE the dynamic import/CDN fetch and throws if denied (so denied = zero remote fetch); `getWllamaEngine()` wires `requireCdnConsent: () => getWllamaCdnConsent(db)`.
  3. `SettingsScreen.tsx`: Models-section "Load runtime from CDN" Toggle (read on mount, persist on change, mirrors the Pass-33 lock-timeout pattern); the change is audited via `recordAudit` as `settings.wllama_cdn_consent_granted`/`_revoked` (source `model` — `settings` is NOT a valid AuditSource; valid = user/runtime/provider/storage/skill/backup/model/system; risk medium on grant).
  4. `WllamaProbe.tsx`: self-grants consent (`setWllamaCdnConsent(db,true)`) before download — it's an authorized verification surface, keeps the heavy extended e2e green. (ModelCacheProbe uses the blob store directly, NOT getInstance, so it's unaffected.)
- Paired tests (same commit): appSettings (+2: default false, persists), engineConsent.test.ts (+2, NEW — vi.mock('@wllama/wllama/esm/index.js') with a ctor-counting fake: deny→rejects WllamaCdnConsentError AND ctor count 0 i.e. no fetch; grant→constructed once + loadModelFromHF called), SettingsScreen (+2: toggle defaults off→persists grant→audited; reflects persisted value on mount).
- GREEN GATE: typecheck clean; lint --max-warnings 0 (one fix: dropped unused `_config` ctor param in the mock); **vitest 303→309**; prettier clean on touched files; **28 e2e ×chromium+firefox**. Lesson reaffirmed: ESLint flags unused args even when `_`-prefixed in this config.
- TODO 8.1 ticks (with evidence): "Stop silently loading from CDN", "require explicit user consent" (chosen approach), "pin exact version" (3.4.1 pinned — but full SRI/byte-hash still TODO), and the "CDN use is explicit if retained" test. STILL OPEN in 8.1: vendor/bundle the asset OR add real SRI integrity; offline/runtime availability status; audit the actual runtime LOAD success/failure inside the engine load path; "wllama unavailable state shown" + "load failure visible and audited" tests.
- NEXT candidates (Explore-first each): (a) 8.1 leftover — audit engine runtime load success/failure + offline availability status (smaller, finishes 8.1); (b) 8.1 integrity — vendor the wasm via Vite OR compute+verify SRI hash (larger); (c) 9.1 storage-persistence result + step-resume; (d) 9.3 ModelsScreen replace defaultValue-only fields with controlled persisted state. Still contract-blocked (need claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot triggers + 3.3 effect-lifecycle audit. Housekeeping: 5 pre-existing files fail `prettier --check` (not in gate).

## 2026-06-13T09:01:18Z - Claude Opus 4.8 - Pass 36: Phase 8.1 — audit wllama runtime load success/failure
- Explore confirmed the runtime WASM load itself was never audited (only model.* ops were) and engine.ts had no audit access. Distinct value: "the runtime couldn't load at all" vs "this model failed".
- FIX (bounded, mirrors the requireCdnConsent injection so the factory stays pure/testable):
  - `engine.ts`: added `WllamaEngineOptions.onRuntimeLoad?: (loaded: boolean) => void`; `getInstance()` now try/catches the dynamic import + `new Wllama()`, calling `onRuntimeLoad(false)` then rethrow on failure, `onRuntimeLoad(true)` on success. A consent-gate block does NOT call it (policy block ≠ load attempt; already captured at model level). `getWllamaEngine()` wires onRuntimeLoad → `appendAuditEvent(db, …)` as `runtime.wllama_load_succeeded`/`runtime.wllama_load_failed` (source **'runtime'**, which already exists in AuditSource; risk info/low). engine.ts now imports appendAuditEvent from ../audit/auditService.ts (no cycle).
- Tests (engineConsent.test.ts, all paired): extended the vi.mock fake Wllama with a hoisted `failCtor.on` flag so the ctor can throw. deny→onRuntimeLoad NOT called (+ no ctor); grant→onRuntimeLoad(true) exactly once; ctor-throw→onRuntimeLoad(false) once + rejects 'runtime fetch failed'; INTEGRATION→ real `getWllamaEngine().load()` (consent set true) writes a durable runtime-source `runtime.wllama_load_succeeded` event. Used vitest `toHaveBeenCalledExactlyOnceWith` (works in v4.1.8).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 309→311**, prettier (had to --write engineConsent.test.ts once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 8.1 now ticked: stop-silent-CDN, require-consent, pin-version, "CDN use explicit" test (Pass 35) + **audit runtime load success/failure (Pass 36)**. STILL OPEN in 8.1: vendor/bundle the asset OR real SRI/byte-hash integrity (3.4.1 is pinned but bytes unverified); "Add offline/runtime availability status" (UI surface — sidebar health for wllama currently always 'unconfigured'; could dispatch providerHealthSet('wllama',…)); tests "wllama unavailable state shown" (ModelsScreen has the !wllamaSupported banner but no test stubbing isWllamaSupported=false) and "load failure is visible and audited" UI-layer test (ModelsScreen runModelAction shows a danger toast but no UI test asserts it). These are the obvious Pass 37/38 candidates (Explore-first each).
- Reusable facts: AuditSource enum (src/db/types.ts:130) = user/runtime/provider/storage/skill/backup/model/system — 'settings' is NOT valid. appendAuditEvent(db, input) writes durably without dispatch (good for module-level/non-React code); recordAudit(db, dispatch, input) also updates the live Redux audit tail. ModelsScreen error UX = `runModelAction(promise, msg)` → danger toast (engine throws bubble up through modelManager which also audits model.*_failed).

## 2026-06-13T09:04:35Z - Claude Opus 4.8 - Pass 37: Phase 8.1 — UI-layer guardrail tests (unavailable banner + download failure visible/audited)
- Test-only pass locking in already-implemented but UNTESTED behavior (allowed when behavior genuinely exists & is uncovered): ModelsScreen's !wllamaSupported warning banner and runModelAction danger toast.
- Added to ModelsScreen.test.tsx (+2, 7->9 in file):
  1. "shows the unavailable banner when wllama is unsupported": `vi.stubGlobal('WebAssembly', undefined)` -> isWllamaSupported() false -> asserts the "browser-local models can't run here" warning renders. (Robust vs jsdom: by default jsdom has WebAssembly='object' but Worker='undefined', so support is already false in tests — but stubbing makes intent explicit.)
  2. "shows a visible, audited error when a model download fails": `vi.stubGlobal('Worker', class {})` to ENABLE the (otherwise-disabled) Download button, then with CDN consent at its fail-closed default, clicking Download throws WllamaCdnConsentError -> asserts danger toast "Could not download the model." is visible AND a durable model.download_failed audit (status failure) lands. No module mock needed: the consent gate throws before the dynamic import, so the real @wllama import never runs in jsdom.
- KEY ENV FACT (verified by a throwaway probe): in the vitest jsdom env, `typeof WebAssembly === 'object'` but `typeof Worker === 'undefined'`, so isWllamaSupported() returns FALSE by default and the wllama Download button is disabled in tests unless you stub Worker. afterEach already does vi.unstubAllGlobals().
- GREEN GATE: typecheck (unchanged), lint --max-warnings 0, **vitest 311->313**, prettier clean (touched file), **28 e2e ×chromium+firefox**. Committed/pushed.
- Phase 8.1 STATUS after this pass: all the spec's Tests items ticked (unavailable shown / CDN explicit / load failure visible+audited), plus stop-silent-CDN, require-consent, pin-version, audit-runtime-load. ONLY remaining 8.1 gap: real INTEGRITY (vendor/bundle the wasm via Vite OR compute+verify an SRI/byte hash — 3.4.1 is pinned but bytes are unverified) and the optional "offline/runtime availability status" UI surface (sidebar wllama health is always 'unconfigured'; could dispatch providerHealthSet('wllama',…)). Both are larger/optional; 8.1 is otherwise effectively complete.
- NEXT candidates (Explore-first): (a) 8.2 Model Download Manager (real queue state / accurate progress / cancel / quota check) — note modelManager already tracks per-model download status+progress in modelsSlice, so verify the real gap first; (b) 8.1 integrity via vendoring (Vite asset) or SRI — larger; (c) 9.1 storage-persistence result + step-resume; (d) 9.3 ModelsScreen replace remaining defaultValue-only Settings fields with controlled persisted state. Still contract-blocked (need claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot triggers + 3.3 effect-lifecycle audit. Housekeeping: 5 pre-existing files fail prettier --check (not in gate).

## 2026-06-13T09:10:44Z - Claude Opus 4.8 - Pass 38: Phase 8.2 — storage-quota preflight before model download (hollow-affordance fix)
- Explore confirmed: of 8.2's items, "accurate progress" was already DONE (modelCache.fetchGguf streams real content-length bytes); the genuine HOLLOW AFFORDANCE was "check storage quota before download" — quota was displayed (top bar, Storage screen) but NEVER enforced, so a too-large model download just surprise-failed mid-stream. Also CatalogModel had only a human `sizeLabel` string, no numeric size to compare.
- FIX (bounded vertical slice):
  - `catalog.ts`: CatalogModel gained `sizeBytes: number`; filled all 3 entries via MB/GB consts (105MB, 1.1GB, 0.5GB).
  - `modelManager.ts`: new exported `InsufficientStorageError`; `createModelManager` gained a 4th param `estimate: () => Promise<StorageUsage> = estimateStorage` (injectable, defaults to live navigator.storage). `download()` now runs a PREFLIGHT first: `const {usedBytes,quotaBytes}=await estimate(); if (quotaBytes>0 && usedBytes+model.sizeBytes>quotaBytes)` -> dispatch status 'error', audit `model.download_blocked` (failure/low), throw InsufficientStorageError BEFORE engine.download. quotaBytes===0 means estimate unavailable (jsdom/privacy) -> proceed (can't assess; don't false-block).
- Tests (wllama.test.ts, +2): "blocks a download that would exceed the storage quota" (inject estimate {0,1024}, ~105MB model -> rejects InsufficientStorageError + engine.download spy NOT called + status 'error' + model.download_blocked audit) and "allows a download when the quota is sufficient" (10GB quota -> ready). Existing download tests stay green because default estimate in jsdom returns {0,0} -> preflight skipped. ModelsScreen download-failure test also still green (ModelsScreen uses default 3-arg createModelManager -> {0,0} -> skip preflight -> consent gate throws as before).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 313->315**, prettier (had to --write modelManager.ts once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 8.2 ticks: accurate progress (already done), check-quota-before-download, handle-quota-failure-during-download, + the "quota preflight blocks too-large model" test. STILL OPEN in 8.2: real download QUEUE state (modelsSlice has a 'queued' status value but it's never used/shown — currently honest because never displayed; a true multi-model queue is unimplemented); CANCEL (no AbortController/AbortSignal anywhere in engine/modelCache/fetchGguf, no cancel button — MISSING); PAUSE (impossible without upstream support — honest fix is to NOT add a pause control); "delete cache removes actual cached data and metadata" + "load/unload status reflects real engine state" (PARTIAL — engine has loadedModelId() but it's never reconciled with modelsSlice.activeModelId); tests: failed-download-not-cached, cancel-updates-state, delete-cache-removes-record, loaded-status-requires-actual-loaded-model.
- NEXT candidates (Explore-first): (a) 8.2 CANCEL — thread an AbortSignal through fetchGguf/getOrFetchModelBlob/engine.download + a Cancel button + 'cancelled' state + audit (medium size; note wllama's loadModelFromHF may not accept a signal, so cancel may only cover the IndexedDB-fallback fetch path — verify before promising full cancel); (b) 8.2 reconcile loaded status with engine.loadedModelId() (smaller); (c) 8.3 user-provided HF repo/file form; (d) 9.1 storage-persistence result + step-resume; (e) 9.3 ModelsScreen controlled persisted Settings fields. Still contract-blocked (claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot triggers + 3.3 effect-lifecycle audit. Housekeeping: 5 pre-existing files fail prettier --check (not in gate). Phase 8.1 effectively complete except optional SRI/vendoring integrity + offline availability-status UI.

## 2026-06-13T09:13:49Z - Claude Opus 4.8 - Pass 39: Phase 8.2 — reconcile active-model selection with real engine state on delete
- Verified honesty bug against source: modelManager.remove() deleted the model + engine.deleteCache()->unload() cleared the engine's loaded id, but only dispatched modelDownloadRemoved — it NEVER cleared modelsSlice.activeModelId/activeModelLabel. So deleting the ACTIVE model left the UI (top-bar model label via AppLayout, Settings "Model", provider health) claiming a model was loaded that the engine no longer had. Direct violation of "never claim a model is loaded when it isn't".
- FIX (1 file behavior, bounded): modelManager.remove() now captures `wasLoaded = engine.loadedModelId() === model.id` BEFORE deleteCache (which unloads), and after a successful delete dispatches `activeModelSet(null)` iff wasLoaded. Uses the engine's REAL loaded id as the signal (the manager has no getState), which is exactly the "reflect real engine state" requirement.
- Tests (wllama.test.ts, +2): "clears the active model when the loaded model is deleted" (fakeEngine loadedModelId=()=>model.id; load->active set; remove->activeModelId & activeModelLabel null) and "keeps the active model when a different model is deleted" (remove a different MODEL_CATALOG[1] -> active stays model.id). Existing "load sets active; remove clears download entry" test still green (default fake loadedModelId=()=>null -> wasLoaded false -> active untouched; that test never asserted active after remove).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 315->317**, prettier clean (touched files), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 8.2 ticks: "Load/unload status must reflect real engine state" + the "loaded status requires actual loaded model" test. 8.2 REMAINING: real download QUEUE state (modelsSlice has unused 'queued' status — honest since never shown; true queue unimplemented); CANCEL (MISSING — no AbortController/signal in engine/modelCache/fetchGguf; CAVEAT: wllama.loadModelFromHF likely can't be aborted, so an honest cancel may only cover the IndexedDB-fallback fetch path — a cancel button that silently no-ops on the primary path would be a hollow affordance, so verify upstream abort support BEFORE building it); PAUSE (impossible upstream — honest fix = no pause control); tests: failed-download-not-cached, cancel-updates-state, delete-cache-removes-record (modelCache.test.ts covers store.delete API; an end-to-end remove-removes-blob test could be added).
- NEXT candidates (Explore-first): (a) 8.2 "delete cache removes actual cached data and metadata" test — small, verify engine.deleteCache->blobStore.delete actually empties db.model_blobs end-to-end; (b) 8.2 "failed download does not mark model cached" test — verify a thrown engine.download leaves no model_blobs/cache row; (c) 8.3 user HF repo/file form (bigger feature); (d) 9.1 storage-persistence result + step-resume; (e) 9.3 ModelsScreen controlled persisted Settings fields. Cancel/queue/pause are the riskier 8.2 leftovers (hollow-affordance risk) — defer until upstream abort is confirmed. Contract-blocked (claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot + 3.3 effect-lifecycle audit. Housekeeping: 5 files fail prettier --check (not in gate). 8.1 done except optional SRI/vendoring + offline availability-status UI.

## 2026-06-13T09:17:11Z - Claude Opus 4.8 - Pass 40: Phase 8.2 — cache-integrity guardrail tests (failed download not cached; deleteCache evicts data)
- Test-focused pass locking in already-correct cache behavior that was untested:
  - modelCache.test.ts (+1) "does not cache anything when the download fails": a 404 Response -> getOrFetchModelBlob rejects ("Model download failed (404)") and the store stays empty (getOrFetchModelBlob fetches first, only store.put AFTER a full successful download, so no partial/corrupt row). Verifies "avoid corrupt cache metadata on failed download" + "failed download does not mark model cached".
  - engineConsent.test.ts (+1, new "wllama engine cache deletion" describe) "deleteCache removes the cached model data from the blob store": seed an in-memory ModelBlobStore, createWllamaEngine({blobStore}), engine.deleteCache(id) -> store empty. (deleteCache calls blobStore.delete + unload; no getInstance/import triggered, so no wllama mock needed even though the file mocks it.) Verifies "delete cache removes actual cached data and metadata" (the db.model_blobs row is {modelId, blob, cachedAt} = data + metadata, deleted whole).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 317->319**, prettier clean (touched files), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 8.2 ticks: avoid-corrupt-cache-on-fail, delete-removes-data+metadata, + tests failed-download-not-cached & delete-cache-removes-record. 
- 8.2 NOW REMAINING (all the riskier ones): real download QUEUE state; CANCEL + its "cancel updates state honestly" test; PAUSE. Per earlier Explore, cancel needs AbortSignal threaded through fetchGguf/getOrFetchModelBlob/engine.download + a Cancel button + a 'cancelled' state; CAVEAT: wllama.loadModelFromHF (the primary OPFS path) likely can't be aborted, so an honest cancel may only cover the IndexedDB-fallback fetch path — a cancel button that silently no-ops on the primary path is a HOLLOW AFFORDANCE; verify upstream abort support before building. PAUSE is impossible upstream — honest fix = no pause control (don't add one). QUEUE: modelsSlice has an unused 'queued' status (honest since never shown); a true multi-model queue is a real feature, not just hardening.
- So 8.2's hardening-meaningful items are essentially DONE except cancel (blocked on upstream abort verification) and queue/pause (feature-scope / honest-omission). Good point to move OFF Phase 8 to another phase.
- NEXT candidates (Explore-first): (a) 9.1 storage-persistence result + step-resume; (b) 9.3 ModelsScreen/Settings replace remaining defaultValue-only fields with controlled persisted state (Settings "General/Theme", "Default provider/model", "Key storage mode", "Log level" are still uncontrolled defaultValue selects — but AVOID theme until dark mode lands, and confirm each has a real consumer before wiring to avoid hollow settings); (c) 8.3 user HF repo/file form (feature). Contract-blocked (claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot + 3.3 effect-lifecycle audit. Housekeeping: 5 files fail prettier --check (not in gate). 8.1 done except optional SRI/vendoring + offline availability-status UI.

## 2026-06-13T09:23:54Z - Claude Opus 4.8 - Pass 41: Phase 9.3 Models Screen — verified DONE; added llama-server health-ID regression test
- Explore confirmed Phase 9.3 was effectively ALREADY DONE in code (no real bug, no hollow affordance): ProviderCard fields are controlled+persisted (value+onChange, seeded from listProviderProfiles via useLiveQuery, saved via saveProviderProfile); health is written/read by provider id not kind (id 'llama-server' vs kind 'llama_server' kept distinct); health only set by real checkHealth(); all buttons wired; Download disabled when unsupported. The ONLY genuine gap was a missing regression test for the llama-server health-ID mapping.
- ADDED (test-only): ModelsScreen.test.tsx "keys llama-server health by its provider id, not its kind" — stub fetch reject, find the llama-server card by heading + scope with within(card), click Test, assert store.providers.health['llama-server'] is defined & != 'unconfigured' AND health['llama_server'] (the kind) is undefined. Guards against a future regression where someone keys health by kind. (Used within from @testing-library/react; card located via heading.closest('div.rounded-card').)
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 319->320**, prettier (had to --write the test once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO: ALL of 9.3 now ticked (every item + all 3 tests) with evidence. Phase 9.3 COMPLETE.
- USEFUL FACTS for next time: provider profiles (src/providers/providerProfiles.ts) — ids: openai/anthropic/compatible/ollama/llama-server; kinds: openai/anthropic/openai_compatible/ollama/llama_server; labels: OpenAI/Anthropic/OpenAI-compatible/Ollama/llama-server. ModelsScreen sections: "Remote Providers" (non-LOCAL_KINDS) and "Local Endpoints" (LOCAL_KINDS: ollama, llama_server). statusOf(id)=health[id]??'unconfigured'. activeProviderSet only on a 'connected' test result + setActiveProviderId(db,...).
- PHASE STATUS: 9.3 COMPLETE. 9.2 — only lock-timeout wired end-to-end; theme BLOCKED (single-theme app, no dark tokens); other Settings controls (autoStart/requireApproval/keyWarning/autoBackup/allowUnsigned/autoUpdate/devMode toggles + Theme/Default provider/Default model/Key storage mode/Log level selects) are local-useState-only / defaultValue-only HOLLOW affordances — but wiring them needs REAL consumers (approval policy needs a decision-path consumer; skill install policy needs skillManager; etc.) to avoid creating inert persisted fields. 9.1 — completion+active-provider persist; remaining (storage-persistence result, step-resume, mode) are mostly inert/speculative (no consumer) per prior analysis EXCEPT step-resume which is a real UX feature.
- NEXT candidates (Explore-first): (a) 9.1 onboarding step-resume (persist step index so a mid-onboarding refresh resumes — real UX, real consumer); (b) 9.2 approval-policy setting IF a real approval decision-path consumer exists (Explore the runtime/effect approval flow first — only wire if consumed); (c) Phase 10 "UI Honesty Pass" (likely targets the remaining hollow Settings toggles — Explore what Phase 10 actually lists); (d) 8.3 user HF repo/file form (feature). Contract-blocked (claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot + 3.3 effect-lifecycle audit. Housekeeping: 5 files fail prettier --check (not in gate). Phase 8 hardening-complete except cancel(blocked on upstream abort)/queue/pause + optional 8.1 SRI/availability-UI.

## 2026-06-13T09:29:12Z - Claude Opus 4.8 - Pass 42: Phase 11.2 — security regression tests (no key in localStorage; Authorization header not logged)
- Explore of the 4 secret-leak invariants: #1 (not in Redux) and #2 (not in audit) were already HOLDS+TESTED (secretLeak.test.ts, vaultWiring.test.ts, secretsSlice.test.ts, auditService.test.ts redactDetails, ModelsScreen 'sk-' assertion). #3 (localStorage) and #4 (Authorization header not logged) HELD but were UNTESTED — the two genuine gaps. No violations found.
- ADDED (test-only, +2):
  - secretLeak.test.ts "never writes a decrypted key to web storage": vi.spyOn(Storage.prototype, 'setItem') (one spy covers BOTH localStorage + sessionStorage since they share the prototype), run vault.setup + putEncryptedSecret + resolveApiKey, assert setItem never called and localStorage.length===sessionStorage.length===0. Mirrors the existing suite's vault setup (SecretVault + InMemoryVaultStore, lockTimeoutMs:0).
  - providers.test.ts "never logs the API key or Authorization header to the console": spy console.log/debug/info/warn/error (mockImplementation noop), createOpenAICompatibleProvider({fetchImpl}).complete(..., {apiKey:'sk-secret-DO-NOT-LOG'}), assert no console call contains the key or 'Bearer'.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 320->322**, prettier clean (touched files), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.2 ticks: raw-key-not-in-Redux, raw-key-not-in-audit, raw-key-not-in-localStorage, Authorization-header-not-logged (all 4 now have explicit regression tests). 11.2 REMAINING: backup import rejects raw secret-looking fields if not encrypted; skill cannot access reserved keys; skill cannot path-traverse; runtime cannot ignore missing handlers (these need Explore against backup/skill/runtime code — some may already hold+be tested, some may be contract-blocked like the runtime-missing-handlers one which relates to 1.2/5.1/7.4).
- KEY API FACTS: SecretVault ctor = new SecretVault({store, observer?, lockTimeoutMs}); InMemoryVaultStore + createReduxVaultObserver(dispatch) from vaultWiring.ts; vault.setup(passphrase), vault.putEncryptedSecret(id,label,key), resolveApiKey(vault, profileRow)->{ok,apiKey}, providerSecretId('openai'). createOpenAICompatibleProvider({id,baseUrl,model,fetchImpl}).complete(request,{apiKey}) sets header authorization:'Bearer <key>'; anthropic uses 'x-api-key'. auditService.redactDetails SECRET_KEY_PATTERN=/api[-_]?key|token|authorization|secret|password|bearer|credential/i.
- NEXT candidates (Explore-first): (a) 11.2 leftovers — backup-import-rejects-unencrypted-secrets (Explore src/backup/backupService.ts; likely real + testable), skill-reserved-keys + skill-path-traverse (Explore src/skills/skillManager.ts/skillRunner.ts); (b) 11.3 integration tests (e.g. "wllama model download failure -> truthful model status" — now well-supported by Pass 35-40 work); (c) Phase 10 empty/error states; (d) 9.1 onboarding step-resume. Contract-blocked (claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot + 3.3 effect-lifecycle audit, and likely 11.2 "runtime cannot ignore missing handlers". Housekeeping: 5 files fail prettier --check (not in gate). Phases COMPLETE/near: 8 (hardening-complete sans cancel/queue/pause + optional 8.1 SRI), 9.1 (core), 9.2 (lock-timeout only), 9.3 (COMPLETE).

## 2026-06-13T09:34:31Z - Claude Opus 4.8 - Pass 43: Phase 11.2 — backup/skill secret invariants verified + containsLikelyRawSecret direct unit tests
- Explore found ALL THREE remaining 11.2 invariants already HOLD+TESTED: (A) backup import rejects raw-secret rows (backupService.validateBackup -> containsLikelyRawSecret on every row); (B1) skill reserved keys (skillFs.isReservedStateKey gates getState/setState, skillRunner audits deny; tested in skillFs.test.ts + skillRunner.test.ts); (B2) skill path-traversal (skillFs.isPathSafe/isPathAllowed reject ../ ./ absolute backslash nullbyte + encoded forms, namespace-prefix required; tested comprehensively). No violations.
- The one marginal gap: containsLikelyRawSecret was only tested INDIRECTLY (one sk-ant pattern via validateBackup). ADDED a direct unit suite to backupService.test.ts (+11): detects each value pattern (Anthropic sk-ant-, OpenAI sk-, Slack xoxb-, AWS AKIA+16, GitHub ghp_, Google ya29., JWT eyJ.x.y) under an innocuous field name; field-name normalization ("API-Key"/"Access_Token" -> reserved); empty reserved field not flagged; secret-free metadata (apiKeyMode/encryptedSecretId) not flagged; recursion DEPTH boundary (nest(7,key)->true, nest(8,key)->false).
- DEPTH SEMANTICS (documented for future): containsLikelyRawSecret(value, depth) descends objects only while depth<=6; strings are pattern-tested whenever reached (any depth). A leaf wrapped N times is reachable iff N<=7. So detection covers up to 7 nesting levels — a bounded scan, intentional.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 322->333** (+11), prettier clean (touched file), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.2: ticked backup-secrets, skill-reserved-keys, skill-path-traverse (HOLDS+TESTED w/ evidence). 11.2 now FULLY ticked EXCEPT "Runtime cannot ignore missing handlers" which is CONTRACT-BLOCKED (tied to effect-port-fatal 1.2/5.1/7.4 needing claw-schema/WASM conversationId/skill_id on effects). 11.1 invariants (raw key not in Redux/audit/localStorage, Authorization header not logged) done in Pass 42.
- KEY API FACTS: backupService exports containsLikelyRawSecret(value, depth=0); PLAINTEXT_SECRET_FIELDS (normalized) + SECRET_VALUE_PATTERNS (7 regexes); normalizeFieldName lowercases+strips non-alphanumerics. skillFs: RESERVED_STATE_KEYS / isReservedStateKey(startsWith '__'), isPathSafe/isPathAllowed(path, namespaces), readText/writeText/getState/setState. skillRunner routes skill effects through SkillFs and audits deny() on violation.
- NEXT candidates (Explore-first): (a) 11.3 integration tests — strongest now-supported one: "wllama model download failure -> truthful model status" (Pass 35-40 built the real consent/quota/audit paths); also "provider failure -> visible error -> audit failure" likely already coverable; (b) Phase 10 empty-states/error-states (UI work, bounded per-state); (c) 9.1 onboarding step-resume (real UX feature); (d) 11.1 "remove tests that enshrine bad behavior" (Explore for any test asserting unknown-provider->mock or seeded fake data — likely already clean post-hardening). Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 effect-lifecycle audit + 11.2 runtime-missing-handlers. Housekeeping: 5 files fail prettier --check (not in gate). Phases status: 8 (hardening-complete sans cancel/queue/pause), 9.1 core, 9.2 lock-timeout only (rest blocked/hollow w/o consumers), 9.3 COMPLETE, 11.1 done, 11.2 done sans contract-blocked item.

## 2026-06-13T09:41:14Z - Claude Opus 4.8 - Pass 44: Phase 10 — MemoriesScreen sidebar empty states + audit existing empty/error states
- Explore of Phase 10 found MOST empty/error states already EXIST+TESTED. The genuine REACHABLE+SILENT gap: MemoriesScreen sidebar "Recently used" and "Retrieval history" rendered an empty <ul> with no message when no memory had lastUsedAt (memories exist but none retrieved into chat).
- FIX (MemoriesScreen.tsx): added derived `usedMemories` + `recentlyRetrieved` (computed once near pinnedCount); each sidebar list now renders "No memories used yet." / "No retrievals yet." when empty instead of a blank box. Paired test (MemoriesScreen.test.tsx +1): seed SAMPLE_MEMORIES with lastUsedAt deleted -> assert both honest empty messages render. (delete copy.lastUsedAt is allowed — lastUsedAt is optional; avoided destructure-omit to dodge no-unused-vars.)
- IMPORTANT DESIGN NOTES discovered (so future passes don't add dead UI): several Phase 10 empty states are UNREACHABLE-BY-DESIGN — "no providers" (ModelsScreen merges DEFAULT_PROVIDER_PROFILES, list never empty) and "no skills" (bundled skills auto-seed). Adding empty-state UI for those would be unreachable dead code / a hollow affordance. Ticked them with that rationale rather than building dead UI.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 333->334**, prettier (--write the test once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO Phase 10 ticks (with evidence): empty states no-providers(N/A), no-memories(+sidebar), no-audit, no-skills, no-backups, no-downloaded-models; error states runtime-failed, provider-missing, secret-locked, backup-import-failed, skill-import-failed; test "empty states render honestly". STILL OPEN Phase 10: error states "storage unavailable" (StorageScreen has no error UI if estimate/query fails) and "model unavailable" (only ephemeral toast + 'error' row status, no persistent per-model error/recovery affordance); tests "no-op buttons absent/disabled" and "error states render visibly"; top-level "Audit all visible buttons/actions", "For each action implement/disable/mark", "Remove or gate fake/demo UI data" (demo data is already env-gated via appConfig/VITE_DEMO_MODE — likely tickable after a quick verify).
- WHERE EMPTY/ERROR STATES LIVE: MemoriesScreen EmptyState "No memories yet." + sidebar; AuditScreen "No audit events." row; SkillsScreen "No skills installed."; StorageScreen "No backups yet." + danger toast "Import failed"; ModelsScreen per-row status + "No downloads in progress."; ChatScreen EmptyState "No provider configured" -> /models; SecurityScreen vault Locked badge/form (state.secrets.vaultLocked); AppLayout runtimeFatal -> RuntimeBlockedScreen (ErrorState). Components: src/components/ui/EmptyState.tsx + ErrorState.tsx. Most screens read via useLiveQuery(Dexie); providers/storage/secrets via redux.
- NEXT candidates (Explore-first): (a) Phase 10 "storage unavailable" error state — add an honest error/warning on StorageScreen when navigator.storage estimate is unavailable or quota critical (assessStorageHealth already returns level 'critical'/'warning' + recommendations — may just need surfacing); (b) Phase 10 "model unavailable" persistent error affordance in ModelsScreen table (per-row 'error' status already in slice — surface a retry/clear); (c) 11.3 integration tests (wllama download failure -> truthful status; provider failure -> visible error -> audit); (d) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 effect-lifecycle audit + 11.2 runtime-missing-handlers. Housekeeping: 5 files fail prettier --check (not in gate). DONE/near: 8 (sans cancel/queue/pause), 9.1 core, 9.2 lock-timeout, 9.3 COMPLETE, 11.1 done, 11.2 done(sans blocked), 10 mostly done.

## 2026-06-13T09:44:51Z - Claude Opus 4.8 - Pass 45: Phase 10 — honest StorageScreen health panel (remove fake all-green; surface storage-unavailable)
- Found a REAL fake-UI-data hollow affordance: StorageScreen "Local Data Health" rendered a HARDCODED all-green list ("IndexedDB healthy", "Cache storage healthy", "Local storage healthy", "Service worker healthy") with success checkmarks, and even showed "Quota tight" with a GREEN check — fabricated health claims, and it never surfaced storage-unavailable (quotaBytes 0).
- FIX (StorageScreen.tsx): replaced the hardcoded list with honest derived state — a new HealthRow helper (green Check when ok, amber AlertTriangle when not). When quotaBytes>0: shows real "Quota healthy/getting tight/critical (N% used)" tied to assessStorageHealth.level + real "Persistent storage granted/not granted". When quotaBytes===0 (estimate unavailable, e.g. jsdom/privacy mode): shows "Storage estimate unavailable in this browser." warning. Dropped the 4 fabricated subsystem lines entirely (no real signal backed them). Imported AlertTriangle from lucide-react.
- Paired test (StorageScreen.test.tsx +1, 3 total): default jsdom render (navigator.storage absent -> quota 0) asserts "storage estimate unavailable" message renders AND the fabricated "Cache storage healthy"/"Service worker healthy" lines are GONE.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 334->335**, prettier clean (touched files), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO Phase 10: ticked error-state "storage unavailable" (with the fake-data removal noted). 
- REMAINING Phase 10: error-state "model unavailable" (per-model persistent error/recovery affordance — only ephemeral toast + 'error' row status today); tests "no-op buttons absent/disabled" + "error states render visibly"; top-level "Audit all visible buttons/actions", "implement/disable/mark each action", "Remove or gate fake/demo UI data" (this pass removed ONE instance; NOTE another likely-fake value remains: StorageScreen "Model Cache" StatCard is hardcoded "0 B" / "Tracked per model" — never reflects real cached blob bytes; candidate for a follow-up — could sum db.model_blobs sizes).
- NEXT candidates (Explore-first): (a) Phase 10 "Model Cache" StatCard — compute real cached size from db.model_blobs instead of hardcoded 0 B (bounded, removes another fake value); (b) Phase 10 "model unavailable" persistent error affordance in ModelsScreen table (retry/clear on 'error' row); (c) 11.3 integration tests; (d) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 effect-lifecycle audit + 11.2 runtime-missing-handlers. Housekeeping: 5 files fail prettier --check (not in gate). DONE/near: 8(sans cancel/queue/pause), 9.1 core, 9.2 lock-timeout, 9.3 COMPLETE, 10 mostly, 11.1 done, 11.2 done(sans blocked).

## 2026-06-13T09:48:05Z - Claude Opus 4.8 - Pass 46: Phase 10 — real Model Cache size on StorageScreen (remove hardcoded "0 B")
- Continued fake-UI-data removal (Phase 10): StorageScreen "Model Cache" StatCard was hardcoded value="0 B" sub="Tracked per model" regardless of actual cache.
- FIX (StorageScreen.tsx): added a useLiveQuery over db.model_blobs computing { bytes: sum(row.blob.size), count: rows.length }; StatCard now shows formatBytes(bytes) + "N model(s) cached". Honest best-effort: reflects the IndexedDB blob-cache (the no-OPFS fallback). NOTE limitation: OPFS-cached models (Chrome primary path) aren't measurable via IndexedDB, so this counts only the blob store — still strictly more honest than the old constant 0 B (and on Firefox the blob store IS the real cache).
- Paired test (StorageScreen.test.tsx +1, 4 total): clear db.model_blobs, render, assert "0 models cached" shows and the old "Tracked per model" is gone. (Non-zero size not asserted: jsdom Blobs don't round-trip through fake-indexeddb reliably — see modelCache.test.ts node-env note; the summation is a trivial reduce.)
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 335->336**, prettier clean, **28 e2e ×chromium+firefox**. Committed/pushed.
- Phase 10 "Remove or gate fake/demo UI data": NOT yet ticked (umbrella) — removed 2 instances in StorageScreen (Local Data Health + Model Cache) across Pass 45-46, but other screens may still have decorative/static values (e.g. Settings "Up to date" badge, duplicated APP_VERSION rows) — would need a sweep before ticking. Demo/seed data itself is already env-gated via appConfig/VITE_DEMO_MODE.
- NEXT candidates (Explore-first): (a) Phase 10 sweep "Audit all visible buttons/actions" + remaining static values across screens (Settings/Security) to finish the fake-data umbrella — but BEWARE: many Settings controls are local-only toggles that need real consumers, not just removal; (b) Phase 10 "model unavailable" persistent error affordance in ModelsScreen table; (c) 11.3 integration tests (wllama download failure -> truthful status; provider failure -> visible error -> audit); (d) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 effect-lifecycle audit + 11.2 runtime-missing-handlers. Housekeeping: 5 files fail prettier --check (not in gate). DONE/near: 8(sans cancel/queue/pause), 9.1 core, 9.2 lock-timeout, 9.3 COMPLETE, 10 mostly (storage-unavailable + 2 fake-data fixes done; model-unavailable + button-audit + tests remain), 11.1 done, 11.2 done(sans blocked).

## 2026-06-13T09:50:55Z - Claude Opus 4.8 - Pass 47: Phase 11.3 — integration test "wllama download failure -> truthful model status"
- Added ModelsScreen.test.tsx "reflects a truthful error status in the model row after a failed download" (+1): Worker stubbed (Download enabled), CDN consent at fail-closed default -> click first Download -> waitFor downloads['smollm2-135m'].status === 'error', assert NOT 'ready', and the row visibly shows /^error/. The row-status angle complements Pass 37's toast+audit test, fully covering the 11.3 "truthful model status" item.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 336->337**, prettier (--write the test once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.3 tick: "wllama model download failure -> truthful model status". Other 11.3 integration tests still open (onboarding->provider->chat; provider failure->visible error->audit [provider failure audit IS tested in ModelsScreen, the "visible error" health badge could be added]; memory write approval->persisted->audit; backup export->import preview->restore [StorageScreen has export + import preview Dialog + restore — likely coverable]; skill import->permission review->enable->audit; reload->runtime snapshot restored).
- CONTEXT NOTE: This window has now run Passes 35-47 (13 passes) continuously since the /compact. Lots done across Phases 8.1, 8.2, 9.3, 10, 11.1, 11.2, 11.3. Context is very large; expect compaction. All work is durably committed/pushed to origin master. Latest commit before this entry: 369ab61 (Pass 46); this pass commits next.
- NEXT candidates (Explore-first): (a) 11.3 "backup export -> import preview -> restore" integration test (StorageScreen already has the full flow + Dialog; likely a clean E2E-in-jsdom test); (b) 11.3 "provider failure -> visible error -> audit failure" (add the visible health-badge assertion to the existing failure-audit test); (c) Phase 10 model-unavailable persistent affordance; (d) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 effect-lifecycle audit + 11.2 runtime-missing-handlers. Housekeeping: 5 files fail prettier --check (not in gate). DONE/near: 8(sans cancel/queue/pause), 9.1 core, 9.2 lock-timeout, 9.3 COMPLETE, 10 mostly, 11.1 done, 11.2 done(sans blocked), 11.3 started.

## 2026-06-13T09:54:02Z - Claude Opus 4.8 - Pass 48: Phase 11.3 — backup export -> import preview -> restore integration test
- Added StorageScreen.test.tsx "imports a backup file: shows a preview, then restore writes it back" (+1, 5 in file): seed a memory, build a real backup File via serializeBackup(exportBackup(db)), user.upload to the file input (aria-label "Import backup file"), assert the "Restore backup?" preview Dialog appears (NO silent restore), wipe db.memories, click Restore, assert "Backup restored" toast + db.memories.get(id) truthy.
- GOTCHA: waitFor wasn't imported in StorageScreen.test.tsx — and it's unnecessary here: confirmRestore awaits importBackup BEFORE the success toast, so once "Backup restored" shows the write has landed; assert db directly. user.upload works on the hidden file input directly (no click needed). jsdom File.text() works for the JSONL parse.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 337->338**, prettier clean, **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.3 tick: "Backup export -> import preview -> restore". 11.3 REMAINING: onboarding->provider setup->chat; provider failure->visible error->audit failure (audit IS tested; add a visible health-badge assertion); memory write approval->memory persisted->audit persisted; skill import->permission review->enable->audit; reload->runtime snapshot restored.
- CONTEXT: Passes 35-48 (14) run continuously since /compact this window. All committed/pushed to origin master. Phases substantially done: 8.1, 8.2(sans cancel/queue/pause), 9.3 COMPLETE, 10 (most), 11.1, 11.2(sans contract-blocked), 11.3 (2 of ~7). Expect compaction soon.
- NEXT candidates (Explore-first): (a) 11.3 "provider failure -> visible error -> audit failure" — extend the existing ModelsScreen failure test to also assert the visible health badge shows the error tone (currently only asserts the audit); (b) 11.3 "skill import -> permission review -> enable -> audit" (Explore SkillsScreen flow); (c) 11.3 "memory write approval -> persisted -> audit"; (d) Phase 10 model-unavailable affordance; (e) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 + 11.2 runtime-missing-handlers + likely 11.3 "reload->runtime snapshot restored" (depends on snapshot/effect contract). Housekeeping: 5 files fail prettier --check (not in gate).

## 2026-06-13T09:56:10Z - Claude Opus 4.8 - Pass 49: Phase 11.3 — provider failure -> visible error -> audit failure
- Extended ModelsScreen.test.tsx "writes a failure audit event when a provider test fails" to ALSO assert the VISIBLE error (not just the audit): after a thrown fetch on the OpenAI Test, the health badge shows "CORS issue" (cors_error). Used findAllByText (the label appears on both the card badge and the sidebar health entry) + length>0. HEALTH_META: unconfigured="Not configured"/neutral, connected="Connected"/success, auth_failed="Auth failed"/danger, cors_error="CORS issue"/warning, model_not_found="Model not found"/warning, unreachable="Unreachable"/danger.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 338** (extended existing test, count unchanged), prettier (--write once), **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.3 tick: "Provider failure -> visible error -> audit failure". 11.3 REMAINING: onboarding->provider setup->chat; memory write approval->persisted->audit persisted; skill import->permission review->enable->audit; reload->runtime snapshot restored (likely contract-blocked — depends on snapshot/effect contract).
- CONTEXT: Passes 35-49 (15) since /compact this window. Everything committed/pushed to origin master (latest 5001139 + this pass). Phases: 8.1/8.2(sans cancel/queue/pause), 9.3 COMPLETE, 10 most, 11.1, 11.2(sans blocked), 11.3 (4 of ~7: wllama-status, backup-roundtrip, provider-failure done; download-failure visible+audited too). Expect compaction.
- NEXT candidates (Explore-first): (a) 11.3 "skill import -> permission review -> enable -> audit" (Explore SkillsScreen import/enable flow + skill_import audit); (b) 11.3 "memory write approval -> persisted -> audit" (Explore the memory approval-card path); (c) 11.3 "onboarding -> provider setup -> chat" (OnboardingScreen flow); (d) Phase 10 model-unavailable affordance; (e) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 + 11.2 runtime-missing-handlers + 11.3 reload->snapshot-restored. Housekeeping: 5 files fail prettier --check (not in gate).

## 2026-06-13T09:59:43Z - Claude Opus 4.8 - Pass 50: Phase 11.3 — skill import -> permission review -> enable -> audit
- Added SkillsScreen.test.tsx "imports a skill: reviews permissions, installs, enables, and audits" (+1, 3 in file): upload a SKILL.md (aria-label "Import skill file"), assert "Install note-taker?" permission-review Dialog appears BEFORE install, click Install (toast "Installed note-taker"), toggle "Enable note-taker", assert db.skills.get('note-taker').enabled true + audit events filtered by skillId==='note-taker' contain skill_installed AND skill_enabled.
- SkillsScreen FLOW: import file -> handleFile parses (parseClawskill for .clawskill else parseSkillMd) -> setPending -> Dialog with PermissionView (tools/read/write/network) -> Install -> confirmInstall -> manager.install(parsed, source) -> toast. Enable via per-row Toggle -> manager.setEnabled (audits skill_enabled/skill_disabled; fails closed + audits skill_enable_failed if db.skills.update no-ops on unknown id). Skill id === manifest.name. skillManager audits skill_installed/reinstalled/enabled/disabled/uninstalled/import_failed with skillId, source 'skill'. Bundled skills (web-search, summarize-pdf) auto-seed once when db.skills empty.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 338->339**, prettier clean, **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO 11.3 tick: "Skill import -> permission review -> enable -> audit". 11.3 REMAINING: onboarding->provider setup->chat; memory write approval->persisted->audit persisted; reload->runtime snapshot restored (likely contract-blocked).
- CONTEXT: Passes 35-50 (16) since /compact this window. All committed/pushed to origin master. 11.3 now 5 of 7. Phases: 8.1/8.2(sans cancel/queue/pause), 9.3 COMPLETE, 10 most, 11.1, 11.2(sans blocked), 11.3 (5/7). Expect compaction.
- NEXT candidates (Explore-first): (a) 11.3 "memory write approval -> persisted -> audit" (Explore the memory approval-card flow — ChatScreen/approval cards + memoriesSlice/db.memories + audit); (b) 11.3 "onboarding -> provider setup -> chat" (OnboardingScreen multi-step -> /chat); (c) Phase 10 model-unavailable affordance; (d) 9.1 onboarding step-resume. Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 + 11.2 runtime-missing-handlers + 11.3 reload->snapshot-restored. Housekeeping: 5 files fail prettier --check (not in gate).

## 2026-06-13T10:03:55Z - Claude Opus 4.8 - Pass 51: Phase 11.3 — verify onboarding flow covered; annotate the 2 contract-blocked integration items (docs-only)
- VERIFIED "Onboarding -> provider setup -> chat" is ALREADY fully covered: OnboardingScreen.test.tsx "persists the remote provider selection durably" picks remote (OpenAI/Anthropic) mode -> selects provider (anthropic) -> walks steps -> finish -> asserts "Chat screen" renders AND getActiveProviderId(db)==='anthropic'. Ticked with evidence (no new code needed).
- Assessed the 2 remaining 11.3 items as CONTRACT-BLOCKED / not-cleanly-testable, annotated in TODO:
  - "Memory write approval -> persisted -> audit": a runtime memory write is a storage_* effect routed through ctx.ports.storage in effectExecutor.ts:91-95 — the UNWIRED Phase 5.1 storage handler (needs claw-schema/WASM effect contract). ApprovalCard + audit_append effect exist, but persist-on-approve can't run end-to-end yet.
  - "Reload -> runtime snapshot restored": partially wired (effectExecutor runtime_snapshot_save -> db.runtime_snapshots; main.tsx withSnapshotRestore builds runtime from latest snapshot + audits/banner on failure) but the restore wrapper is in main.tsx (entry point, not export-testable) and a real reload->restore assertion needs the WASM runtime resuming from a snapshot (effect/snapshot contract). Snapshot SAVE is covered (snapshotScheduler.test.ts / effectExecutor.test.ts).
- DOCS-ONLY commit (TODO + memory; no source changed -> code gate unaffected, last full gate was green at vitest 339/28 e2e in Pass 50).
- PHASE 11.3 STATUS: 5 of 7 done with dedicated tests (provider-failure, backup-roundtrip, skill-import, wllama-status, +download-failure-visible/audited), 1 done already-covered (onboarding), 2 contract-blocked (memory-write-approval, reload-snapshot). So 11.3 is COMPLETE except the 2 contract-blocked items.
- CONTEXT: Passes 35-51 (17) since /compact this window — a very large amount of work, all committed/pushed to origin master. Phases: 8.1 done(sans optional SRI), 8.2 done(sans cancel/queue/pause — blocked on upstream abort), 9.3 COMPLETE, 10 mostly (storage fake-data fixes + sidebar empty states done; model-unavailable affordance + button-audit + 2 tests remain), 11.1 done, 11.2 done(sans runtime-missing-handlers contract-blocked), 11.3 done(sans 2 contract-blocked). Expect compaction.
- NEXT candidates (Explore-first, all need fresh context): (a) Phase 10 "model unavailable" persistent error/retry affordance in ModelsScreen table (real UI feature; 'error' row status exists, add retry/clear); (b) Phase 10 "no-op buttons absent/disabled" test + "Audit all visible buttons/actions" sweep (verify no hollow buttons remain across screens); (c) 9.1 onboarding step-resume (persist step index -> resume mid-onboarding; real UX feature, moderate); (d) 8.1 optional SRI/byte-hash integrity for wllama wasm (larger). Contract-blocked (need claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot triggers, 3.3 effect-lifecycle audit, 11.2 runtime-missing-handlers, 11.3 memory-write-approval + reload-snapshot. Housekeeping: 5 files fail prettier --check (NOT in the gate; could wire format:check into pretest OR just --write them).

## 2026-06-13T10:06:22Z - Claude Opus 4.8 - Pass 52: Phase 10 — "no-op buttons absent/disabled" test
- Extended ModelsScreen.test.tsx "shows the unavailable banner when wllama is unsupported" to ALSO assert every Download button is DISABLED when wllama can't run (no live-looking button that silently no-ops). Uses findAllByRole('button',{name:'Download'}) -> each toBeDisabled().
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 339** (extended test, count unchanged), prettier clean, **28 e2e ×chromium+firefox**. Committed/pushed.
- TODO Phase 10 tick: test "no-op buttons are absent/disabled". 
- PHASE 10 REMAINING: error-state "model unavailable" persistent affordance (retry/clear on an 'error' download row — currently only toast + 'error' status); top-level "Audit all visible buttons/actions" + "implement/disable/mark each action" + "Remove or gate fake/demo UI data" umbrellas (removed 2 fake StorageScreen values in Pass 45-46; a full cross-screen sweep would be needed to tick the umbrellas — beware Settings local-only toggles need real consumers, not removal); test "error states render visibly" (left until model-unavailable exists).
- CONTEXT: Passes 35-52 (18) since /compact this window. Enormous body of work, all committed/pushed to origin master. Context is near limits — next iteration should run with fresh context. Phases: 8.1(done sans optional SRI), 8.2(done sans cancel/queue/pause), 9.3 COMPLETE, 10(mostly: empty states + 2 fake-data fixes + no-op-buttons test done; model-unavailable affordance + button-audit umbrellas + "error states render visibly" test remain), 11.1 done, 11.2 done(sans contract-blocked), 11.3 done(sans 2 contract-blocked).
- NEXT candidates (Explore-first, fresh context): (a) Phase 10 "model unavailable" persistent retry/clear affordance on failed download rows in ModelsScreen (real UI feature; ties off "error states render visibly" test too); (b) 9.1 onboarding step-resume (persist step index, resume mid-flow — moderate); (c) 8.1 optional SRI/byte-hash integrity for the wllama wasm (larger). Contract-blocked: 1.2/5.1/7.4 + per-effect snapshot + 3.3 + 11.2 runtime-missing-handlers + 11.3 memory-write-approval + reload-snapshot. Housekeeping: 5 files fail prettier --check (NOT in gate; tangential to hardening — leave unless asked).

## 2026-06-13T10:08:13Z - Claude Opus 4.8 - Pass 53: Phase 10 — tick model-unavailable + error-states-render-visibly (verified covered, docs-only)
- Verified the "model unavailable" error state is honestly surfaced (3 ways): danger toast + audited model.*_failed + persistent truthful 'error' row status; and the Download button stays ENABLED in the 'error' state (disabled only when !wllamaSupported || downloading — confirmed ModelsScreen.tsx:390-392), so failed downloads are re-attemptable (recovery affordance). Covered by ModelsScreen.test "shows a visible, audited error..." (Pass 37) + "reflects a truthful error status..." (Pass 47). Ticked.
- Ticked "error states render visibly" test: ALL listed error states have a tested visible surface — runtime(RuntimeBlockedScreen), provider(CORS badge, Pass 49), secret-locked(SecurityScreen), model-unavailable(toast+row), backup-import(toast), skill-import(toast+audit), storage-unavailable(warning, Pass 45).
- DOCS-ONLY (TODO + memory; no source changed -> code gate unaffected; last full gate green at vitest 339/28 e2e in Pass 52).
- PHASE 10 STATUS: empty states done; error states done (runtime/storage/provider/secret/model/backup/skill all ticked); tests "no-op buttons absent/disabled" + "empty states render honestly" + "error states render visibly" all ticked. REMAINING Phase 10 (umbrellas, need a cross-screen sweep before ticking): "Audit all visible buttons/actions", "For each action implement/disable/mark", "Remove or gate fake/demo UI data" (removed 2 StorageScreen fake values Pass 45-46; other screens may still have decorative static values e.g. Settings "Up to date" badge / duplicated APP_VERSION — and many Settings toggles are local-only needing real consumers, NOT removal).
- CONTEXT: Passes 35-53 (19) since /compact this window. Massive body of work, all committed/pushed to origin master. Context is at its practical limit — strongly recommend the next iteration run with FRESH context (the Ralph Loop design intends one fresh agent per iteration; this window has done 19 in one context, well beyond that). All progress is durable in git + TODO checkboxes.
- NEXT candidates (Explore-first, FRESH context): (a) Phase 10 umbrella sweep "Audit all visible buttons/actions" — go screen by screen (Settings, Security, Workflow, Chat) listing every button/action and classify real/disabled/coming-later; fix or annotate; this also informs "Remove fake/demo UI data" (e.g. Settings static badges). BEWARE Settings local-only toggles need real consumers, not deletion. (b) 9.1 onboarding step-resume (persist step index; moderate feature). (c) 8.1 optional SRI/byte-hash integrity for wllama wasm (larger). Contract-blocked (need claw-schema/WASM conversationId/skill_id on effects): 1.2, 5.1, 7.4, per-effect snapshot, 3.3 effect-lifecycle audit, 11.2 runtime-missing-handlers, 11.3 memory-write-approval + reload-snapshot. Housekeeping (tangential, not in gate): 5 files fail prettier --check.

## 2026-06-13T12:38:20Z - Claude Opus 4.8 - Pass 54: Phase 1.2 — fail-closed effect executor (llm/skill/unknown fatal; storage audited-not-silent)
- USER DIRECTIVE: "Take the next loop pass on 1.2." Did so. KEY DISCOVERY that corrects my earlier claim: the storage half of 1.2 IS contract-coupled after all — the chat flow emits a REDUNDANT storage_put (referenceRuntime.ts:107-122, store:'messages') with NO conversation_id, while llmRunner.ts:102 ALREADY persists the assistant message to db.messages directly. So a generic storage handler can't build a valid MessageRow (no conversationId), and making storage fatal would break chat. Making storage truly fatal needs 5.1 (storage handler) + conversation_id on the storage effect (claw-schema StoragePut). The llm/skill/unknown halves ARE safe to make fatal now.
- IMPLEMENTED (effectExecutor.ts): `failEffect(ctx, effectType, reason, now)` -> durable audit `runtime.effect_failed` (source runtime, status failure, risk high) + dispatch `runtimeErrored` (status 'error', user-visible in sidebar) + throw. Wired: llm_request missing handler -> failEffect; skill_* missing handler -> failEffect; `default:` unknown effect -> failEffect. Storage_* missing handler -> AUDITED drop `runtime.effect_dropped` (status failure, risk low) but NOT fatal (honest interim; chat persists via llm handler so nothing lost). Added `import { runtimeErrored }`.
- TESTS (effectExecutor.test.ts, 4->8): added runtime reducer to makeCtx store; unknown-effect-fails (throws + runtime.status error + runtime.effect_failed audit), llm-handler-missing-fails, skill-handler-missing-fails, storage-no-handler-is-audited-not-thrown.
- BROKE THEN FIXED: runtimeHost.test.ts 3 tests failed because they submit_user_message (emits llm_request) with no ports -> now fatal. FIX: makeContext now wires a no-op `ports: { llmRequest: () => undefined }` stub (these tests exercise the host loop/snapshots, not missing handlers) + updated the stale "llm_request has no port -> no-op" comment. This confirms the fatal behavior works as intended.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 339->343** (net; +4 executor tests, runtimeHost still 5), prettier clean (4 files: effectExecutor.ts/.test.ts, runtimeHost.test.ts), **28 e2e ×chromium+firefox (chat end-to-end still passes)**. Committed/pushed next.
- TODO 1.2 ticks: llm_request + skill handlers required (fatal); audit_append + runtime_snapshot_save (inline, never missing); unknown-effect-fails; all 3 "every effect failure must" sub-items; tests skill-handler-missing/unknown-effect/failure-audited. LEFT UNTICKED (honest): top-level "missing ports fatal" + "missing handlers must fail" + storage_get/put/search handler-required + "missing storage handler fails" test — all annotated as audited-drop interim pending 5.1 + conversation_id on the storage effect.
- IMPORTANT CORRECTION to prior memory/notes: storage's part of 1.2 (and 5.1) genuinely needs a claw-schema change — add `conversation_id` to `StoragePut`/storage effects (crates/claw-schema/src/lib.rs:26-31) so the host can persist a valid row, AND/OR remove the llmRunner direct-persist redundancy. This is in addition to the 7.4 `skill_id`-on-`ToolCallProposal` gap. So the concrete claw-schema gaps are TWO: (1) ToolCallProposal needs skill_id (7.4), (2) StoragePut/storage effects need conversation_id (5.1 + storage half of 1.2). The reference runtime is TS-editable but the WASM core is the source of truth, so reference-only changes would diverge from production WASM.
- NEXT candidates (Explore-first, fresh context): (a) the claw-schema Rust changes — add skill_id to ToolCallProposal (unblocks 7.4) and conversation_id to StoragePut (unblocks 5.1 + storage-fatal in 1.2); requires editing crates/claw-schema + claw-core to populate, rebuilding claw-wasm, mirroring in effectTypes.ts, then cargo test/clippy (which CLAUDE.md says to run when Rust changes — would join the gate). (b) 5.1 storage handler TS side once conversation_id lands. (c) 9.1 onboarding step-resume. (d) Phase 10 button-audit umbrella sweep. Housekeeping: 5 files fail prettier --check (tangential).

## 2026-06-13T15:14:05Z - Claude Opus 4.8 - Pass 55: claw-schema/core RUST change — conversation_id on storage_put (unblocks 5.1)
- USER directed the Rust-side pass. Scoped to the GROUNDED gap: conversation_id on the storage effect (the one claw-core actually emits). DEFERRED skill_id-on-ToolCallProposal because nothing emits ToolCallProposal yet (7.x tool execution unbuilt) — adding it would be inert/untestable.
- WHY needed: claw-core emits storage_put {store:'messages'} during chat (resolve of llm_request), but at resolve time conversation_id wasn't available (pending map stored only the kind string), so a host storage handler couldn't build a valid MessageRow (needs conversationId). Fixed by threading it.
- RUST changes (crates/): claw-schema Effect::StoragePut gained `#[serde(default)] conversation_id: String`; RuntimeState gained `#[serde(default)] pending_conversation: BTreeMap<String,String>` (ADDITIVE — old snapshots restore with it empty, NO snapshot-version bump). claw-core: on SubmitUserMessage stash pending_conversation[llm_id]=conversation_id; on ResolveEffect remove+read it (cleanup on both success/failure) and populate StoragePut.conversation_id. Updated the core test to assert conversation_id=='c1'.
- TS MIRROR: effectTypes.ts storage_put gained conversation_id; referenceRuntime.ts mirrored exactly (RuntimeSnapshot.pending_conversation, stash on submit, read+populate on resolve, include in snapshot(), tolerate missing on restore via ?? {}); referenceRuntime.test.ts asserts conversation_id=='c1'; effectExecutor.test.ts storage_put literal updated.
- WASM REBUILT: `pnpm run build:wasm` (wasm-pack 0.13.1, wasm32 target present). Only src/runtime/wasm/claw_wasm_bg.wasm changed (the .js/.d.ts glue unchanged since exported methods are the same — the change is internal serde). All 5 wasm/ files are git-tracked (force-added despite the dir's `*` gitignore), so the rebuilt binary commits.
- GREEN GATE (BOTH languages): `cargo test --workspace` (claw-core 5 / schema 1 / testkit 2), `cargo clippy --workspace --all-targets` clean, typecheck, lint --max-warnings 0, **vitest 343** (assertions updated, count steady), prettier clean (4 TS files), **28 e2e ×chromium+firefox (loads the REAL rebuilt wasm — true integration check)**. Committed/pushed next.
- BEHAVIOR THIS PASS: no observable change yet — conversation_id is now POPULATED on storage_put but still audited-dropped (no storage handler). This is the contract prerequisite. TODO updated: 5.1 section + 1.2 storage note annotated that conversation_id has landed; 5.1 is no longer contract-blocked.
- BUILD/TOOLCHAIN FACTS for next time: cargo 1.94, wasm-pack 0.13.1 both on PATH; `pnpm run build:wasm` = `wasm-pack build crates/claw-wasm --target web --out-dir ../../src/runtime/wasm --out-name claw_wasm`; TS loads it via wasmRuntime.ts (import './wasm/claw_wasm.js' + claw_wasm_bg.wasm?url). Snapshot version is SNAPSHOT_SCHEMA_VERSION=1 in referenceRuntime.ts only (Rust has no version constant; the host stamps the row). Additive #[serde(default)] fields avoid version bumps. claw-core mirrors referenceRuntime.ts 1:1 — keep them in sync.
- NEXT (Explore-first): 5.1 storage handler — implement createStorageEffectHandler (store 'messages' -> db.messages.put building a MessageRow from {conversation_id, value:{role,content}} + a generated id + createdAt; validate/reject unknown stores; audit failures), wire ctx.ports.storage in main.tsx, REMOVE llmRunner's redundant db.messages.put (make runtime storage_put the single source of truth — careful: keep the chat live-query display working; llmRunner still dispatches runStateSet/chat slice + resolve_effect), then flip effectExecutor storage branch from audited-drop to fatal-on-missing (finishes 1.2 storage half). HIGH RISK: touches the chat hot path + e2e — verify chat e2e at each step. Then 7.4 needs skill_id on ToolCallProposal (only once tool execution emits it). Housekeeping: 5 files fail prettier --check (tangential).

## 2026-06-13T17:43:39Z - Claude Opus 4.8 - Pass 56: Phase 5.1 storage handler + 1.2 storage-fatal (chat persistence refactor)
- USER directed: implement the 5.1 storage handler, wire it, remove the redundant llmRunner persist, flip executor storage to fatal. Done — the message-persistence ownership moved from llmRunner to the runtime's storage_put (single source of truth).
- NEW: src/runtime/storageRunner.ts createStorageEffectHandler({db,dispatch,now?}) — storage_put for store 'messages' builds a conversation-scoped MessageRow (id=randomUUID, conversationId=effect.conversation_id, role validated against MessageRole allowlist, content string, createdAt=now). Fails closed (audit storage.effect_failed source 'storage' status failure + throw) on: unknown store, malformed record, or storage_get/storage_search (not emitted yet). storage_put is FIRE-AND-FORGET (claw-core doesn't put it in pending), so no resolve needed.
- WIRED: main.tsx ctx.ports.storage = createStorageEffectHandler({db, dispatch}). 
- REMOVED: llmRunner.ts db.messages.put block (the redundant direct persist) — now llmRunner just runStateSet('idle') + resolve_effect({ok:true,text}); the runtime emits storage_put which the handler persists. Updated llmRunner doc comment.
- FATAL: effectExecutor.ts storage branch now uses failEffect on missing port (same as llm/skill) — replaced the Pass-54 audited-drop. 1.2 storage half COMPLETE.
- TESTS: storageRunner.test.ts (NEW, +4): put persists scoped message; rejects+audits unknown store; rejects+audits malformed record; fails closed on unsupported get/search. effectExecutor.test.ts storage test reworked: "fails when the storage handler is missing" (throws + runtime status error). llmRunner.test.ts first test reworked: "resolves the effect with the reply (persistence is the storage port)" — asserts llmRunner does NOT persist an assistant row + resolves with {ok:true, text contains 'Mock response'}. Other llmRunner tests (failure/secret_locked/apikey) unaffected (failure paths never persisted).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 343->347** (+4 storageRunner; executor/llmRunner reworked not net-added), prettier clean (7 files), **28 e2e ×chromium+firefox — chat converses end-to-end via the runtime storage path (the real integration proof: user msg -> llm_request -> resolve -> storage_put -> handler persists assistant -> display)**. No cargo changes this pass. Committed/pushed next.
- TODO TICKS: 5.1 storage_put + validate-collections + validate-row + reject-unknown + audit-failures + tests (put-persists, rejects-unknown, invalid-audited); 1.2 ALL now ticked (refactor-fatal, all Require-handlers incl storage, missing-handlers-fail, missing-storage-handler-fails test). 5.1 storage_get/search left unticked (fail-closed, not emitted; "storage_search returns records" test deferred). PHASE 1.2 COMPLETE. Phase 5.1 effectively complete except get/search (no consumer yet).
- ARCH NOTE: message persistence flow is now: ChatScreen persists USER msg to db.messages -> submit_user_message -> llm_request -> llmRunner reads history, calls provider, resolve_effect(text) -> runtime emits storage_put{conversation_id, store:'messages', value:{role:'assistant',content}} -> storageRunner persists ASSISTANT msg -> chat live-query displays. llmRunner no longer persists.
- NEXT candidates (Explore-first): (a) 7.4 tool-execution enforcement — needs skill_id on ToolCallProposal (claw-schema) BUT ToolCallProposal still isn't emitted by claw-core, so still premature until tool execution is built. (b) 5.2 "remove fake memory seeding" (Explore). (c) 9.1 onboarding step-resume. (d) Phase 10 button-audit umbrella sweep. (e) 11.3 memory-write-approval / reload-snapshot now that storage handler exists — memory-write-approval may be partially unblocked (storage handler persists; but the approval-card->memory.write path is separate from chat messages — Explore whether a memory.write effect exists). Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1; `pnpm run build:wasm` rebuilds the wasm.

## 2026-06-13T19:22:10Z - Claude Opus 4.8 - Pass 57: Phase 9.1 — onboarding step-resume (persist + restore in-progress setup)
- Verified 5.2 (remove fake memory seeding) was ALREADY fully done (seeding demo-gated via appConfig.isDemoMode, all boxes ticked) — skipped. Picked 9.1 step-resume (real bounded UX gap: OnboardingScreen step/mode/endpoint/provider were local useState, so a mid-setup reload restarted at step 0).
- IMPLEMENTED: appSettings.ts gained OnboardingProgress {step,mode,endpoint,provider} + ONBOARDING_PROGRESS_KEY + getOnboardingProgress/setOnboardingProgress/clearOnboardingProgress. OnboardingScreen: useEffect on mount restores saved progress (clamps step to [0,STEPS.length-1], validates mode) and a `restored` flag gates a second useEffect that writes progress on every step/mode/endpoint/provider change (so defaults never clobber saved progress before load). finish() now clearOnboardingProgress(db) after setOnboardingComplete.
- TESTS: OnboardingScreen.test.tsx +2 ("persists in-progress selections so a reload can resume" — select remote + continue -> onboardingProgress {mode:'remote',step:1}; "resumes at the saved step on reload" — seed {step:2,mode:'remote',provider:'openai'} -> mounts on "Configure model"/Step 3 of 4 with provider restored). appSettings.test.ts +1 (round-trip + clear). Existing onboarding tests unaffected (fresh db -> restore loads nothing -> defaults; restore only setStep if progress present, so no clobber of test interactions).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 347->350** (+3), prettier clean (4 files), **28 e2e ×chromium+firefox (onboarding now does db reads on mount — still green)**. No cargo changes. Committed/pushed next.
- TODO 9.1 ticks: "Persist selected inference mode", "If setup incomplete, resume correct step", tests "selected mode persists" + "refresh resumes incomplete onboarding". LEFT: "Persist selected/default model" (partial — wllama model select in step 2 still defaultValue-only, not in progress payload; add when it has a downstream consumer) and "Persist storage persistence result" (inert — isStoragePersisted reads live browser state, persisting to app_settings would be redundant/speculative). So 9.1 is now essentially complete except those two deliberately-deferred items.
- KEY PATTERN (reuse): async-restore + gated-write — init useState defaults; useEffect mount loads persisted value (set a `restored` flag when done); a second useEffect guarded by `if (!restored) return` writes on change. Avoids the default-clobbers-saved race.
- NEXT candidates (Explore-first): (a) Phase 10 button-audit umbrella sweep ("Audit all visible buttons/actions" + "Remove or gate fake/demo UI data" — go screen by screen, BEWARE Settings local-only toggles need real consumers not deletion); (b) 11.3 memory-write-approval — now that a storage handler exists, Explore whether a memory.write effect/approval-card path exists (chat-message storage_put is separate from a memory.write); (c) 5.3 "Complete Memory Filters and Provenance" (Explore unchecked items); (d) 7.4 still premature (ToolCallProposal not emitted). Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1; pnpm run build:wasm.

## 2026-06-13T22:03:58Z - Claude Opus 4.8 - Pass 58: Phase 3.3 — audit backup import; reconcile skill/secret audit events
- Checked remaining non-blocked work: 5.3 provenance/retrieval = BLOCKED (no memory-write/retrieval pipeline). Found stale 3.3 annotations + one real gap (backup import not audited).
- REAL CODE: StorageScreen.confirmRestore was fire-and-no-audit AND had no error handling (an importBackup throw would be unhandled). Now: try/catch — success -> recordAudit 'backup.imported' (source 'backup', success) + toast; failure -> recordAudit 'backup.import_failed' (failure) + danger toast "Restore failed". Added `import { recordAudit }`. Mirrors the export audit (runBackupExport already did backup.exported/export_failed).
- TEST: extended StorageScreen.test "imports a backup file..." to also assert a durable backup.imported (source 'backup') event after the restore. (A UI import-FAILURE test isn't cleanly triggerable: validateBackup is thorough — allowlisted collections + per-collection key-field checks — so a bad backup is rejected at PREVIEW, never reaching importBackup; the post-validation throw is a Dexie-infra edge handled defensively. Didn't mock-force it.)
- RECONCILED STALE 3.3 annotations (verified against source, ticked): "skill installed/enabled/disabled/import failed" — skillManager already audits skill_installed/reinstalled/enabled/disabled/uninstalled/import_failed (SkillsScreen.test asserts install+enable from Pass 50). "secret unlocked/locked/deleted" — secretVault audits secret_unlocked + secret_locked via the observer (left unticked: deletion isn't a wired action yet).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 350** (extended test, steady), prettier clean (StorageScreen.tsx/.test.tsx), **28 e2e ×chromium+firefox**. No cargo changes. Committed/pushed next.
- TODO 3.3 ticks: backup export/import success/failure; skill installed/enabled/disabled/import-failed. Remaining 3.3 (all genuinely blocked/pending): effect emitted/resolved/failed (effect-lifecycle, tied to runtime), tool proposed/approved/rejected/edited (7.4 ToolCallProposal not emitted), memory created/updated/deleted (no memory-write pipeline), secret deleted (no delete action).
- BIG PICTURE — remaining non-blocked codeable work is thinning. Mostly-blocked clusters: memory-write/retrieval pipeline (5.3 provenance+retrieval, 11.3 memory-approval, 3.3 memory events), tool execution (7.4, 3.3 tool events — needs ToolCallProposal emission + skill_id), effect-lifecycle audit (3.3, 3.x). Genuinely-doable remaining: Phase 8.3 user-HF-model form (real feature: validate repo/file shape + form UI + downloads-go-to-HF-not-app-server which fetchGguf already satisfies); Phase 10 button-audit/fake-data umbrella sweep (open-ended, per-screen); 8.2 cancel (blocked on wllama abort support); 9.2 Settings real-consumer toggles (most need consumers). 
- NEXT candidates (Explore-first): (a) 8.3 user HF model support — start with the pure HF-reference validator (validate repo 'owner/name' + file '*.gguf' shape; reject URLs/paths) + assert fetchGguf builds an HF URL not an app-server URL; the add-model form UI is the bigger follow-up. (b) Phase 10 per-screen button audit. Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T01:29:03Z - Claude Opus 4.8 - Pass 59: Phase 8.3 — HF reference validator + single HF download-URL builder
- USER directed the 8.3 HF-reference validator. Implemented the bounded, pure core (form UI deferred).
- NEW src/wllama/hfReference.ts: validateHfReference(repoInput, fileInput) -> {ok:true,reference}|{ok:false,reason}. Accepts ONLY a bare repo id "owner/name" (2 segments, charset [A-Za-z0-9._-]) + a ".gguf" file path within the repo (subdirs allowed). Rejects: empty, URLs/schemes (looksLikeUrl: ':' '\\' '//'), wrong extension, absolute ('/'), and '..' traversal — each with a user-facing reason. Plus hfDownloadUrl(ref) = https://huggingface.co/<repo>/resolve/main/<file> as the SINGLE URL builder.
- REFACTOR: modelCache.fetchGguf now uses hfDownloadUrl(model) instead of the inline template (CatalogModel is structurally an HfReference: repo+file). One source of truth for the HF URL → the "downloads go to HF not app server" guarantee is now centralized + testable.
- TESTS src/wllama/hfReference.test.ts (+14): accepts well-formed + subdir + ALL MODEL_CATALOG entries; it.each rejects 10 bad inputs (empty/malformed repo, URL, space, wrong ext, '..', absolute, url-file); hfDownloadUrl origin === https://huggingface.co.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 350->364** (+14), prettier (--write hfReference.ts once), **28 e2e ×chromium+firefox**. No cargo changes. Committed/pushed next.
- TODO 8.3 ticks: "Validate URL/repo/file shape", "Do not proxy model downloads through app server", + all 3 tests (valid accepted / invalid rejected / URL points to HF). LEFT (follow-up): "Add form for user-provided HF repo/file" (ModelsScreen add-model form UI calling validateHfReference — the validator is ready), "Show size/license if discoverable" + "Warn when metadata cannot be fetched" (need an HF metadata fetch).
- NEXT candidates (Explore-first): (a) 8.3 add-model FORM UI on ModelsScreen — a repo+file form that runs validateHfReference, shows the reason on invalid, and on valid adds a user model the existing download/load path can use (the modelManager/engine work with any CatalogModel-shaped {id,name,repo,file,sizeLabel,sizeBytes}; sizeBytes unknown for user models -> quota preflight skip when 0, or fetch metadata). (b) 8.3 HF metadata fetch (size/license + warn-on-failure). (c) Phase 10 button-audit/fake-data sweep. (d) 9.2 Settings real-consumer toggles. Blocked clusters unchanged (memory-write/retrieval pipeline; tool execution/7.4; effect-lifecycle audit). Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T01:43:30Z - Claude Opus 4.8 - Pass 60: Phase 8.3 — add-model form UI (user-provided HF models)
- Built the ModelsScreen "Add a Hugging Face model" form on top of the Pass-59 validator. Completes 8.3 except the size/license metadata fetch.
- NEW src/wllama/userModels.ts: addUserModel(db, repo, file) -> validateHfReference -> persist ModelCatalogRow {id:'hf:<repo>/<file>', provider:'wllama', label:'<repo> · <file>', repo, file} to db.model_catalog (unused table before this); listUserModels(db) -> CatalogModel[] (toCatalogModel maps row: name=label, sizeLabel from sizeBytes or 'Unknown size', sizeBytes ?? 0 so quota preflight skips); removeUserModel(db,id); isUserModelId(id)=startsWith('hf:').
- ModelsScreen: useLiveQuery(listUserModels) merged after MODEL_CATALOG into `localModels`; table maps localModels. Form (repo+file Inputs + "Add model" Button + inline error <p text-danger> + "downloads directly from huggingface.co" note); handleAddModel sets addError on invalid (persists nothing), clears+toasts on valid. Actions cell now: Load(ready) | Download(not ready, disabled if !wllamaSupported); Delete(ready & built-in, cache only); Remove(user model, always — manager.remove(model).then(removeUserModel) = cache+catalog row). Built-ins can't be removed from the catalog; user models can.
- TESTS ModelsScreen.test.tsx (+2): "adds a user-provided HF model to the table" (type owner/name + model.gguf -> Add -> row 'owner/name · model.gguf' appears + db.model_catalog has it); "rejects an invalid HF model with a reason and persists nothing" (bad repo -> /must look like/ reason + model_catalog count 0). Added db.model_catalog.clear() to afterEach.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 364->366** (+2), prettier clean (3 files), **28 e2e ×chromium+firefox**. No cargo. Committed/pushed next. (Process note: avoid inventing TODO checkboxes — I briefly added a "Persist user model" line not in the spec, then folded it into the form item's evidence.)
- TODO 8.3 tick: "Add form for user-provided HF repo/file". REMAINING 8.3 (follow-up): "Show size/license if discoverable" + "Warn when metadata cannot be fetched" (need an HF metadata fetch — GET https://huggingface.co/api/models/<repo> or HEAD the file for content-length; populate sizeBytes/sizeLabel + a license field; warn on fetch failure). With metadata, user-model sizeBytes would feed the quota preflight.
- 8.3 STATUS: validate-shape + downloads-to-HF + form + persistence + all 3 validator tests DONE; only size/license metadata remains.
- NEXT candidates (Explore-first): (a) 8.3 HF metadata fetch (size/license + warn) — the last 8.3 piece; bounded (a fetch + populate sizeBytes + handle failure). (b) Phase 10 button-audit/fake-data sweep. (c) 9.2 Settings real-consumer toggles. Blocked clusters unchanged (memory-write/retrieval; tool-exec/7.4; effect-lifecycle audit). Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T01:56:04Z - Claude Opus 4.8 - Pass 61: Phase 10 — UI honesty sweep (disable hollow Settings controls; remove dead button + fake badge)
- Explore-swept all screens for hollow affordances. Found: SettingsScreen had 12 controls that flipped/accepted input but had NO consumer (local useState `flags` or defaultValue-only) — theme, auto-start, default-provider, default-model, key-storage-mode, require-approval, warn-keys, auto-backup, allow-unsigned, auto-update, log-level, dev-mode; OnboardingScreen "View setup guide" button had no onClick; SettingsScreen Version panel had a fake "Up to date" badge (no update check). Genuinely-wired controls confirmed: lock-timeout, Load-runtime-from-CDN, Reset-runtime.
- FIX (SettingsScreen.tsx): removed the dead `Flags` state + toggle(); every unwired Toggle is now `disabled` with checked=<its default> + a shared `noop` onCheckedChange; every unwired Select/Input got `disabled`; added a header note "Disabled controls are placeholders … don't take effect yet. Lock timeout and 'Load runtime from CDN' are active." Removed the fake "Up to date" Badge (kept the version Rows — runtime version == app version is honest, runtime is bundled). Toggle already supported `disabled`.
- FIX (OnboardingScreen.tsx): replaced the dead "View setup guide" button with "Setup guide coming soon." text.
- TESTS: SettingsScreen.test — replaced "toggles a setting and resets the runtime" (the approval toggle is now disabled) with two tests: "shows unwired preferences as disabled placeholders, not fake switches" (require-approval/auto-backup/dev-mode disabled; Load-runtime-from-CDN NOT disabled) + "resets the runtime (a real action)". No new OnboardingScreen test (the removed button had none).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 366->367** (split 1 test into 2), prettier clean (3 files), **28 e2e ×chromium+firefox (renders settings + onboarding still green)**. No cargo. Committed/pushed next.
- TODO Phase 10 ticks: "Audit all visible buttons/actions"; "For each action: disable / mark coming later" (implement sub-item left — wired ones already done, the rest have no consumer to implement against, would be speculative); "Remove or gate fake/demo UI data" (fake Up-to-date badge removed; StorageScreen fakes removed earlier; demo data env-gated). Phase 10 is now essentially COMPLETE (empty states, error states, no-op-buttons, fake-data, button-audit all addressed across Passes 44-46, 52-53, 61).
- DESIGN NOTE: when a control has no real consumer yet, the honest Phase-10 treatment is DISABLE it (visibly inactive) + a note, NOT delete it (it signals intent) and NOT wire it to nothing. Reuse: pass `disabled` + `onCheckedChange={noop}` (noop = ()=>undefined to dodge no-empty-function) for placeholder Toggles.
- NEXT candidates (Explore-first): remaining work is mostly the BLOCKED clusters (memory-write/retrieval pipeline → 5.3 provenance+retrieval, 11.3 memory-approval, 3.3 memory events; tool execution → 7.4 + 3.3 tool events, needs ToolCallProposal emission + skill_id; effect-lifecycle audit). Genuinely-doable: 8.3 HF metadata fetch (size/license + warn-on-failure — the last 8.3 piece); 8.2 cancel (blocked on wllama abort support — verify upstream first); wiring real Settings consumers as features land (approval policy, etc.). Could also do Phase 11.4 "Required Commands" doc reconciliation / Phase 12 acceptance checklist review. Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T02:13:12Z - Claude Opus 4.8 - Pass 62: Phase 8.3 — HF metadata (size/license) + fixed an onboarding write race
- COMPLETED Phase 8.3. NEW src/wllama/hfMetadata.ts fetchHfModelMetadata(ref, fetchImpl=fetch): best-effort — HEAD hfDownloadUrl -> Content-Length -> sizeBytes (CORS-safelisted header); GET https://huggingface.co/api/models/<repo> -> cardData.license || license. Each lookup in its own try/catch (empty catch allowed via a comment to dodge no-empty); if NEITHER found -> {ok:false, reason} so the caller can warn.
- WIRING: ModelCatalogRow + CatalogModel gained optional `license`. userModels.toCatalogModel passes license through (conditional spread for exactOptionalPropertyTypes); NEW updateUserModelMetadata(db,id,{sizeBytes?,license?}) merges discovered metadata onto the row. ModelsScreen.handleAddModel: after a successful add, awaits fetchHfModelMetadata(result.model) -> ok: updateUserModelMetadata (Size column shows real size + feeds quota preflight; Model cell shows "License: <id>"); not ok: warning toast "Model metadata unavailable" (model still added). Table Model cell renders the license line when present.
- TESTS: hfMetadata.test.ts (node env, +3: size+license / partial / failure). ModelsScreen.test.tsx: stubbed fetch (HEAD content-length + API license) in the add test, assert license shown + sizeBytes/license persisted; new "still adds a model but warns when HF metadata is unavailable" (fetch rejects -> warning toast). NOTE: had to stub fetch in the add tests because handleAddModel now hits the network.
- BUG FIXED (gate caught it): the full suite flaked on OnboardingScreen "persists in-progress selections" (expected 'remote' got 'wllama'). Root cause = Pass-57 onboarding wrote progress on every change via `void setOnboardingProgress(...)` — overlapping IDB puts to the SAME key whose RESOLVE order isn't guaranteed under fake-indexeddb, so a stale default {mode:'wllama'} write could land last. FIX: serialize writes through a `useRef<Promise>` chain (writeChain.current = writeChain.current.catch(()=>undefined).then(()=>setOnboardingProgress(...))) so fire-order == resolve-order, latest state wins. Added useRef import. Verified: full suite green x2.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 367->371** (+4: hfMetadata 3 + ModelsScreen metadata-unavailable 1; onboarding split unchanged net), prettier clean, **28 e2e ×chromium+firefox**, ran full suite TWICE to confirm the race fix. No cargo. Committed/pushed next.
- TODO 8.3 ticks: "Show size/license if discoverable", "Warn when metadata cannot be fetched", + the Tests parent. PHASE 8.3 COMPLETE. (Whole Phase 8 now: 8.1 done sans optional vendoring/SRI; 8.2 done sans cancel/queue/pause; 8.3 COMPLETE.)
- LESSON (reuse): when persisting on every state change via `void put(sameKey)`, overlapping async writes can resolve out of order under fake-indexeddb — serialize via a ref promise chain (or debounce+single write). The async-restore + gated-write pattern from Pass 57 now also needs the serialized-write guard.
- NEXT candidates: remaining work is the BLOCKED clusters (memory-write/retrieval pipeline; tool-exec/7.4 needs ToolCallProposal emission + skill_id; effect-lifecycle audit) + 8.2 cancel (verify wllama abort support first) + wiring real Settings consumers as features land. Could also review Phase 11.4 Required-Commands / Phase 12 acceptance checklist. Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T02:22:20Z - Claude Opus 4.8 - Pass 63: Tool-execution cluster — investigated, declined to build (speculative); corrected TODO
- User asked to unblock tool execution (emit ToolCallProposal + skill_id) thinking it was a bounded contract pass like conversation_id. Explore proved it is NOT: there is NO tool-execution loop at all. (a) claw-core NEVER constructs Effect::ToolCallProposal (grep: only the schema definition; runtime handles only SubmitUserMessage/ResolveEffect, never parses LLM results for tool calls). (b) approval flow is UI-ONLY — effectExecutor turns tool_call_proposal into approvalRequested, but ApprovalCard/approvalsSlice/ChatScreen only mutate Redux; nothing resolves an approval back to RuntimeHost (no listener drives host.submit on approval; runtimeListeners only fires on userMessageSubmitted). (c) NO tool registry/runner — skills' permissions.tools is only DISPLAYED (SkillsScreen), never executed/checked.
- DECISION: declined to build it. Adding skill_id + enforcement now = speculative DEAD CODE (a field nothing populates guarding a path nothing triggers) → violates the canonical no-fake/no-no-op rule. CORRECTED my earlier over-optimistic "contract pass like conversation_id" framing (conversation_id was grounded: storage_put IS emitted live; ToolCallProposal is not).
- The real build would be a MULTI-PASS FEATURE w/ genuine design: (1) runtime parses tool calls from LLM result + emits ToolCallProposal{skill_id} + tracks pending (Rust core + schema + wasm); (2) approval listener sends ResolveEffect back on approve/reject; (3) tool registry/runner that executes named tools — and (3) has real security design (web/page/file tools = network/CORS/sandbox). Only after 1-3 does skill_id enforcement (7.4) + tool-lifecycle audit (3.3) land on a path that runs.
- DOCS-ONLY change this pass: rewrote the 7.4 annotation ("Enforce skill permissions during tool execution") and the 3.3 "tool proposed/approved/rejected/edited" annotation to state the FULL blocker (not just "add skill_id") so a future pass doesn't naively add the inert field. No code, no gate (markdown only). Committed/pushed.
- IMPORTANT for future passes: do NOT add skill_id to ToolCallProposal or write tool-permission enforcement until the tool-execution loop (emit→approve→execute→resolve) actually exists. It would be dead code.
- BLOCKED-CLUSTER STATUS (definitive): tool execution (7.4 + 3.3 tool events) = no loop, multi-pass speculative feature. memory-write/retrieval pipeline (5.3 provenance+retrieval, 11.3 memory-approval, 3.3 memory events) = no memory-write effect path. effect-lifecycle audit (3.x) = tied to runtime. These are the bulk of remaining unchecked items and all need substantial feature/runtime work, not bounded contract changes.
- GENUINELY-DOABLE remaining (small/bounded): 8.2 cancel (verify wllama abort support FIRST — may be a hollow-affordance trap), 8.1 optional vendoring/SRI integrity for the wasm (larger), wiring real Settings consumers as features land. Otherwise the hardening TODO's bounded TS-side work is largely DONE (Phases 1.2, 2, 3.1/3.3-core, 4-core, 5.1-storage_put, 8.x, 9.1/9.2-partial/9.3, 10, 11.1, 11.2, 11.3-non-blocked). 
- NEXT: ask the user whether to (a) commit to the multi-pass tool-execution feature (with the tool-set/security design decision), (b) take a remaining bounded item (8.2 cancel after verifying abort support), or (c) pause — the easy wins are mostly exhausted; what's left is feature-scope or blocked. Housekeeping: 5 files fail prettier --check (tangential). Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T02:52:20Z - Claude Opus 4.8 - Pass 64: Tool execution feature (user-approved multi-pass) — Pass 1/4: tools foundation
- USER chose Option 1 (build the tool-execution feature). DESIGN DECISIONS (via AskUserQuestion): (1) protocol = TEXT-MARKER convention — model emits a fenced ```tool JSON block {"tool","args"} (works with ANY model incl. local GGUF), NOT provider-native function calling. (2) first tool = a NETWORK tool now ("Page Reader" = fetch a page → text), accepting the CORS/sanitization design.
- PLANNED 4 passes: (1) tools foundation [THIS]; (2) claw-schema/core: ToolCallProposal{skill_id} emission + pending tracking + feed result back + wasm rebuild; (3) host wiring: detect block, enforce perms, queue approval, run on approve, resolve back, audit lifecycle; (4) UI: approval queue wired to runtime + show results.
- PASS 1 DELIVERED src/tools/tools.ts (+ tools.test.ts, node env, 11 tests): parseToolCall(reply) extracts the first ```tool fenced JSON block → {name,args}|null (malformed/missing tool → null = "no call"); Tool interface {name,description,run(args,ctx)}; pageReaderTool name 'Page Reader' (matches the bundled web-search skill's declared tool so enforcement is REAL not disconnected) — validates http/https URL, fetches via injectable ctx.fetchImpl, htmlToText strips script/style/tags+collapses ws, truncates 20k; TOOL_REGISTRY; runToolCall(call,{allowedTools,ctx}) FAILS CLOSED — ToolNotPermittedError if name ∉ allowedTools (the 7.4 enforcement), UnknownToolError if not in registry, else executes. NOT yet wired to runtime/approval (pieces 2-3 next) so not user-reachable — a tested building block, not a hollow UI affordance.
- ALSO FIXED a recurring flaky test (onboarding "persists in-progress selections" → 'wllama' vs 'remote'). Pass-62 serialization was INSUFFICIENT: root cause = fire-and-forget setOnboardingProgress writes from a component that UNMOUNTS (e.g. a prior test's component in default wllama mode, or after finish()) resolving across boundaries and clobbering. FIX: an `alive` useRef set false on unmount (useEffect cleanup); the chained write skips if !alive.current. This also fixes a real (tiny) prod bug — a late write resurrecting progress after finish() cleared it. Verified: full suite green 4x (382 tests stable).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 371->382** (+11 tools), prettier clean, **28 e2e ×chromium+firefox**, full suite run 4x to confirm the flake is gone. No cargo this pass. Committed/pushed next.
- LESSON (reuse): fire-and-forget IDB writes from unmounting components leak across React-testing-library auto-cleanup boundaries → guard chained async writes with an alive-ref (skip post-unmount). The async-restore+gated-write+serialized-write pattern now also needs the alive-ref guard for components that unmount mid-flow.
- NEXT (Pass 2/4, Rust): claw-schema Effect::ToolCallProposal gains skill_id; claw-core: when an llm_request resolves with a reply containing a tool block, emit ToolCallProposal{skill_id, name, args} + track pending (so the tool result can be resolved back → emit a follow-up llm_request with the result). Mirror in effectTypes.ts + referenceRuntime.ts; rebuild wasm. DESIGN Q for Pass 2: where does skill_id come from in a plain chat (no active skill)? Likely: chat carries an optional active-skill context; base agent (no skill) either can't call tools (fail closed) or has its own allowlist — decide in Pass 2. Toolchain: cargo 1.94 + wasm-pack 0.13.1.

## 2026-06-14T03:08:10Z - Claude Opus 4.8 - Pass 65: Tool execution 2/4 — runtime emits ToolCallProposal{skill_id} (Rust + TS)
- Fail-closed model chosen by user: tools require an active skill that declared them. Built piece (1): the RUNTIME emits a tool proposal attributed to the active skill.
- RUST (claw-schema): Effect::ToolCallProposal +#[serde(default)] skill_id; Command::SubmitUserMessage +#[serde(default)] skill_id; RuntimeState +#[serde(default)] pending_skill (additive, no snapshot version bump). claw-core: SubmitUserMessage stashes pending_skill[llm_id]=skill_id; on ResolveEffect of an llm_request, reads conversation_id+skill_id, and if result has {tool_call:{name,args}} emits ToolCallProposal{id, skill_id, name, args, risk:'medium'} + tracks pending(proposal)=convo+skill (so a later pass can continue the loop) — else the existing text/error logic. claw-testkit.user_message + core test submit() helper updated for the new field. cargo test 6 in core + clippy clean.
- TS MIRROR: effectTypes.ts ToolCallProposal +skill_id (required), Command submit_user_message +skill_id? (optional, matches serde default). referenceRuntime.ts mirrored exactly (pending_skill in snapshot + init tolerate-missing; readToolCall helper; submit stashes skill; resolve emits tool_call_proposal when readToolCall hits). effectExecutor.test tool_call_proposal literal +skill_id. referenceRuntime.test +1 (tool proposal attributed to 'web-search'). WASM REBUILT (pnpm run build:wasm; only the .wasm binary changed).
- GREEN GATE: cargo test+clippy, typecheck, lint --max-warnings 0, **vitest 382->383** (+1 ref test; effectExecutor test modified), prettier clean, **28 e2e ×chromium+firefox**. Committed/pushed.
- KEY DESIGN: skill_id flows command(SubmitUserMessage.skill_id) -> pending_skill -> ToolCallProposal.skill_id. The proposal-RESOLUTION arm (tool ran -> result -> follow-up llm_request) is NOT yet implemented in the core (resolving a 'tool_call' pending currently hits the _ => no-op arm) — deferred to Pass 3 where the host actually runs the tool. So pending(proposal) is tracked but its continuation is Pass 3.
- NEXT (Pass 3/4, host wiring — the big one): llmRunner must (a) parse the reply for a ```tool block (parseToolCall from src/tools/tools.ts) and, if found, resolve the llm_request with {tool_call:{name,args}} instead of {text}; effectExecutor's tool_call_proposal handler must ENFORCE perms — look up the skill (skill_id), check the tool ∈ skill.permissions.tools (and skill enabled), DENY (audit + resolve_effect rejected) if not, else queue approvalRequested; on APPROVE -> runToolCall (src/tools) -> resolve_effect with the tool result -> (Pass 3b core continuation: emit storage_put(role:'tool') + follow-up llm_request); on REJECT -> resolve_effect rejected. Also: the core needs the proposal-resolution continuation arm (storage_put tool message + follow-up llm_request) — do in Pass 3 alongside host. And SubmitUserMessage needs an active skill_id from the chat UI (where does the user pick the active skill? Pass 4 UI — for now chat sends skill_id:'' so tool calls are denied/none, which is the fail-closed default). llmRunner currently FILTERS OUT role==='tool' from history — must INCLUDE tool messages for the follow-up to see results. Audit tool lifecycle (3.3). Toolchain: cargo 1.94 + wasm-pack 0.13.1.
- HONEST STATUS: tool loop is ~half-built (foundation + runtime emission). Not user-reachable yet (chat sends no skill_id, host doesn't parse/run). Passes 3-4 make it real. This remains a multi-pass feature in progress; nothing claims tools work in the UI.

## (3a) - Claude Opus 4.8 - Tool execution 3a/4: runtime continuation arm (approve->store result+ask again; reject->note)
- claw-core + referenceRuntime: ResolveEffect Some("tool_call") arm. Approved ({text}): message_count++, emit [storage_put{role:'tool',content}, llm_request{follow-up, prompt:''}, audit tool_result_stored] + stash pending(follow-up)=convo+skill. Rejected ({ok:false}/error): emit [storage_put{role:'tool',content:'Tool call was not completed.'}, audit tool_call_rejected], NO follow-up (turn ends). The runtime tool loop is now COMPLETE (propose->approve->store+continue / reject->note). Host doesn't run tools yet (3b).
- cargo test 7 in core + clippy clean; referenceRuntime.test +1 (approved tool stores result + asks again); vitest 384; wasm rebuilt; 28 e2e. Committed.
- NEXT 3b (host wiring, the big one): llmRunner parse reply for ```tool block (parseToolCall) -> resolve llm_request with {tool_call:{name,args}} (else {text}); STOP filtering role==='tool' from history. effectExecutor tool_call_proposal: ENFORCE — look up skill(skill_id) + check tool ∈ skill.permissions.tools (skill enabled) -> deny (audit + resolve_effect rejected {ok:false}) OR queue approvalRequested (carry name/args/skill_id so the approval listener can run it). Approval listener: approve -> runToolCall(src/tools) -> resolve_effect {text:output}; reject -> resolve_effect {ok:false}. Audit tool lifecycle. Then 4: UI active-skill picker + approval card wired.

## 2026-06-14T03:35Z - Claude Opus 4.8 - Tool execution 3b/4: skill-permission ENFORCEMENT (7.4) + fixed onboarding flake for good
- 3b (the security headline): src/runtime/toolRunner.ts createToolEffectHandler({db,dispatch,submit}) handles tool_call_proposal — FAILS CLOSED: deny (audit 'tool.denied' source 'skill' + resolve_effect {ok:false,error:{kind:'tool_not_permitted'}}) unless an INSTALLED + ENABLED skill (db.skills.get(skill_id)) DECLARED the tool (skill_state[skillId,'__permissions__'].value.tools includes effect.name); otherwise dispatch approvalRequested (queue; nothing runs until user approves). effectExecutor: tool_call_proposal now ROUTES to ctx.ports.tool (failEffect fatal if missing) — removed the inline approvalRequested; exported normalizeApprovalRisk (toolRunner imports it); dropped approvalRequested import. main.tsx wires ctx.ports.tool = createToolEffectHandler({db,dispatch,submit:host.submit}).
- TESTS: toolRunner.test.ts (+5: queue-when-declared [no submit], deny-undeclared [resolve rejected + tool.denied audit w/ toolName], deny-disabled, deny-unknown-skill + deny-no-skill). effectExecutor.test: "routes tool_call_proposal to the injected tool port" + "fails when the tool handler is missing". 
- TICKED 7.4 test "skill cannot call undeclared tool" (toolRunner deny test). 7.4 PARENT still unticked — enforcement exists but tool EXECUTION on approve isn't wired (host parse + approval-run listener + UI remain).
- FLAKE FIXED FOR GOOD: OnboardingScreen "persists in-progress selections" flaked ~1/3 even after serialize+alive-ref. ROOT CAUSE: vitest afterEach hooks run LIFO, so the file's `await db.app_settings.clear()` ran BEFORE testing-library's auto-unmount → a still-in-flight progress write (component still alive) landed AFTER the clear, leaking into the next test. FIX: call cleanup() explicitly at the START of the file's afterEach (unmount → alive=false → pending writes skip) BEFORE clearing. Verified: full suite 389 passed 5x in a row.
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 384->389** (+5 toolRunner, +1 effectExecutor net... 389), prettier, **28 e2e**, suite 5x stable. No cargo this sub-pass (3b is TS-only). Committed/pushed next.
- TOOL FEATURE STATUS: foundation(1) + runtime emit(2) + runtime continuation(3a) + enforcement(3b) DONE. NOT live end-to-end: llmRunner doesn't parse the reply for a tool block (always resolves {text}), approvals are UI-only (approve doesn't run the tool), chat sends skill_id:'' (so all tool calls denied — fail closed). REMAINING: 3c host-parse + approval-run listener (llmRunner parseToolCall -> resolve {tool_call}; include role:'tool' in history; an approval listener: approve->runToolCall->resolve {text}, reject->resolve {ok:false}); Pass 4 UI (active-skill picker in chat -> SubmitUserMessage.skill_id; render approval queue wired to host). Then 7.4 parent + 3.3 tool-events tick.
- CHECKPOINT: this "Pass 3" turn did a LOT (3a+3b+flake). Session is very long. Good point to report + let user decide whether to continue to 3c/4.

## 2026-06-14T03:48Z - Claude Opus 4.8 - Tool execution 3c-1/4: llmRunner parses tool calls (runtime now emits proposals from real replies)
- llmRunner.ts: after the provider reply, parseToolCall(text) (src/tools/tools.ts) -> resolve_effect with {ok:true, tool_call:{name,args}} if a ```tool block is present, else {ok:true, text} as before. ALSO: history now INCLUDES role:'tool' messages (ChatMessage has no 'tool' role, so a tool message is mapped to {role:'user', content:`Tool result:\n<content>`}) so follow-up turns see tool output. Imported parseToolCall.
- TESTS llmRunner.test (+2): reply with a ```tool block -> resolves {tool_call:{Page Reader, args}}; a prior tool message is fed back as user "Tool result:\n..." context. Existing tests unaffected (mock echo has no tool block -> still {text}).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 389->391** (+2), prettier, **28 e2e (mock provider never emits a tool block -> chat flow unchanged)**. No cargo (TS-only). Committed/pushed.
- LIVE NOW: a real model reply with a ```tool block flows llm_request -> {tool_call} -> ToolCallProposal{skill_id} -> toolRunner enforcement. In plain chat skill_id='' so every tool call is DENIED (fail closed) -> runtime stores a "Tool call was not completed." note. End-to-end success requires Pass 4 (active skill + approval-run).
- REMAINING = Pass 4 (UI + run, coupled): (a) approvalsSlice ApprovalRequest carry toolName+toolArgs; createToolEffectHandler include them in approvalRequested. (b) approval-RUN listener (runtimeListeners or a new listener on the approve/reject action): on approve -> runToolCall({name,args},{allowedTools:[name], ctx}) -> host.submit(resolve_effect {ok:true,text:output}); on reject -> host.submit(resolve_effect {ok:false}); audit tool.executed/tool.failed. (c) active-skill picker in ChatScreen -> SubmitUserMessage.skill_id (where the runtime/host build the command — find where userMessageSubmitted -> host.submit(SubmitUserMessage) is built; thread skill_id). (d) render the approvals queue (ApprovalCard) in ChatScreen wired to host (it likely already renders; verify onApprove/onReject wired to the run path). Then tick 7.4 parent + 3.3 tool-events.
- NEED TO EXPLORE for Pass 4: approvalsSlice actions (approvalResolved payload — approved vs rejected?), ChatScreen approval rendering + handlers, where userMessageSubmitted is turned into SubmitUserMessage (runtimeListeners.ts), and how the chat would pick an active skill. ChatMessage role lacks 'tool' (mapped to user prefix — revisit if a provider-native tool role is wanted).

## 2026-06-14T04:00Z - Claude Opus 4.8 - Tool execution 4a+4b/4: run on approve + active-skill picker — FEATURE COMPLETE, 7.4 ticked
- 4a (run path): approvalsSlice ApprovalRequest +toolName/+toolArgs (set by createToolEffectHandler). toolRunner.runApprovedToolCall(deps,{id,status,toolName,toolArgs}): approve -> runToolCall({name,args},{allowedTools:[name],ctx}) -> resolve {ok:true,text} + audit tool.executed; reject -> resolve {ok:false,error:user_rejected} + audit tool.rejected; tool error -> resolve {ok:false,tool_failed} + audit tool.failed. runtimeListeners: new approvalResolved listener reads the entry via api.getOriginalState() (intact even though ChatScreen dismisses after) -> runApprovedToolCall (host.submit). registerRuntimeListeners gained a {db} deps arg (main.tsx passes it).
- 4b (active skill): chatSlice +activeSkillId + activeSkillSet. runtimeListeners userMessageSubmitted reads api.getState().chat.activeSkillId -> SubmitUserMessage.skill_id (?? ''). ChatScreen: a "Active skill" Select (enabled skills via useLiveQuery db.skills.filter(enabled)) above the composer, "No skill (tools disabled)" default -> dispatch activeSkillSet. So the user picks which skill is active; none = all tool calls denied (fail closed).
- TESTS: toolRunner.test +3 (runApprovedToolCall approve/reject/error). runtimeListeners.test reworked (setup() helper + {db}; existing test asserts skill_id:''; +active-skill case; +reject-path listener). chatSlice.test +activeSkillSet + fixed initial-state assertion (+activeSkillId:null). Fixed the existing listener test (skill_id:'' now in the submit payload).
- GREEN GATE: typecheck, lint --max-warnings 0, **vitest 391->397** (+6 across 4a+4b), prettier, **28 e2e**. No cargo (4a/4b TS-only). Committed/pushed (4a = dfdc2f7; 4b commits next).
- TICKED 7.4 PARENT "Enforce skill permissions during tool execution" — the loop is LIVE end-to-end + fails closed. The TOOL-EXECUTION FEATURE (Passes 64-68) is COMPLETE: foundation -> runtime emit -> continuation -> enforcement -> host parse -> run-on-approve -> active-skill picker.
- REMAINING tool-adjacent: 3.3 "tool proposed/approved/rejected/edited" is PARTIAL (executed/rejected/denied/failed audited; "proposed" emission + "edited" not audited) — left unticked. Could add a propose-audit + edited-audit later. ChatMessage has no 'tool' role (tool results fed back as user "Tool result:" prefix) — revisit if a provider-native tool role is wanted. Only one tool (Page Reader); more tools = add to TOOL_REGISTRY.
- HOW TO USE (manual verify): enable a skill (Skills screen) that declares "Page Reader" (the bundled web-search skill does), pick it as Active skill in chat, and a model reply with a ```tool block for Page Reader -> approval card -> approve -> page fetched+summarized. With a remote provider that follows the convention. (Mock provider never emits tool blocks, so e2e chat is unchanged.)
- NEXT (if continuing): 3.3 propose/edited audits; more tools; or back to other TODO areas. Most of the hardening TODO is now done; remaining is mostly the runtime-snapshot-restore (11.3) + effect-lifecycle audit + memory-write pipeline (5.3/11.3) clusters + polish. Toolchain: cargo 1.94 + wasm-pack 0.13.1.
