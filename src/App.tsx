export default function App() {
  return (
    <main className="flex min-h-full items-center justify-center p-8">
      <div className="rounded-card border border-border bg-surface p-8 shadow-sm">
        <h1 className="text-2xl font-extrabold text-primary">BrowserClaw</h1>
        <p className="mt-2 text-md text-muted">
          Browser-only, local-first AI agent console. Design tokens in place —
          building the shared AppShell next.
        </p>
      </div>
    </main>
  );
}
