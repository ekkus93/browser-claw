export default function App() {
  return (
    <main className="flex min-h-full items-center justify-center p-8">
      <div className="rounded-xl bg-surface p-8 shadow-sm">
        <h1 className="text-2xl font-extrabold text-primary">BrowserClaw</h1>
        <p className="mt-2 text-muted">
          Browser-only, local-first AI agent console. Scaffold ready — building
          from the Phase&nbsp;0 setup.
        </p>
      </div>
    </main>
  );
}
